# 功法系统全面审查缺口清单

> ## ⚠️ 2026-07-26 复核对账（0.2.193，协议 30）
>
> 本清单正文是 2026-07-21（0.2.112 / 协议 26 时代）的历史快照。以下按当前数据源 `text_material/cultivation_methods.json`（schema_version 19，136 个功法）逐项复核；正文保留仅作历史参考，与本节冲突时以本节为准。
>
> ### 已解决
>
> - **层数配置 17/136 → 现 136/136**：全部功法在 `setting.layers_max` 配置层数（82 个 >1 层，54 个 =1 层）。正文"119 个功法缺少层数配置"作废。曾点名的 P0 功法现状：玄阴经 4 层、大衍诀 5 层、天符灵经 4 层、掩月周天功 3 层、烈焰功 1 层、长春功/青元剑诀 13 层。
> - **14 个空功法矩阵 → 现 136/136 非空**：每个功法的 `matrices` 均有数据，正文第五节的空矩阵问题作废。
> - **正文引用的 `TechniqueGateService` 不存在**：实际门槛类是 `cultivation/ProgressionGateApi.java`（配合 `TechniqueDataManager` 的条件检查）。后续引用请以真实类名为准。
>
> ### 仍待办（有效遗留项）
>
> - **精进成本个性化 `cultivation_costs`：仍 0/136**（数据与消费端均无该结构化字段）。正文第三节的建议仍完全有效。
> - **灵根纯度门槛 `purity_min`：数据与 Java 均不存在**（已用 `grep -rlE` 全库复核为零命中）。
> - **同宗门限制 `same_sect` 与叛宗惩罚：数据与 Java 均不存在**。
> - **转修网络仍无结构化边**：全库仅长春功一条 `must_convert_after: "FOUNDATION"`；长春功→青元剑诀的转修关系仍靠 `prerequisite_methods`/`prerequisite_realm_layers` 表达，无通用 `convert_to` 边、无强制转修提示、无低阶功法效率惩罚。
> - **层数已"配置"但深度不足**：多数功法层数为 3–5 层（烈焰功仅 1 层），realm band 分段映射、每层成本/解锁细化仍是后续 P1 深化目标（对应 pending_requests 中的核心功法数据深化计划）。
> - 体质专属功法、种族转换流程、生活功法多层级等正文 P1/P2 项：仍未实现，保持待办。
>
> ### 复核依据
>
> 0.2.193 构建绿（1,162 测试全过）；复核脚本见 `.bak/tmp_methods_deep_check.py`、`.bak/tmp_matrices_check.py`、`.bak/tmp_named_methods.py`（不入库）。注意：早期一次复核误扫 `data/seeking_immortals/cultivation/*.json`（477 条术法条目数据集）得出 0/136 的错误结论——功法主数据源是 `text_material/cultivation_methods.json`，且 `layers_max` 嵌套于 `setting` 下，需递归查找。

---

**审查日期**: 2026-07-21（历史快照；2026-07-26 已对账，见顶部）  
**基础数据**: 136 个功法条目，17 个配置了 layers_max，约 5176 行 JSON  
**协议版本**: 26  
**mod 版本**: 0.2.112

---

## 一、功法层数与境界带实现状态

### 1.1 已实现动态层数的功法
根据 0.2.48 特性记录，功法现已支持 authored dynamic maxima，但实际配置情况：

- **长春功 (changchun_gong)**: ✅ 已配置 `layers_max: 13`，5 个阶段 (1/4/7/10/13 层)
- **青元剑诀 (qingyuan_sword_art)**: ✅ 有 prerequisite_methods 要求长春功 13 层
- **其他功法**: ⚠️ 仅 17 个功法配置了 layers_max（其中 7 个为生活功法 layers_max: 1）

### 1.2 缺失层数配置的功法 (119/136)
**优先级 P0 - 核心战斗功法**:
- 玄阴经 (xuan_yin_art) - 魔道功法，未配置层数
- 大衍诀 (dayan_art) - 神识功法，未配置层数
- 烈焰功 (lieyan_gong) - 火系入门，未配置层数
- 天符灵经 (tianfu_spirit_scripture) - 未配置层数
- 掩月周天功 (yanyue_cycle_art) - 未配置层数

**优先级 P1 - 宗门/流派功法** (约 50+):
- 所有六宗功法大多未配置具体层数
- 剑修、体修、炼丹、炼器、阵法类专修功法未配置

**优先级 P2 - 特殊/种族功法** (约 40+):
- 妖修化形诀 (beast_transformation_art) - 未配置
- 冥河鬼修诀 (ghost_nether_art) - 未配置
- 各区域特色功法未配置

### 1.3 Realm Band 配置状态
- ✅ 已有 `realm_min` / `realm_max` / `realm_max_learn` 字段
- ❌ **缺失**: 大部分功法未配置 `realm_band` 分段信息
- ❌ **缺失**: 功法阶段与境界的精确映射（除长春功外）

---

## 二、功法学习门槛实现状态

### 2.1 灵根门槛 (✅ 部分实现)
**已实现**:
- `TechniqueDataManager.matchesAttributeCondition()` 检查灵根属性匹配
- `spirit_roots.required_any` 字段已在 JSON 中广泛使用
- `spirit_roots.bonus` 字段存在但加成计算不完整

**缺失**:
- ⚠️ **灵根纯度门槛**: JSON 中未见 `purity_min` 字段应用
- ⚠️ **天灵根/隐灵根专属**: 特殊灵根类型的强制要求未明确
- ❌ **多灵根优势**: 五行合一对炼虚后期的优势未实现

### 2.2 体质门槛 (⚠️ 部分实现)
**已配置**:
- 部分功法有 `forbidden_traits: ["pure_yang_body"]` 限制
- 玄阴经明确禁止纯阳体质

**缺失**:
- ❌ **特殊体质专属功法**: 龙吟之体、冰髓之体等专属功法未配置
- ❌ **体质冲突检测**: 多功法学习时的体质冲突未完整实现
- ❌ **体质致命缺陷**: 特殊体质的负面效果未事件化

### 2.3 种族门槛 (✅ 已配置)
**已实现**:
- `race_required` 字段广泛存在
- 妖修 (demon_cultivator)、鬼修 (ghost_cultivator)、慕兰法士等已配置

**缺失**:
- ⚠️ **种族转换**: 人修转鬼修、化形等转换机制未完整
- ❌ **种族冲突**: 跨种族功法学习的冲突检测不完整

### 2.4 转修与必转门槛 (⚠️ 部分实现)
**已配置**:
- 长春功: `must_convert_after: "FOUNDATION"` ✅
- 青元剑诀: `prerequisite_methods: ["changchun_gong"]` + `prerequisite_realm_layers: {"changchun_gong": 13}` ✅

**缺失**:
- ❌ **其他转修功法**: 除青元剑诀外，其他转修路线未配置
- ❌ **强制转修提示**: 境界到达后未主动提示必须转修
- ❌ **低阶功法惩罚**: 长春功筑基后继续修炼的效率惩罚未实现

### 2.5 宗门门槛 (⚠️ 部分实现)
**已配置**:
- `faction` / `faction_relation_min` 字段存在
- `outer_disciple` / `inner_disciple` / `core_disciple` 等级已定义

**缺失**:
- ❌ **同宗门限制**: `TechniqueGateService` 未见 `same_sect` 严格检查
- ❌ **叛宗惩罚**: 学习敌对宗门功法的后果未实现
- ❌ **宗门贡献兑换**: 功法的贡献点兑换门槛不完整

---

## 三、功法精进成本个性化 (❌ 未实现)

### 3.1 当前实现状态
根据 `pending_requests.md` 0.2.63:
> method cultivate costs are school/attribute/ladder aware

**实际情况**:
- ✅ 已有成本意识框架
- ❌ **个性化成本表**: 未见功法 JSON 中配置每层的具体成本
- ❌ **流派差异**: 剑修、体修、丹修等不同流派的成本差异未实现
- ❌ **属性成本**: 五行属性功法的灵石消耗差异未实现
- ❌ **阶梯成本**: 境界越高成本指数增长的公式未明确

### 3.2 需补充的成本字段
每个功法应配置:
```json
{
  "cultivation_costs": {
    "base_spiritual_power_per_layer": 1000,
    "cost_multiplier_by_realm": {
      "QI_REFINING": 1.0,
      "FOUNDATION": 2.5,
      "CORE_FORMATION": 6.0
    },
    "school_modifier": {
      "sword": 1.2,
      "body": 1.5,
      "alchemy": 0.8
    },
    "requires_special_materials": []
  }
}
```

---

## 四、生活功法熟练度系统 (✅ 已实现基础)

### 4.1 已实现 (LifeSkillService)
- ✅ `successBonus()`: 等级 + 熟练度 → 成功率加成
- ✅ `BONUS_PER_LEVEL = 0.02D` (每级 +2%)
- ✅ `PROFICIENCY_BONUS_MAX = 0.10D` (满熟练度 +10%)
- ✅ 熟练度上限 10000

### 4.2 生活功法配置状态
**已配置 layers_max: 1 的生活功法** (7 个):
- 炼丹类、炼器类、阵法类等
- ⚠️ 层数为 1，意味着**无层级递进系统**

**缺失**:
- ❌ **多层生活功法**: 高级炼丹术、高级炼器术等进阶版本未配置
- ❌ **熟练度衰减**: 长期不使用的技能熟练度下降机制未实现
- ❌ **熟练度上限提升**: 功法品阶对熟练度上限的影响未配置

---

## 五、空功法矩阵的可执行术法 (⚠️ 部分未实现)

### 5.1 14 个空矩阵问题
根据 `pending_requests.md` 0.2.107:
> 14 empty method matrices grant executable techniques

**问题分析**:
- 部分功法在 `method_layer_technique_matrix_v130.json` 中缺失
- 这些功法虽已注册，但无法通过层数解锁术法
- 影响游戏体验的完整性

### 5.2 需补充矩阵的功法 (优先级 P0)
需要查找:
1. 已注册但矩阵中不存在的功法 ID
2. 矩阵中 `layers: []` 为空的功法
3. 矩阵中 `total_layers: 0` 的功法

**行动项**:
- 补全 14 个空矩阵的层数定义
- 为每层配置可解锁的术法 ID
- 确保术法 ID 在 `TechniqueDataManager` 中已注册

---

## 六、长春功 13 层扩展完整性 (✅ 基本完整)

### 6.1 已实现
- ✅ `layers_max: 13` 配置完整
- ✅ 5 个阶段划分 (1/4/7/10/13 层)
- ✅ 每阶段解锁的术法已在 matrix 中定义
- ✅ `prerequisite_realm_layers: {"changchun_gong": 13}` 用于青元剑诀转修

### 6.2 可能的增强点
- ⚠️ **13 层境界突破**: 第 13 层圆满是否触发筑基机缘？
- ⚠️ **转修过渡**: 长春功 → 青元剑诀的平滑过渡体验
- ⚠️ **低阶功法标识**: UI 是否明确标识这是"启蒙功法"

---

## 七、优先级与实施建议

### P0 - 立即修复 (影响核心玩法)
1. **补全核心战斗功法的层数配置** (约 15 个)
   - 玄阴经、大衍诀、烈焰功等
   - 为每个功法配置 3-13 层不等
   
2. **补全 14 个空功法矩阵**
   - 生成每层可解锁术法清单
   - 确保术法 ID 有效

3. **完善灵根门槛检查**
   - 纯度、天灵根专属检查
   - 统一门槛文案

### P1 - 近期完善 (提升体验)
4. **实现功法精进个性化成本**
   - 为所有功法配置 cultivation_costs
   - 实现流派/属性/阶梯成本差异

5. **完善宗门门槛**
   - same_sect 严格检查
   - 叛宗惩罚机制

6. **体质门槛深化**
   - 特殊体质专属功法配置
   - 体质冲突检测

### P2 - 长期规划 (系统完整性)
7. **生活功法多层级系统**
   - 设计初级/中级/高级炼丹术等
   - 配置 layers_max > 1

8. **转修系统完善**
   - 更多转修路线配置
   - 强制转修提示与引导

9. **种族转换与冲突**
   - 人修转鬼修完整流程
   - 跨种族功法冲突表

---

## 八、具体功法缺口示例

### 示例 1: 玄阴经 (xuan_yin_art)
```json
{
  "id": "xuan_yin_art",
  "缺失字段": {
    "layers_max": "未配置，默认 9？",
    "realm_band_per_layer": "未定义",
    "cultivation_costs": "未个性化",
    "unlocks_techniques_detail": "仅有 school，无具体层数映射"
  }
}
```

### 示例 2: 大衍诀 (dayan_art)
```json
{
  "id": "dayan_art",
  "缺失字段": {
    "layers_max": "未配置",
    "prerequisite_methods": "空数组，但应需要一定神识基础",
    "divine_sense_min_per_layer": "未配置",
    "puppet_unlock_layers": "神识+傀儡双修未明确层数关系"
  }
}
```

---

## 九、数据统计汇总

| 类别 | 总数 | 已配置 | 未配置 | 完成度 |
|------|------|--------|--------|--------|
| 功法总数 | 136 | - | - | - |
| 层数配置 (layers_max) | 136 | 17 | 119 | 12.5% |
| 灵根门槛 (spirit_roots) | 136 | ~80 | ~56 | 58.8% |
| 种族门槛 (race_required) | 136 | ~15 | ~121 | 11.0% |
| 体质门槛 (constitution) | 136 | ~5 | ~131 | 3.7% |
| 转修门槛 (prerequisite_methods) | 136 | ~10 | ~126 | 7.4% |
| 精进成本 (cultivation_costs) | 136 | 0 | 136 | 0% |
| 功法矩阵 (matrix 中有数据) | ~50 | ~36 | ~14 | 72.0% |

---

## 十、结论与建议

### 10.1 当前实施完成度评估
- **框架层**: ✅ 80% - 代码框架已支持动态层数、门槛检查、熟练度系统
- **数据层**: ❌ 15% - JSON 数据配置严重不足，大量功法仍使用默认值
- **玩法完整性**: ⚠️ 40% - 核心功法可用，但缺乏个性化和深度

### 10.2 关键阻塞点
1. **119 个功法缺少层数配置** - 导致所有功法统一 9 层，无差异化
2. **14 个空功法矩阵** - 部分功法无法通过修炼解锁术法
3. **精进成本未个性化** - 所有功法成本相同，缺乏策略选择

### 10.3 建议实施路径
**Phase 1 (1-2 周)**:
- 补全 15 个核心战斗功法的层数配置
- 修复 14 个空功法矩阵
- 完善长春功 → 青元剑诀转修体验

**Phase 2 (2-3 周)**:
- 为所有功法配置个性化精进成本
- 完善宗门门槛与叛宗机制
- 深化体质门槛检查

**Phase 3 (长期)**:
- 设计多层级生活功法体系
- 实现完整的转修路线网络
- 种族转换与跨种族功法系统

---

**报告生成时间**: 2026-07-21  
**审查人**: Claude Code Agent  
**下次审查**: 建议在完成 Phase 1 后重新评估
