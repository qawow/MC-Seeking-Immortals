# M16 图鉴与百科

- 分支：`task/m16-lore-books` ｜ worktree：`/root/mc-mod-worktrees/m16-lore-books`
- 前置模块：全部（只读展示层，最后合并）
- 优先级：P2/P3（收口）

## 目标

把全部已落地系统的数据汇入 Patchouli 图鉴与游戏内百科展示：词条扩量、视觉规范、术语表与数值总览，作为纯只读展示层收口。

## 功能点清单（可勾选）

- [x] Patchouli 词条扩量：`patchouli_static_entries` + `patchouli_item_book` → `data/seeking_immortals/patchouli_books/seeking_immortals_guide/`（现 47 词条 ×2 语言）按各模块落地成果扩写（功法/丹药/秘境/宗门/法宝/妖兽分册）。
- [x] 视觉规范：`visual_*`（v118–v122）→ 图鉴排版、图标引用、结构展示页（消费 M07 的 MP 序列导出）。
- [x] 术语表：`name_alias_glossary_v103` → 游戏内术语对照（与 M03 别名解析一致性校验）。
- [x] 数值总览：`numeric_overview_v103` → 百科内数值速查页（境界表/灵石阶梯/十三阶表，数据只读引用 M01/M05/M10）。
- [x] 妖兽图鉴展示：M10 bestiary 解锁记录的图鉴页（解锁状态驱动可见性）。
- [x] 编年史展示:M11 chronicle_events/时间线记录的阅读界面。
- [x] `SettingCatalogSummaryService`（现读 techniques/index.json 做摘要）扩展为全类目摘要供 HUD/tooltip。
- [x] lang 全量校对：zh_cn/en_us 缺口清单（各模块新增 key 的对账，不改业务代码）。
- [x] Patchouli 可选性守卫复查：Patchouli 缺席时零崩溃（`ModCompat.PATCHOULI_LOADED`）。

## 涉及文件

**语料侧（本模块拥有）**
- `文本材料/data/patchouli_static_entries*`、`patchouli_item_book*`
- `文本材料/data/visual_style_v118*`、`visual_storyboards_v119*`、`visual_effect_sheets_v120*`、`visual_fx_pipeline_v121*`、`visual_look_cards_v122*`、`docs/VISUAL_BIBLE_v118/v120/v122*`（v122 为视觉圣经权威）、`docs/ASSET_TEXTURE_STYLE.md` + `asset_texture_batches*`/`asset_texture_job_map*`/`asset_texture_pill_queue*`（贴图任务队列:330 注册项分批）
- `文本材料/data/name_alias_glossary_v103*`、`numeric_overview_v103*`、`item_descriptions_v119`–`v147*`（v119 起的增量描述波次;v95–v118 归 M03）、`key_item_descriptions_v141*`、`catalog_description_align_v112/v116*`
- `文本材料/data/chaotic_sea_lore_compendium*`、`novel_canon_audit_v124*`、`novel_canon_checklist_v140/v143/v145*`、`setting_crosslink_matrix_v137*`、`merge_bridge_v109*`、`timeline_guide_v115*`（展示层引用;任务数据归 M11）

**模组侧（本模块拥有）**
- `data/seeking_immortals/patchouli_books/seeking_immortals_guide/`（zh_cn + en_us 全部词条）
- `catalog/SettingCatalogSummaryService`
- `assets/seeking_immortals/lang/`（校对层面；新增业务 key 归各模块）
- 图鉴/编年史阅读屏幕（新建，遵循已统一的 ImmortalUiSkin 风格）

## 依赖前置库

- Patchouli API（**可选依赖**——一切 API 调用必须 `ModCompat.PATCHOULI_LOADED` 守卫或隔离类）。

## 接口交叉（谁提供 → 谁消费）

- **M16 ← 全部模块**：只读消费所有模块的最终数据与解锁状态。
- **M16 → 无**：不提供任何运行时接口；其他模块**不得**依赖 M16。

## 验收与红线

- Patchouli 词条内引用的物品/方块 id 全部可解析；双语言词条数一致。
- 纯展示原则：本模块**零**游戏性状态写入（除图鉴解锁记录的读取）。
- 不重构任何业务屏幕（0.1.501–0.1.504 已统一 UI，只新增阅读页）。

## 工作流提醒

按 README 通用工作流。词条/资源改动随 jar 发布 → 升 mod_version；无网络包变更预期，协议保持 19。

## 落地摘要（2026-07-17）

- Patchouli：47→87 词条 ×2 语言，8 分类；语料 static/item_book 缺口词条补齐。
- 运行时服务：`NameAliasGlossaryService` / `NumericOverviewService` / `VisualStyleService` / `LoreCompendiumService` / `LoreSyncService`。
- 阅读屏：`BestiaryScreen` / `ChronicleScreen` / `LoreCompendiumScreen` + ImmortalUiSkin。
- 同步：追加 `SyncLoreUnlockPacket` + `LoreScreenActionPacket`（字段追加，PROTOCOL 保持 22）。
- `SettingCatalogSummaryService` 扩全类目；lang 4510/4510 对齐；Patchouli `getBookStackSafe`。
- 任务红线：不升 `mod_version`（0.1.509）。
