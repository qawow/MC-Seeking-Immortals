# 2026-07-04 0.1.104 Worldpack Text-Material Data Expansion

## Scope

- Expanded shipped worldpack data resources from the text-material setting pack.
- Current `mod_version` is `0.1.104`; this preserves the already advanced texture-redraw worktree version instead of downgrading it to the earlier planned `0.1.103` label.
- `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields, field order, encode/decode format, packet registrations, or channel compatibility changed.
- Backup path: `.bak/20260704_0.1.103_worldpack_text_materials/`.

## Data Added

- `worldpack/regions.json`: 11 regions covering Qinglan, Tiannan, Yue mist lands, Chaotic Star Sea, outer sea, Mulan grassland, Great Jin, Nether River, Fallen Demon Valley, Tianyuan, and Spirit Realm Fengyuan.
- `worldpack/secret_realms.json`: 11 secret realms with realm gates, existing ticket items, cooldowns, and return policies.
- `worldpack/daily_events.json`: 19 regional events using existing effect tokens such as `aura_plus_5`, `herb_shop_bonus`, `trade_risk_up`, `secret_realm_ticket_hint`, and `rare_loot_hint`.

## Text-Material Source Cleanup

- Wrapped `text-materials/data/asset_texture_job_map.json` as an object root with a `jobs` array so the repository JSON sanity gate can parse it consistently.
- Added missing alchemy schema fields to the last three text-material recipes in `text-materials/data/alchemy_recipes.json`: `medium`, `furnace_min_grade`, and `ideal_fire_tier`.

## Item And Resource Impact

- No new obtainable items, blocks, creative-tab entries, models, textures, recipes, loot tables, tags, or GUI assets were added.
- Secret-realm tickets reuse existing registered/model-backed items such as element spirit stones and `immortal_jade`.
- New region travel anchors must be placed by OP command before travel succeeds; missing anchors fail safely through the existing worldpack service.

## Spell Implementation Rule For Next Slice

- Per user instruction, future spell effect work should use custom mod-owned logic/entities/effects as the core behavior.
- Do not implement effects by simply wrapping vanilla projectiles or effects, for example using a vanilla fireball entity as Fireball Spell's core implementation.

## Verification

- First sandboxed Gradle run failed on wrapper network access with `Permission denied: getsockopt`; escalated reruns were used for tests.
- Focused tests passed:
  `./gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.resources.JsonSanityTest --tests com.xunxian.seekingimmortals.worldpack.WorldpackDataServiceTest --tests com.xunxian.seekingimmortals.network.MarketWorldpackPacketTest`
  - Result: `BUILD SUCCESSFUL in 19s`.
- Final build passed:
  `./gradlew.bat --no-daemon --max-workers=1 build`
  - Result: `BUILD SUCCESSFUL in 27s`; aiPreflight recorded `mod_version=0.1.104`.
- The network-package preflight warning was reviewed; this data/docs pass did not change packet compatibility, so protocol remains `9`.

## Remaining Manual Regression

- Worldpack GUI and command display for expanded region/realm/event lists.
- `/seeking_immortals worldpack set_anchor` for the new travel anchors.
- Travel, secret-realm enter/return, ticket consumption, cooldowns, and return locations.
- Daily-event aura/market modifiers and multiplayer packet behavior.