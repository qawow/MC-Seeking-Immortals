# 2026-07-03 0.1.77 Core Attributes And Heavenly Tribulation

## Summary

- Final shippable version for the core-attributes and heavenly-tribulation work is `0.1.77`.
- The implementation was originally targeted at `0.1.76`, but `.gradle/ai-preflight/last-success.json` had already recorded a successful `0.1.76` build before this gameplay change, so the required version gate forced a patch-version advance.
- `ModNetwork.PROTOCOL_VERSION` remains `6`; no packet fields, order, encoding, registration, or channel compatibility changed.

## Verification

- `./gradlew --no-daemon --max-workers=1 test` passed before final version-gate correction: `BUILD SUCCESSFUL in 20s`.
- Final `build` is pending and should be recorded after the version-gate correction build.

## Backup

- Existing edited files were backed up to `.bak/20260703_025355_0.1.76_core_attrs_tribulation/`.

## Follow-Up

- Manual in-game checks are still needed for Core Formation and Nascent Soul breakthrough tribulations, relog persistence, death/respawn failure, and dimension-escape failure.
