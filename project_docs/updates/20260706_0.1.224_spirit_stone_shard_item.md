# 2026-07-06 - 0.1.224 Spirit Stone Shard item prerequisite

- Change class: code/resource/docs item slice.
- Source material: `文本材料/data/currency_items.json#spirit_stone_shard`, plus existing references from alchemy/material/refinement/puppet/formation/talisman/quest/flight/loot source data.
- Implemented: registered `seeking_immortals:spirit_stone_shard` as a simple stackable item, added it to the 寻仙问道 creative tab, added zh_cn/en_us names, and added an item model that reuses the existing metal spirit-stone texture.
- Reference map: `spirit_stone_shard` is now `implemented` with canonical id `seeking_immortals:spirit_stone_shard`.
- Deferred: 10:1 exchange handling, merchant stock, recipes, loot tables, fragment-specific economy UI, and material-category behavior.
- Backup: `.bak/20260706_0.1.223_spirit_stone_shard_item/` and `.bak/20260706_0.1.224_spirit_stone_shard_item_reconcile/`.
- Version/protocol: `mod_version=0.1.224`; `ModNetwork.PROTOCOL_VERSION=9` unchanged.
- Verification: UTF-8 JSON checks passed for the changed lang/reference/model files; focused `SettingCatalogSummaryServiceTest` passed after syncing the current text-material technique count to 346; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 22s and `aiPreflight` recorded `mod_version=0.1.224`.
