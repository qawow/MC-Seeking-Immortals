# 2026-07-07 0.1.308 FTB main quest defaults

- Scope: first FTB Quests implementation slice for the text-material quest-line goal, preserving the parallel `0.1.307` tree and avoiding packet/capability/runtime quest-progress changes.
- Source read: all 315 files under `文本材料` were read with UTF-8; all 207 JSON files parsed successfully. Direct implementation basis was `data/quest_chains.json` plus `data/quest_hooks.json` for `qixuan_mortal_path`, `huangfeng_cultivation_path`, `blood_forbidden_campaign`, `spirit_realm_rise`, and `chain_tianyuan_enlist`.
- Implementation: added bundled FTB quest defaults under `src/main/resources/seeking_immortals/ftbquests/quests/`. The first chapter contains 21 safe checkmark nodes covering Seven Mysteries/Qixuan, Huangfeng cultivation, Blood Forbidden, and Tianyuan/Spirit Realm progression.
- Runtime seeding: `FtbQuestDefaults` copies bundled `data.snbt` and `chapters/seeking_immortals_main.snbt` into `config/ftbquests/quests/` only when those files are missing, so existing FTB quest packs are not overwritten.
- Version/protocol: implemented as the `0.1.308` quest-presentation slice and current-tree verified after shared version movement to `mod_version=0.1.309`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or SimpleChannel behavior changed.
- Verification: focused `FtbQuestSnbtTest` passed with BUILD SUCCESSFUL in 14s. Final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 19s and `aiPreflight` recorded `mod_version=0.1.309`; inherited network-package warning reviewed as unrelated to this quest-presentation slice.
- Remaining: replace checkmark placeholders with server-authoritative FTB item/advancement/custom tasks, bridge Seeking Immortals quest/cultivation state into FTB progress where safe, add branch/exclusion handling, add rewards only through validated server-side paths, and expand the remaining quest chains from `文本材料`.
