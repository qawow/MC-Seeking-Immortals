# 2026-07-07 - 0.1.308 11x11 realm gate array

- Planning: enlarged the existing realm-gate multiblock through the current `PortalArrayStructure` constants and reused `SpiritGatheringArrayBlock`, `WorldpackGameplayService`, existing blocks, localization, Patchouli, and focused tests. No packet, registry, dimension resource, route-cost, recipe, loot, GUI, shop-loader, or runtime-loader work was added.
- Source basis: user requested a larger portal multiblock, with support from `文本材料/docs/DIMENSIONS_DESIGN.md`, `data/dimensions_catalog.json`, `data/spatial_nodes_catalog.json`, `data/trade_routes.json`, and formation catalog references.
- Implementation: realm gates now require an 11x11 Spirit Gathering Array base, four eight-high Spirit Ore corner pillars, and a clear 9x9, seven-block-high aperture. Default built-in dimension anchors reuse the same constants, so generated Tianyuan/Fengyuan/Yinming/Nether River platforms match runtime validation.
- Resources: zh_cn/en_us incomplete-portal feedback, worldpack route hints, and the formation Patchouli overview now describe the 11x11 gate.
- Backup: `.bak/20260707_0.1.307_portal_array_11x11/`.
- Current-tree doc backup: `.bak/20260707_0.1.309_portal_array_build_docs/`.
- Verification: changed lang/Patchouli JSON parsed successfully with explicit UTF-8. Focused `PortalArrayStructureTest` + `WorldpackPortalArrayRulesTest` passed with BUILD SUCCESSFUL in 24s. Final current-tree `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 31s and `aiPreflight` recorded `mod_version=0.1.309`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or SimpleChannel behavior changed.
