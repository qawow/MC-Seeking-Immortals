# 2026-07-06 0.1.262 Paper Formula Scroll item carrier

- Change class: code/resources/docs; item registration and shipped resources changed, so this slice is now coordinated with current shared `mod_version=0.1.262`.
- Coordination: detected and preserved concurrent `0.1.259` Spirit Realm large portal-array work and the in-progress `0.1.261` Pressure Resist Pill formula slice before adding this independent item carrier.
- Text-material basis: read all 315 files under `文本材料` and parsed all 207 JSON files successfully; direct source ids are `consumables_catalog.json#paper_formula_scroll`, `merchant_shops.json` paper-scroll stock references, and `item_ids_export.csv`.
- Implementation: added `seeking_immortals:paper_formula_scroll` as a visible craft-material carrier, creative-tab entry, zh_cn/en_us localization, dedicated item model reusing the existing paper-formula texture, and `text_material_id_map.json` coverage.
- Scope held back: no inscription recipe, shop stock, quest reward, packet, GUI, runtime loader, loot table, or gameplay system was added.
- Version/protocol: `mod_version=0.1.262`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged because no packet fields, field order, encoding, registrations, or channel behavior changed.
- Backup paths: `.bak/20260706_0.1.260_paper_formula_scroll/`, `.bak/20260706_0.1.261_paper_formula_scroll_reconcile/`, `.bak/20260706_0.1.262_paper_formula_scroll_reconcile/`, and `.bak/20260706_0.1.262_paper_formula_scroll_post_build_docs/`.
- Validation: changed JSON validation passed. Initial `./gradlew --no-daemon --max-workers=1 build` at `0.1.260` failed in `aiPreflight` because the shared tree had shippable tracked changes against an occupied/successful 0.1.260 fingerprint; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 37s, `aiPreflight` recorded `mod_version=0.1.262`, and build output includes `build/libs/seeking_immortals-0.1.262.jar`.
