# 2026-07-06 0.1.271 Utility spell effects

- Change class: code/resources/docs. This slice adds executable server-side behavior for four existing shipped technique ids and keeps the work on the current shared `mod_version=0.1.271` tree.
- Coordination: preserved the build-verified `0.1.269` Yin Stone and P0 Artifact Carrier work, the build-verified `0.1.268` Patchouli Guide Index, and the parallel `0.1.271` Patchouli Core Topics pending work without reverting unrelated files.
- Source basis: project-wide text-material scan remains complete for all 315 files and 207 parsed JSON files; direct implementation basis is `voice_transmission_art`, `object_control_art`, and `quicksand_art` in `qi_refining_techniques.json`, `wind_wall_art` in `foundation_establishment_techniques.json`, and the pending spell follow-up list.
- Implementation: `SkillType` and `SkillEffectRegistry` now map `voice_transmission_art`, `object_control_art`, `quicksand_art`, and `wind_wall_art` to executable `SkillEffect` classes.
- Reuse: `VoiceTransmissionSpell` uses the existing player/server message path, `ObjectControlSpell` uses vanilla entity ray tracing and item/living entity APIs, `AreaDebuffSpell` provides a reusable area control spell for quicksand-style effects, and `WindWallSpell` uses vanilla projectile entities plus a short resistance buff.
- Version/protocol: `mod_version=0.1.271`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged because no packet fields/order/encoding/registrations or channel behavior changed.
- Backup paths: `.bak/20260706_0.1.269_utility_spell_effects/`, `.bak/20260706_0.1.269_utility_spell_effects/post_parallel_0.1.269/`, `.bak/20260706_0.1.269_utility_spell_effects/post_parallel_0.1.270_docs/`, and `.bak/20260706_0.1.269_utility_spell_effects/post_build_0.1.271_docs/`.
- Validation: text-material scan covered all 315 files with 207 JSON files parsed successfully; zh_cn/en_us language JSON parsed successfully; forced `compileJava --rerun-tasks` passed with existing JEI deprecation warnings; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 22s and `aiPreflight` recorded `mod_version=0.1.271`.
- Follow-up: water arrow, metal needle, richer custom projectiles/particles, source-accurate acquisition, in-game feel checks, and balance tuning remain deferred.
