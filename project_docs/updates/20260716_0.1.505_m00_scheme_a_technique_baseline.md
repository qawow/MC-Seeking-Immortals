# 0.1.505 M00 方案 A：功法基线与 body.json.json 解阻

- 日期：2026-07-16
- mod_version：`0.1.504` → `0.1.505`
- 协议：保持 `19`（无网络包变更）
- 备份：`.bak/20260716_133639_m00_scheme_a/`

## 变更

1. `文本材料/data/techniques/index.json` 的 `files[]` 从完整文件名（`body.json`）改回 stem（`body`），与 `by_school` key 及 Java 拼接契约一致。
2. `SettingCatalogSummaryServiceTest`：`declaredTechniqueCount` 期望由 346 更新为 **747**；文件数仍为 20。
3. 文档：M00 简报两项勾选 + 裁决记录；`ai_handoff.md` / `step_progress.md` 标记 747/346 与 `body.json.json` 阻塞已解除。

## 裁决

- **当前权威条数 = 747（作者侧语料）**。
- **346 为历史已发布快照**（`src/main/resources/data/seeking_immortals/text_material/techniques/`）。
- **是否升级发布副本至 747 留待 M02 决策，本轮不处理。**

## 明确未改

- 发布副本 `src/main/resources/.../text_material/techniques/` 保持 346。
- `JsonSanityTest` 拼接逻辑未改。
- `TechniqueGateService` 等硬编码文件名读取未改。

## Linux 构建配套（环境必要）

- 新增 `scripts/preflight.py`，`build.gradle` 在非 Windows 上用 `python3` 跑 preflight（本环境无 `pwsh`）。
- `processResources` 排除 `*:Zone.Identifier`，避免 Windows ADS 侧车文件在 Linux 上导致资源打包失败。
- `.gitignore` 补充 `*:Zone.Identifier`（历史已跟踪文件不在本轮批量删除）。

## 验证

- `./gradlew --no-daemon build`：**BUILD SUCCESSFUL**（1m31s）
- `SettingCatalogSummaryServiceTest` / `JsonSanityTest` technique index 相关用例：随全量 test 通过
