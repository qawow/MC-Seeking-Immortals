# 2026-07-17 0.2.0 十七模块统一里程碑

## 变更分类

正式发布整合：版本、网络协议、构建门禁、运行时文本材料索引、回归测试与项目文档。

## 合并结论

- `task/m00-data-pipeline` 至 `task/m16-lore-books` 共 17 个分支 tip 均已验证为 main 祖先。
- 缺失的 M04、M05、M07、M14 已补合并并分别完成集成构建；M16 已合入 main。
- `mod_version` 从集成基线 `0.1.509` 统一发布为 `0.2.0`。
- `ModNetwork.PROTOCOL_VERSION` 从 22 升至 23。M16 新增 lore 同步/动作消息，且整合期存在不兼容 channel 行为，旧 22 客户端不得通过握手。

## 发布完整性

- sh、Python、PowerShell 三套 preflight 从硬编码 `0.1.X` 改为数字三段式 `X.Y.Z`。
- 发布侧 text-material 运行时清单由 209/289 修正为 289/289：268 个 catalog JSON、21 个 technique JSON。
- 新增 `scripts/rebuild_text_material_manifest.py`，保留既有人工 primary-key 元数据并补齐新文件。
- `TextMaterialManifestServiceTest` 现在精确断言全部发布 JSON 均被索引，禁止仅检查下限造成清单漂移。
- `data_manifest.json` 的顶层 JSON、region card 与最后扩展记录已同步。

## 验证

- 17/17 合并态预发布验证：`./gradlew build --no-daemon`，`mod_version=0.1.509`，BUILD SUCCESSFUL in 1m。
- 正式工作树验证：`./gradlew build --no-daemon`，555/555 tests，BUILD SUCCESSFUL in 56s。
- 独立发布候选验证：基于 `HEAD + staged release patch` 的干净本地克隆执行 `./gradlew build --no-daemon`，526/526 tests，BUILD SUCCESSFUL in 1m21s。
- 语言审计：zh/en 4575/4575，单边缺失 0。
- manifest：actual 289、indexed 289、missing 0、extra 0。

## 产物

- 文件：`build/libs/seeking_immortals-0.2.0.jar`
- 大小：30,865,197 bytes
- SHA-256：`edaaea11120da3e9443ae6e1fcb78feef80f90fd163767fe2df1016e4a69e4b7`

## 备份

- 总备份根：`.bak/20260717_053227_merge_m16_release_0.2.0/`
- 正式发布前：`.bak/20260717_053227_merge_m16_release_0.2.0/pre_release/`
- 并发构建断点复核：`.bak/20260717_053227_merge_m16_release_0.2.0/pre_concurrent_build_fix/`

## 已知风险

- 尚未执行真实客户端、专服和多人烟测。
- M04 仍有部分炼丹配方缺少可靠定向公式入口，部分效果使用通用回退。
- M05 动态价格、跨币种换算和拍卖经济仍需实机回归。
- M07/M13 飞舟船坞结构定义尚未统一；M13 飞升备份恢复仍需防复制审计。
- M14 的 `outgoingDamageMul`、`blocksTechnique`、`hidesRealm` 及 M07/M15 伤害钩子仍未全部接入消费路径。
- M15 器灵觉醒、祭炼成长链和装备扫描仍需深化；高保真美术与专用妖兽实体仍是后续工作。
