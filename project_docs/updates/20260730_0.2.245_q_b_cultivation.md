# `0.2.245` Q-B-1 修炼域结构化证明

变更类别：代码、资源、测试与项目文档。

本批完成 Q-B-1：引入不可伪造的 `DetailedQuestProofEvent` 和 `DetailedQuestProofService`，以当前任务步骤和服务端实时能力状态验证自然事件，再复用既有幂等奖励账本推进。新增有界证明账本/历史回放及死亡克隆复制；管理员 `prove/claim` 保留为 permission 2 救档路径并写入 `Admin=true`。

生产者已接入功法学习、功法升层、自动授予功法、术法学习/解锁、无雷劫突破和雷劫最终成功。修正功法与境界路由的真实解析 id，增加 `minimum_layer`/`minimum_realm` 校验；梵圣真魔功调整为元婴门槛，并实现元婴后期/圆满、明王诀、托天魔功和梵圣真片的原子拼合流程。

编辑文件：`gradle.properties`、修炼/目录/任务 Java 源码、任务路由与功法资源、中英文语言、定向测试、生成档案，以及 `project_docs/` 文档。备份路径：`.bak/20260730_004318_q_b_cultivation/`。用户既有 `CLAUDE.md` 和 `project_docs/frontend_interaction_audit_0.2.198.md` 未纳入提交。

验证结果：

- `python3 scripts/generate_authored_spell_effects.py --check`：2,292 profiles current。
- `python3 scripts/generate_authored_visual_catalog.py --check`：5,727 profiles current。
- Q-B 修炼域定向测试通过。
- `./gradlew build --no-daemon --max-workers=1 --console=plain`：`BUILD SUCCESSFUL`，257 个测试套件、1,217 项测试，failure/error/skipped 均为 0。
- JAR SHA-256：`19bcba6aad691e508b3587647dbc8f7b777aa7db1340be80d617f448e37c8abd`。

版本与协议：`mod_version=0.2.245`；未改变网络包字段、顺序、类型、注册或频道行为，`ModNetwork.PROTOCOL_VERSION=31` 保持不变。

下一步：Q-B-2 地域、维度、秘境和结构事件。当前非修炼证明的默认权威校验仍需在各域迁移时逐项收紧；真实客户端、专服和双客户端验收仍未完成。
