# 2026-07-17 M10 / M12 主线集成

## 摘要

按顺序将 `task/m10-beasts-ecology` 与 `task/m12-npc-dialogue` 合入 `main`，保留妖兽生态和具名 NPC / 对话树两套实现，并统一主线版本门禁。

## 合并复核

- M10：32 个文件，新增 39,246 行、删除 730 行；`git diff --check` 通过。
- M12：33 个文件，新增 9,213 行、删除 467 行；`git diff --check` 通过。
- 重叠文件仅有 `ai_handoff.md`、`step_progress.md`、`ModEvents.java`。
- `ai_handoff.md` 首行冲突保留两条 CURRENT TRUTH；其余代码自动合并，`ModEvents` 的妖兽击杀与 NPC 交互位于不同事件路径。

## 版本与协议

- `mod_version`：`0.1.506` → `0.1.507`，用于主线代码/资源集成。
- `ModNetwork.PROTOCOL_VERSION`：保持 `21`；`DialogueActionPacket` 仅扩展服务端路由，未修改字段、顺序或类型。

## 验证

- 最终 `./gradlew build`：BUILD SUCCESSFUL in 1m13s；11 个任务中 9 个执行、2 个 up-to-date。
- `aiPreflight`：通过并记录 `mod_version=0.1.507`。
- 合并后的 `git diff --check` 与暂存区 `git diff --cached --check`：通过。
- Java 编译仅保留 3 条既有 JEI deprecated API 警告。

## 已知风险

- M10 生态刷怪路径优先调用现有召唤代理，可能把生态实体登记为玩家随从，使野生标签、附近数量上限和掉落链无法按预期生效。
- `BeastSpawnTableService.findTable` 在零匹配时仍可能选中首表，使区域回退路径难以到达。
- 两个大型图鉴 JSON 合计约 3.5 万行，仍需关注首次加载与内存开销。
