# 2026-07-04 0.1.91 left-side technique skill bar

## Summary

- Moved the existing 7-slot technique skill bar from the right screen edge to the left screen edge.
- Kept the current jade-slip/talisman native-drawn style, slot behavior, cooldown overlay, low-mana mark, numbering, and icon lookup.
- Changed technique tooltip placement to open to the right of the left-side skill bar, with screen-edge clamp.
- Updated the top-right cultivation HUD layout so narrow screens reserve space for the left-side skill bar instead of a right-side reserve.
- The skill bar keeps its default vertical centering and moves below the left-top cultivation health panel when screen height allows.

## Version And Protocol

- `mod_version` moved from the actual workspace `0.1.90` to `0.1.91`.
- `ModNetwork.PROTOCOL_VERSION` remains `8`.
- No packet fields, order, encoding, registrations, or channel compatibility changed.

## Files

- Modified `gradle.properties`.
- Modified `TechniqueSkillBarOverlay`, `CultivationHudOverlay`, and `ScreenLayoutTest`.
- Updated project docs: `ai_handoff.md`, `step_progress.md`, `features.md`, `pending_requests.md`, `items.md`, and `missing_and_placeholders.md`.

## Verification

- Focused layout test passed: `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.client.ScreenLayoutTest` ended `BUILD SUCCESSFUL in 23s`.
- Final required build passed: `.\gradlew.bat --no-daemon --max-workers=1 build` ended `BUILD SUCCESSFUL in 27s`; `aiPreflight` recorded `mod_version=0.1.91`.
- The network-package warning is inherited from existing dirty network files; this pass did not change packet fields, order, encoding, registrations, or channel compatibility, so `ModNetwork.PROTOCOL_VERSION` remains `8`.

## Backup

- Existing edited files were backed up under `.bak/20260704_0.1.91_left_skill_bar/`.

## Manual Regression

- Check GUI Scale 1/2/3/Auto for the left skill bar, left-top health panel, right-top cultivation HUD, tooltip placement, cooldown overlay, low-mana corner mark, and F1 hiding.
