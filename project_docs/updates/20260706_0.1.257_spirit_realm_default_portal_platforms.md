# 2026-07-06 0.1.257 Spirit Realm default portal platforms

- Change class: gameplay code, focused test reconciliation, docs, and version metadata.
- Source basis: `文本材料/data/dimensions_catalog.json`, `文本材料/data/dimension_registry.json`, `文本材料/data/region_cards/tianyuan.json`, `文本材料/data/region_cards/spirit_fengyuan.json`, and `文本材料/docs/SPIRIT_REALM.md`.
- Implementation: built-in Spirit Realm dimension anchors now generate and maintain a 3x3 `seeking_immortals:spirit_gathering_array` base and clear the two-block center aperture before teleporting to those default anchors.
- Compatibility: custom or moved saved anchors are not rewritten; the guard matches built-in anchor id, dimension id, and default X/Z.
- Parallel coordination: preserved the build-verified `0.1.254` Alliance Merit Token state and the observed parallel Spirit Realm Condense Pill formula acquisition; reconciled `ShopServiceTest` to the current Danxia formula stock so the aggregate tree builds.
- Backups: `.bak/20260706_0.1.255_dimension_anchor_portals/` and `.bak/20260706_0.1.257_dimension_anchor_portals_reconcile/`.
- Version/protocol: `mod_version=0.1.257`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields, field order, encode/decode format, registrations, or channel behavior changed.
- Verification: focused `WorldpackPortalArrayRulesTest` plus `ShopServiceTest` passed with BUILD SUCCESSFUL in 15s. Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 21s and `aiPreflight` recorded `mod_version=0.1.257`.
- Remaining work: stronger frame variants if desired, permit/cost UI, route quest presentation, dedicated portal/dimension art, in-game client/runtime smoke checks, and later Yin/Demon/Asura expansion.
