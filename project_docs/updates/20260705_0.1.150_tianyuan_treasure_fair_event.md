# 2026-07-05 0.1.150 Tianyuan treasure-fair event backfill

- Change class: worldpack data, test, docs, and version metadata.
- Coordination: preserves the verified 0.1.149 Yanyue Heqi formula contribution-shop slice and uses a non-overlapping worldpack daily-event data file.
- Text-material source: `daily_random_events.json#treasure_fair_rumor`; the source covers `spirit_fengyuan` and `tianyuan`, while the shipped data previously covered only Fengyuan.
- Implemented entry: `tianyuan_treasure_fair_rumor` in `worldpack/daily_events.json`.
- Runtime equivalent: existing worldpack daily-event schema with region `tianyuan`, weight 4, 24000 tick duration, and existing effect tokens `secret_realm_ticket_hint` plus `rare_loot_hint`.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains 9 because no packet fields, order, encoding, registrations, or channel compatibility changed.
- Backup path: `.bak/20260705_0.1.150_tianyuan_treasure_fair_event/`.
- Verification: changed JSON parsed successfully; focused worldpack/resource validation passed with BUILD SUCCESSFUL in 23s after wrapper network/cache approval; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 24s and `aiPreflight` recorded `mod_version=0.1.150`.
