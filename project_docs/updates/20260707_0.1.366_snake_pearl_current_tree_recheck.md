# 2026-07-07 - 0.1.366 Snake Pearl current-tree recheck

## Summary
- Preserved the `0.1.365` Snake Pearl recipe after parallel version movement advanced the shared tree.
- Confirmed the recipe still uses the existing `seeking_immortals:beast_core`, `seeking_immortals:true_dragon_blood`, and `seeking_immortals:snake_pearl` carriers.
- Rechecked the current shared tree without reverting concurrent Tiannan FTB, Demon Rift portal, Qingning Mirror, or earlier shared edits.

## Verification
- Final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL.
- `aiPreflight` recorded `mod_version=0.1.366`.

## Compatibility
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed by this recheck.
- The inherited network-package warning remains a release-audit item if pre-existing packet diffs changed compatibility.
