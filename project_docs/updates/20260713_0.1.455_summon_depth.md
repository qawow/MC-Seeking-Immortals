# 0.1.455 Summon / puppet / beast depth (Wave2 priority-2)

## Class
Code (summon services + entity AI + spell) + lang + docs

## Priority board (1→5)
1. 任务权威闭环 — done in 0.1.454
2. **召唤/傀儡/灵兽实体化** ← this wave
3. 法宝/炼器深度
4. 秘境实例化内容
5. 美术与 live smoke 抛光

## Inventory note
Real `SummonedServitorEntity` already existed with archetype stats. Gap was that `HonestSummonSpell` still behaved buff-first and concurrent ownership was uncapped.

## Changes
- `HonestSummonSpell`: entity-first spawn; buff only as fail-safe
- `SummonHonestMvpService`:
  - max 3 owned servitors (despawn oldest)
  - explicit archetype spawn overload
  - owner inventory helpers
  - lighter owner ward on proxy path
- `SummonedServitorEntity`: retaliate against owner's attacker/victim; expose life ticks
- `BeastContractService` / `PuppetCraftService`: force BEAST / PUPPET archetypes
- lang: cap status / despawn messages

## Protocol
13 unchanged

## Build
`./gradlew --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.455`

## Backup
`.bak/20260713_010041_wave455_summon_depth/`
