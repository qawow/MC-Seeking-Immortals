# 2026-07-07 0.1.378 FTB Default Copy Fix

- Crash source: launcher report showed `UnsupportedOperationException: COPY_ATTRIBUTES not supported` during `FtbQuestDefaults.copyIfMissing`.
- Implementation: removed unsupported `COPY_ATTRIBUTES` metadata copying from the bundled FTB default `InputStream` copy and kept the existing copy-only-when-missing behavior.
- Test coverage: added `FtbQuestDefaultsTest` for missing `data.snbt` seeding and existing-file preservation.
- Verification: focused `FtbQuestDefaultsTest` passed after a network-enabled rerun; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL and `aiPreflight` recorded `mod_version=0.1.378`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields/order/encoding/registrations or channel behavior changed.
- Backup: `.bak/20260707_134212_ftb_default_copy_fix/`.
