# 2026-07-03 0.1.83 Review Fixes

- Change class: code + resources/lang + tests + docs.
- `mod_version` moved from `0.1.82` to `0.1.83`.
- `ModNetwork.PROTOCOL_VERSION` remains `8`; this pass does not change packet fields, order, encoding, registration, or channel compatibility.
- Qinglan old saves now migrate `SectId=qinglan_sect` with missing/zero `SectQuestStage` to the outer-disciple stage, and sect GUI/steward sync normalizes old illegal stages before display.
- Big Dipper Sword Array cooldown is now consistently `160` ticks in `SkillType` and the shipped Foundation Establishment technique JSON.
- `SectScreen` and `TechniqueEditScreen` panel sizing now clamps inside small scaled screens instead of forcing 300x220 minimum panels.
- Technique skill-bar and editor user-facing text now comes from zh_cn/en_us language keys instead of hardcoded Chinese strings.
- Tests added/updated for Qinglan old-save migration, sect packet decode boundaries, Big Dipper cost/cooldown data, and small-screen layout helpers.
- Backup path: `.bak/20260703_212637_0.1.83_review_fixes/`.
- Test verification: `./gradlew --no-daemon --max-workers=1 test` passed with `BUILD SUCCESSFUL in 16s`.
- Final build verification: `./gradlew --no-daemon --max-workers=1 build` passed with `BUILD SUCCESSFUL in 36s`.
- The preflight network-package warning was reviewed. This pass added tests around sect packet decode limits but did not change packet fields, order, encoding, registration, or channel compatibility, so protocol `8` remains correct.
