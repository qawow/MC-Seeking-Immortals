# 0.2.88 P1 区域 NPC 对话树扩展

## 本批完成

- `npc_dialogue_branches_v139.json` 新增有序 `regional_profiles`：天南、北境、乱星海、大晋、灵界、阴冥 6 个区域组，各有商人/执事画像，共 12 个。
- 新增 12 棵区域分支树，提供宗门成员/外客分流、本宗贡献堂、公开委托、任务板、本地情报与告别路径；每个节点图均闭合、可达且不超过 8 个选择。
- `DialogueBranchService` 从资源读取 npc/region/role/archetype selectors 并按数据顺序选树；没有把区域映射写进 Java switch。
- `NamedNpcRegistry` 为全部 179 个具名 NPC 解析区域画像，同时保留黄枫贡献吏、星宫注册吏、天渊登记官等专用剧情树。
- 宗门 NPC 商店改从 `SectDefinitionService` 获取作者 shop id，掩月宗、天魔宗、星宫等不再误开通用黄枫谷贡献堂。
- 修复旧树路由：传送阵完整/损坏/禁用与缺灵石状态均可到达；天渊 500 贡献不足节点可到达；秘境窗口关闭直接读取真实 `window_open:false`；贡献吏散修任务缺失节点与配额拒绝不可达节点补齐。

## 验证

- `DialogueBranchServiceTest` 13 项、`NamedNpcRegistryTest` 9 项通过，覆盖画像选择、资源驱动契约、全部 24 棵树的根/next/可达性、旧路由修复、179 NPC 全树覆盖和全部 region/tree/shop 引用。
- NPC 全域、对话包/布局、商店、宗门定义与宗门专精回归通过。
- `./gradlew build --no-daemon`：BUILD SUCCESSFUL；AI preflight 通过；全量 764 项，0 failures/errors/skipped。
- 产物：`build/libs/seeking_immortals-0.2.88.jar`。
- `runClient`：完成模组扫描、资源加载、注册表冻结、Sound Engine 与纹理图集创建后主动 Ctrl-C；仅见环境缺失 `flite` 的 narrator 初始化噪声，没有 NPC/对话资源加载错误。

## 版本、兼容与回滚

- `mod_version`: `0.2.86 -> 0.2.88`。区域画像首次在 0.2.87 完整构建；随后全树 next/可达性审计发现旧贡献吏缺失引用，补修后按 preflight 要求升至最终 0.2.88，未使用跳过参数。
- 未修改网络包字段、顺序、类型、方向、注册或通道行为；`ModNetwork.PROTOCOL_VERSION` 保持 `26`。
- 回滚：`.bak/20260720_0.2.87_regional_dialogue/`。

## 后续

- master_plan P1 下一项：灵兽/傀儡成长，围绕 `BeastContractService` feed 入口接入喂养经验、成长等级与进化阈值。
- 实机遗留：逐区域生成商人/执事并复签成员/外客分支、商店菜单最终停留、任务板 hook、8 格锚点、NPC 走远/卸载、重连和多人并发。
