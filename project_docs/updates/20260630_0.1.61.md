# 2026-06-30 · 0.1.61 未提交改动代码审查收尾

## 背景

git 基线停在 `0.1.51`，工作区累积 `0.1.52`→`0.1.60` 共 40 文件 / +1202 / −753 未提交改动。文档声称各版本已 `./gradlew build` 成功，但 git 历史无证据。本次对这批未提交改动做一次正确性代码审查并落地修复。

审查方式：并行 3 个 Explore agent（战斗/突破、炼丹/丹药、技能/网络/注册表），对最高风险结论（M15 品质余量、Patchouli、realm 百分比重构）亲自核读源码。

## 审查结论

无崩溃级 bug。发现并修复 1 HIGH + 2 MEDIUM；1 MED 为平衡项（确认有意后保留）；若干 LOW 记录不阻塞。

## 落地修复（0.1.61）

| 项 | 文件 | 修复 |
| --- | --- | --- |
| HIGH-1 | `AlchemyFurnaceBlockEntity.java` | M15 炼丹品质：`finishCraft` 把实际成功 `roll` 传入 `rollOutputQuality`，`margin=(successThreshold−realRoll)/successRate`，`normalized=1−margin`。修正原先用全新随机抽样、与成丹质量无关的 bug，恢复「越接近失败边界分越高」设计意图 |
| MED-1 | `BreakthroughService.java` | EXTREME 走火 `hurt(Float.MAX_VALUE)` 后补 `if(!isDeadOrDying()) player.kill()`，保证死亡，避免 Totem/吸收/事件取消让玩家存活导致已保存的 50% 物品永久滞留 PersistentData |
| MED-2 | `SyncCultivationDataPacket.java` | `cap()` 改为按 modified-UTF8 字节数截断（与 `writeUtf(s,n)` 字节上限同口径），避免中文文本撑破 writeUtf 抛 `EncoderException`；代理对落在边界时排除高代理项避免半个代理对 |
| 配置 | `build.gradle` | 恢复 Patchouli `runtimeOnly` 依赖（被误删；`PatchouliAPI` 调用有 `PATCHOULI_LOADED` 守卫不致崩溃，但 dev runClient 下引导手册发放会静默失效；与 JEI 同类 optional 依赖配置对齐） |

## 已审查确认正确（无需改动）

M14 寿元回退、M5 村民兑换、M6 灵石守卫、H13 炼丹加成、EarthWall 注册/`scheduleTick(BlockPos,Block,int)`、M7 土遁穿墙、H8 走火保物归还、CombatStats 委托 RealmStageConfig（MORTAL 短路、阶段倍率仅攻防）、sync decode 裸 `readUtf()`（线长自描述）、6 个新技能图标命中 `KNOWN_SKILL_ICONS` 无客户端类泄漏。

## 记录不阻塞（LOW）

- `ModEvents.java:599` 进阶御剑飞行仅 `abilities.flying` 时扣灵力；落地技能/mayfly 零消耗滞留。
- `ModEvents.java:687` 寿元死亡 flag 在 `hurt` 前置；Totem 存活则残留，下次受伤静默跳过打坐逻辑。
- `FastingPill.java:13` `(int)(10*multiplier)` 截断（HealingPill 用 String.format）。
- `BasePillItem.effectiveMultiplier` 无下限 floor；将来 ≤0 吸收率灵根使回血丹回 0（当前未触发）。

## 待用户确认

- MED-3：realm 改指数曲线后真仙神识 250000→51200（低 5×）、HP 10240→20480（高 2×）。无崩溃，纯平衡。若 0.1.60 本意即换百分比曲线则保留；否则需另行调整 `RealmStageConfig`。

## 版本与协议

- `gradle.properties` `mod_version` `0.1.60`→`0.1.61`。
- 未改 packet 字段/顺序/编码（MED-2 只改 `cap` 度量口径，不改 `writeUtf`/`readUtf` 行为），`ModNetwork.PROTOCOL_VERSION` 维持 `5`。

## 构建

`./gradlew build` 待执行（见 step_progress 构建记录）。
