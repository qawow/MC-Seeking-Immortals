# 0.2.55 术法 corpus 伤害、符箓退款与法宝 powerScale

## 本批完成

### 术法 SkillType 路径消费 corpus 伤害
- `AbstractTechniqueEffectResolver.resolve` 对通用形状注册效果（投射/范围/减益/自增益/恢复/召唤）优先重建 authored RuntimeSpec。
- 定制多形态库法术仍保留 SkillType 实现。
- `SpellEffect.getBaseDamage()` 暴露基础伤害；M02 在可物化时断言 fireball 12 / 符箓 18。

### 符箓失败退款事务
- `TalismanConsumePolicy.tryReserve/Reservation`：预留 → 成功 commit / 失败 refund。
- `ReleaseTechniquePacket` 主释放与 dual-cast 副释放均在 `effect.execute` 失败时退还符箓。
- 创造模式仍免扣；“已消耗”提示改到 commit。

### 法宝 powerScale 贯通通用激活伤害
- `ArtifactActivationService.applyActivation(..., powerScale)` 将 `ArtifactPowerService.scaledDamage` 接到全部通用伤害公式（含 world domain 内联伤害与 talisman 分支）。
- 新增 `M15ArtifactCorpusTest` 覆盖 scaledDamage 与激活源码契约。

## 权威与兼容
- 未改网络包字段/顺序/类型/注册/方向；仅事务时序变化，`ModNetwork.PROTOCOL_VERSION=25`。
- 无新增物品/方块/实体/菜单 id 或存档 schema。

## 验证
- 聚焦术法/符箓/伤害归属/M15 测试通过。
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 701 项测试，0 failures/errors/skipped（以最终报告为准）。
- 最终 `mod_version=0.2.55`。

## 备份
- `.bak/20260720_044116_0.2.55_corpus_damage/`

## 后续
1. mapped 主动技 `SkillContext.powerScale` 贯通。
2. CAST_* 注册表低保真清理。
3. 阵法/多方块 form→consume→rollback。
4. 持久交付 outbox、储物持续授权、实机烟测。
