# `0.2.244` Q-A 详细任务证明路由

## 范围

- 完成 `implementation_plan.md` 的 Q-A：为 23 条可玩详细任务链、95 个步骤建立服务端证明路由清单。
- Q-A 只建立严格的数据目录和运行时校验，不提前迁移 Q-B 的自然事件生产者。

## 实现

- 新增 `text_material/detailed_quest_proof_routes.json`，每步记录证明类型、唯一事件 id、必需参数、归属/队伍/消耗/重复策略、失败键、历史回放标记和生产者。
- 新增 `DetailedQuestProofCatalog`，严格校验 schema、源链/步骤覆盖、重复映射、证明类型、参数键、策略、生产者、失败键和事件 id；未知值及 `ADMIN_ONLY` 失败关闭。
- `DetailedQuestRuntimeService` 初始化时以当前 `quest_chains_playable_v141.json` 快照交叉校验路由，并通过 `proofCatalog()` 暴露已校验目录。
- 重建 text-material manifest，登记新资源；受控作者档案按 `spell -> visual` 顺序重生成并通过检查。

## 验证

- 定向测试：`DetailedQuestProofCatalogTest`、`DetailedQuestRuntimeServiceTest`、`TextMaterialManifestServiceTest` 通过。
- 生成器检查：`generate_authored_spell_effects.py --check`（2,292 profiles）、`generate_authored_visual_catalog.py --check`（5,727 profiles）通过。
- 普通构建：`./gradlew build --no-daemon --max-workers=1 --console=plain`，`:aiPreflight` 通过；256 个测试套件、1,213 项测试，failure/error/skipped 均为 `0`。
- JAR SHA-256：`bba62aa65a544dcd518cfe1c2812ad9d19bfc2e51a53ef426ce837b2c60347cf`。

## 版本与回滚

- `mod_version` 从 `0.2.243` 升至 `0.2.244`，未使用跳过版本门禁参数。
- 未改网络字段、顺序、注册或频道行为，`ModNetwork.PROTOCOL_VERSION=31` 保持不变。
- 既有文件回滚目录：`.bak/20260729_154000_q_a_proof_routes/`；受控生成档案额外备份：`.bak/20260729_154000_q_a_generated_catalogs/`。
- 下一实施入口：Q-B 接入自然事件生产者；对话动作分型、阴阳窟专属闭环、维度分类、本命飞剑/NPC 迁移和最终真实客户端/专服/双客户端 QA 仍未完成。
