# 0.2.96 阵具激活与装备右键

## 变更类
代码 + 资源（formation behaviors / lang）+ 测试。无网络包变化。

## 内容
1. **阵具**：`inspect_only` 与 `uses=null` 条目改为 `activate_free_field` 并补默认 uses；服务端仍兼容旧 inspect 文案映射到激活。
2. **装备 bulk**：`BulkItemKind.EQUIPMENT` + `CatalogEquipmentItem`
   - 灵舟/渡船/云轿/风羽筏 → `FlightVehicleService.board`
   - 傀儡 → `SummonHonestMvpService.summonProxy`
   - 丹炉 g2/g3 → 放置对应阶丹炉方块并消耗

## 版本与协议
- `mod_version` 0.2.95 → **0.2.96**
- protocol **26**

## 验证
全量构建通过，785 项左右测试 0 failures。

## 备份
`.bak/20260720_193500_0.2.96_equipment_formation/`

## 仍未完成
缺表丹药/丹券兑换、最终美术/GeckoLib、完整实机/多人复签。
