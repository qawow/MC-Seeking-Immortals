# 2026-07-29 `0.2.240` F-D 低危交互收口

变更类别：代码、资源、测试与项目文档。

## 完成内容

- UI-16：修炼属性页的移动速度滑条在同步重建、标签切换和 resize 时保留未确认的本地 pending 值；服务端快照确认相同值后才清除。
- UI-17：突破按钮在快照对象变化或 40 tick 超时前保持禁用；服务端以玩家持久数据写入 10 tick 请求闩，在资源扣除前拒绝短窗口重复请求。
- UI-21：拍卖大厅首次打开请求第 0 页，后续 resize 初始化复用当前页，服务端页数变化时仍由快照负责钳制。
- UI-23：背包“修仙”入口改用 `InventoryScreen.getGuiLeft()/getGuiTop()`，配方书位移和窗口 resize 后随原版 GUI 原点重算。
- UI-27：生活技能树统一行高、行间距、段间距及上下 inset，最大滚动精确停在末行底部 inset。

## 验证

- 定向测试：`CultivationStatsInteractionTest`、`BreakthroughRequestGateTest`、`MarketAuctionPagingTest`、`ScreenLayoutTest`。
- 生成器按 `spell -> visual` 顺序执行；`authored_spell_effects.json` 2,292 profiles、`authored_visual_catalog.json` 5,727 profiles 均通过 `--check`。
- 普通 `./gradlew build --no-daemon --max-workers=1 --console=plain`：`BUILD SUCCESSFUL`。
- 完整测试：1,210 项，failure/error/skipped 均为 0。
- JAR SHA-256：`c61063bc3a214328cc6901b163740fd0971153b6ba6f9ee7e13a313d431d95f7`。

## 版本、协议与回滚

- `mod_version`：`0.2.238 -> 0.2.240`。0.2.239 首次构建后补充客户端突破闩触发版本指纹门禁，未使用跳过参数，最终递增至 0.2.240。
- `ModNetwork.PROTOCOL_VERSION`：保持 `30`；未改变网络包字段、顺序、类型、注册或频道行为。
- 回滚目录：`.bak/20260729_0.2.239_f_d/`。
- 本批不纳入用户已有的 `CLAUDE.md` 与 `project_docs/frontend_interaction_audit_0.2.198.md`。

## 遗留

F-D 五项仍需最终真实 Minecraft 客户端抽检；下一代码批为 F-E1/UI-20，之后执行必须独立提交并提升协议的 F-E2/UI-22。
