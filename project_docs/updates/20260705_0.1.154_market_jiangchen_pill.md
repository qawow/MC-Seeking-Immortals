# 2026-07-05 0.1.154 Jiangchen + Danxia flying sword aggregate

- Change class: data/resource/test/docs/version metadata; no packet fields, packet order, packet encoding, registries, item models, textures, recipes, loot tables, GUI assets, or runtime loaders changed by this aggregate.
- Coordination: preserves the verified 0.1.153 contribution-bonus/Luoyun formula aggregate. The first final-build attempt showed the 0.1.153 preflight fingerprint was already occupied, so the current Jiangchen market and parallel Danxia flying-sword data are reconciled under `mod_version=0.1.154`.
- Text-material source: `merchant_shops.json#tiannan_herb_market` sells `jiangchen_pill` for 120 low spirit stones with stock 3; `pills_catalog.json#jiangchen_pill` identifies it as a weaker Foundation breakthrough aid.
- Text-material source: `sect_contribution_shop.json` and `artifacts_catalog.json#flying_sword_low` define a low-tier flying sword contribution-shop artifact.
- Runtime mapping: `jiangchen_pill -> seeking_immortals:foundation_building_pill_low` as `implemented_partial`, because no standalone Jiangchen Pill item/effect is registered yet.
- Runtime mapping: `flying_sword_low -> seeking_immortals:flying_sword` as `implemented_partial`, because no standalone low-tier flying sword item/progression chain is registered yet.
- Backup paths: `.bak/20260705_0.1.153_market_jiangchen_pill/`, `.bak/20260705_0.1.154_market_jiangchen_reconcile/`, `.bak/20260705_0.1.153_danxia_flying_sword_low/`, and `.bak/20260705_0.1.154_aggregate_final_docs/`.
- Verification: changed JSON parsed successfully; focused shop/worldpack/resource validation passed with BUILD SUCCESSFUL in 22s after wrapper network/cache approval; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 20s and aiPreflight recorded `mod_version=0.1.154`.
- Continuing verification: final verification docs were backed up to `.bak/20260705_0.1.154_flying_sword_final_verification/`; changed JSON parsed successfully, focused shop/worldpack/resource validation was rechecked with BUILD SUCCESSFUL in 15s after wrapper network/cache approval, and final `.\gradlew.bat --no-daemon --max-workers=1 build` was rechecked with BUILD SUCCESSFUL in 23s with `aiPreflight` recording `mod_version=0.1.154`.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains `9` because these slices changed no packet fields, field order, encode/decode format, registrations, or channel compatibility.
