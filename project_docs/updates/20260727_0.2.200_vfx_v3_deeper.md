# 0.2.200 semantic_layers_v3 保真深化

日期：2026-07-27  
变更类：代码 + 生成资源  
`mod_version`：0.2.199 → **0.2.200**  
`ModNetwork.PROTOCOL_VERSION`：保持 **30**

## 相对 0.2.199 的深化

1. **copies 上限 24→72**：`VisualProgramLayer` 与运行时 `namedCopies`、生成器复合数词表同步抬升；`七十二口剑` 现为 **72**（不再钳 24），`三十六` 为 **36**。
2. **由X转Y 双色解析**：`program_palette` 优先匹配「由/自/从 … 转/变/化 …」短语，从 11 色板键提取 primary/secondary（例 technique_1483 由红转金仍 pa 火 sa 金）。
3. **新层内形象**（仍不进 spell-effects shape，generated==rendered 52）：
   - `flying_sword` 飞剑/小剑/剑影
   - `formation_banner` 阵旗/令旗
   - `pagoda_tower` 宝塔/浮屠
   - `blood_thread` 血丝
   - `jade_slip` 玉简
   - `burning_talisman` 符纸/符火（纠正原 flame_bird 误配）
4. **aura_burst 次级过配收窄**：secondary 规则去掉裸「光芒/灵光」，aura 层从 3040→**775**。
5. 运行时 5 个新 helper 几何 + copies 硬帽 72。

## 抽检

| id | 结果 |
|----|------|
| technique_1186 | L0 copies **72**，并含 flying_sword 层 |
| technique_479 | copies **36** |
| technique_1483 | 由红转金 pa/sa 双色 |

新 primitive 命中：flying_sword 161、burning_talisman 142、light_curtain 89、formation_banner 21、jade_slip 19、blood_thread 17、cauldron 22…

契约：authored 1806、directQuotes 9947、shape 52、palette 11、source_hashes 29。

## 验证

`./gradlew build` BUILD SUCCESSFUL。

## 备份

`.bak/20260727_020000/`

## 剩余风险

- 「转」字非色变语境仍可能同色（~150 层）；可继续扩色词表。
- 粒子近似几何，非独立模型。
- 并行未提交文件未纳入。
