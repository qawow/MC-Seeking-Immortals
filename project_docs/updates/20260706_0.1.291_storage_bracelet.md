# 2026-07-06 - 0.1.291 Low Storage Bracelet Runtime

- Change class: code/resource/docs; implemented as `0.1.291` and current-tree verified under `mod_version=0.1.294`.
- Coordination: preserves the in-progress `0.1.290` 9x9 realm-gate array, verified `0.1.289` contribution-shop rank gates, and parallel elemental-area spell work without reverting shared edits.
- Text-material basis: full scan read all 315 files under `文本材料` and parsed all 207 JSON files successfully; direct basis is `artifacts_catalog.json#storage_bracelet_low`, `REFINEMENT_ARTIFACTS_MASTER_LIST.md#refine_storage_bracelet`, `ARTIFACTS_FANREN.md`, and `forge_artifact_priority.json` P0.
- Implementation: `ArtifactDataService` now reads artifact `effect`; `ArtifactStorageService` parses `extra_slots_8` and gives `storage_bracelet_low` an 8-slot NBT-backed store. Qi Refining+ players sneak-right-click to deposit the other-hand stack and normal right-click to withdraw the newest stored stack. Nested storage artifacts are blocked.
- Resources/docs: zh_cn/en_us tooltips/messages and mirrored Patchouli artifact guide entries now document storage bracelet use.
- Protocol: `ModNetwork.PROTOCOL_VERSION=9`; no packet fields/order/encoding/registrations or channel behavior changed.
- Backup: `.bak/20260706_0.1.289_storage_bracelet/` plus `post_parallel_before_docs/` for the current shared doc/version state before final docs edits.
- Verification: focused artifact tests and changed JSON parse checks passed. Full `./gradlew --no-daemon --max-workers=1 build` passed on the current shared tree with BUILD SUCCESSFUL in 31s, and `aiPreflight` recorded `mod_version=0.1.294`.
- Deferred: graphical storage menu/sync, Curios/equipment-slot policy, refinement workstation output, source-accurate placement, dedicated art, and live multiplayer inventory smoke checks.
