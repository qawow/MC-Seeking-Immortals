# 2026-07-04 0.1.101 Review Fixes (0.1.92 Plan)

## Summary

- Actual source version is `mod_version=0.1.101`; the workspace had advanced beyond the original `0.1.92` plan label and was not downgraded.
- `ModNetwork.PROTOCOL_VERSION` remains `8`.
- Implemented the review fixes for health HUD replacement gating, technique cooldown overflow clamping, Qinglan shop failure feedback, and Patchouli optional-dependency isolation.
- Removed transient market/worldpack packet-backed GUI/test surface from current source; command/listing feedback remains the non-packet path.

## Verification

- Focused tests passed: `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.network.SyncLearnedTechniquesPacketTest --tests com.xunxian.seekingimmortals.client.ScreenLayoutTest` ended with `BUILD SUCCESSFUL in 16s`.
- Final build passed: `.\gradlew.bat --no-daemon --max-workers=1 build` ended with `BUILD SUCCESSFUL in 18s`; aiPreflight recorded `mod_version=0.1.101`.

## Backups

- Main rollback path: `.bak/20260704_0.1.92_review_fixes/`.
- Latest re-clean rollback paths: `.bak/20260704_0.1.92_review_fixes/reclean_current_state/` and `.bak/20260704_0.1.92_review_fixes/reclean_current_state_2/`.

## Notes

- No packet fields, field order, encoding, registrations, or channel compatibility changes are retained.
- No new obtainable items, blocks, models, textures, recipes, loot tables, tags, data packs, or creative-tab entries were added by this review-fix pass.
- Manual checks remain recommended for `runClientNoPatchouli`, chat/open-screen/F1 health visibility, GUI Scale 1/2/3/Auto, and Qinglan no-stock/currency-error prompts.
