# 2026-07-04 0.1.94 cultivation stats screen and key

## Summary
- Implemented the requested cultivation stats screen restyle originally planned as 0.1.93.
- Final shipped workspace version is `0.1.94` because `.gradle/ai-preflight/last-success.json` had already recorded a successful `0.1.93` fingerprint before the final build, so the build gate required the next patch version.
- No packet fields, packet order, encode/decode format, registrations, or channel compatibility were changed. `ModNetwork.PROTOCOL_VERSION` remains `8`.

## Changes
- `CultivationStatsScreen` now uses an ink-and-gold native `GuiGraphics.fill` style with jade/gold status bars, warning colors, a title banner, two-column normal layout, and single-column narrow layout.
- The screen keeps existing behavior: breakthrough button, movement-speed slider and server sync, waiting-for-sync text, inventory return behavior, and close-to-game behavior.
- Added `key.seeking_immortals.open_cultivation_stats`, default unbound, to open the screen from gameplay when no other screen is open.
- Added layout helpers and `ScreenLayoutTest` coverage for 120x90, 180x120, 320x180, and 854x480 scaled sizes, control overlap, column collapse, and inventory/shortcut return behavior.
- Added zh_cn/en_us localization for the new key and screen path title.

## Verification
- Focused test passed: `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.client.ScreenLayoutTest` ended `BUILD SUCCESSFUL in 17s`.
- Final required build passed: `.\gradlew.bat --no-daemon --max-workers=1 build` ended `BUILD SUCCESSFUL in 28s`; `aiPreflight` recorded `mod_version=0.1.94`.
- Build printed the existing network-package warning for dirty network files, but this pass did not edit network packets and did not change protocol compatibility.

## Backup
- Existing edited files were backed up under `.bak/20260704_0.1.93_cultivation_stats_screen_key/`.
