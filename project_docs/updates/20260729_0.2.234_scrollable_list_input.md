# `0.2.234` F-B 统一滚动列表输入契约

## 变更

- `ScrollableListPanel` 增加按下待定、内容拖动、轨道分页和拇指拖动状态机。
- 行列表改用整数首行滚动；content inset、行间隙和末行尾部空白不再误命中。
- 任务、主题、炼丹、炼器、世界包、宗门、坊市和拍卖界面统一转发列表输入；大厅按钮优先于列表处理点击。
- 新增 `ScrollableListPanelInteractionTest`，并同步宗门候选列表消费者契约断言。

## 验证

- `python3 -B scripts/generate_authored_spell_effects.py --check`：2,292 profiles，通过。
- `python3 -B scripts/generate_authored_visual_catalog.py --check`：5,727 profiles，通过。
- `./gradlew test --no-daemon --max-workers=1 --console=plain`：1,194 项通过。
- `./gradlew cleanTest build --no-daemon --max-workers=1 --console=plain`：`BUILD SUCCESSFUL in 1m 44s`；`:aiPreflight` 记录 `mod_version=0.2.234`。
- JAR：`build/libs/seeking_immortals-0.2.234.jar`，SHA-256 `5aab5bf9b7a5bbe77b32a2ef645f958fef6797a13535f0f4c3cfd947bd5c7e89`。

## 版本与回滚

- 变更类别：代码、测试、受控生成资源、版本和项目文档。
- `mod_version`：`0.2.233 -> 0.2.234`。
- `ModNetwork.PROTOCOL_VERSION`：保持 `30`；未改包字段、顺序、类型、注册或频道行为。
- 备份：`.bak/20260729_200009_f_b_scrollable_list/`、`.bak/20260729_204500_f_b_followup/`、`.bak/20260729_210000_f_b_finalize/`。
- 真实 Minecraft 客户端的逐屏滚轮、内容拖动和滚动条视觉验收仍属于最终 QA；用户既有 `CLAUDE.md` 与 `project_docs/frontend_interaction_audit_0.2.198.md` 未纳入本批。
