# 2026-07-06 - 0.1.247 current-tree build recheck

Change class: docs/build-verification reconciliation only.

Context:
- The shared workspace advanced to `mod_version=0.1.247` after the 0.1.245 Diyuan Permit entry-consumption path and the observed 0.1.246 Diyuan no-fly runtime state.
- This recheck preserved the current tree and did not revert parallel work.

Verification:
- `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 19s.
- `aiPreflight` recorded `mod_version=0.1.247`.

Protocol: unchanged at `ModNetwork.PROTOCOL_VERSION=9`; this recheck did not touch packet fields/order/encoding/registrations or channel behavior.

Backups:
- Recheck docs: `.bak/20260706_0.1.247_current_tree_recheck_codex/`

Notes:
- Focused Diyuan Permit entry validation remains the 0.1.245 `WorldpackPortalArrayRulesTest`, `JsonSanityTest`, and `ResourceJsonParseTest` pass.
