# 2026-07-04 0.1.89 Divine-sense detection highlight

- `mod_version` bumped to `0.1.89`.
- `DetectionSpell` now highlights scanned living entities with a 10-second vanilla glowing outline and END_ROD particles.
- Entity hits are sorted by distance and capped at 64 highlights per scan to avoid extreme high-realm particle/effect spam.
- Existing spiritual block-wave particles remain capped at 32 matches.
- `ModNetwork.PROTOCOL_VERSION` remains `8`; no packet fields, order, encoding, registrations, or channel compatibility changed.
- Backup path: `.bak/20260704_0.1.89_detection_highlight/`.
- Build verification passed:
  - Normal `./gradlew --no-daemon --max-workers=1 build` passed with `BUILD SUCCESSFUL in 21s`.
  - Forced `./gradlew --no-daemon --max-workers=1 build --rerun-tasks` passed with `BUILD SUCCESSFUL in 38s`; 11 tasks executed.
  - The network-package warning is inherited from existing dirty network files; this pass did not edit packet fields, order, encode/decode format, registrations, or channel compatibility, so protocol remains `8`.
