# 2026-07-07 0.1.333 Demonic/Ghost spell compile fix

## Summary

- Observed the shared tree advance to `mod_version=0.1.333` after the verified `0.1.332` FTB item-task bridge.
- Fixed the current compile blocker where `SkillEffectRegistry` referenced `DemonicGhostSpell` but the class did not exist.
- Added `DemonicGhostSpell` with six forms:
  - `BLOOD_SHADOW_ESCAPE`
  - `SKY_SUPPORTING_DEMONIC_SKILL`
  - `MYSTIC_SOUL_GHOST_FIRE`
  - `MYSTIC_SOUL_BONE_CONDENSING_ART`
  - `BLOOD_LUO_BARRIER`
  - `YIN_DEMON_SLASH`
- Reused the existing `SpellEffect` / `SkillContext` path, server-authoritative magic damage, vanilla effects/sounds, safe teleport checks, projectile deflection, and code-side `DustParticleOptions`.

## Coordination

- Preserved the `0.1.332` FTB mainline item-task bridge and earlier FTB/item/spell work.
- Did not touch packet schemas, network registrations, `QuestProgress`, item/block/entity registries, GUI/menu classes, FTB resources, recipes, loot, shops, worldpack schemas, artifact/refinement maps, or runtime loaders.
- `ModNetwork.PROTOCOL_VERSION` remains `9`.

## Verification

- Initial focused `FtbQuestSnbtTest` failed at `compileJava` because `DemonicGhostSpell` was missing.
- After adding the class, focused `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.quest.FtbQuestSnbtTest` passed with BUILD SUCCESSFUL in 24s.
- Full `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 24s; `aiPreflight` recorded `mod_version=0.1.333`.
- The inherited network-package warning remains present and unrelated to this spell compatibility slice.

## Backup

- Existing docs: `.bak/20260707_0.1.333_demonic_ghost_spell_compile_fix/`
- New source file had no previous version to back up.

## Remaining Work

- Add strict demonic/ghost prerequisite gates and locked-reason UI.
- Add dedicated spell icons, particle textures, manual art, and source-accurate manual/sect/ruin acquisition.
- Tune balance/PvP and run live client visual smoke tests.
