# 0.1.456 Artifact / refine depth (Wave3 priority-3)

## Class
Code (refinement service + forge blocks + artifact item + natal) + lang + docs

## Priority board (1→5)
1. 任务权威闭环 — 0.1.454
2. 召唤/傀儡/灵兽实体化 — 0.1.455
3. **法宝/炼器深度** ← this wave
4. 秘境实例化内容
5. 美术与 live smoke 抛光

## Changes
- `ArtifactRefinementService.refine(player, recipeId, forgeGrade)`
  - reject recipes requiring higher forge grade
  - +5% success per extra grade (cap +15%, max 95%)
- `RefinementForgeBlock` / `G2` / `G3` pass grade 1/2/3
- `ArtifactCatalogItem` sneak-use binds natal artifact
- `ArtifactActivationService` grows natal on activation and shortens cooldown
- lang: forge_too_low, natal bind/bound tooltips

## Protocol
13 unchanged

## Build
`./gradlew --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.456`

## Backup
`.bak/20260713_010731_wave456_artifact_refine_depth/`
