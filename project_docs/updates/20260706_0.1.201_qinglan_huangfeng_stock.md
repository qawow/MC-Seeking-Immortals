# 2026-07-06 0.1.201 Qinglan Huangfeng stock backfill

- Change class: resource/data/test/docs. Parallel work advanced `gradle.properties` through `mod_version=0.1.199` and the build-verified `0.1.200` Spirit Rain Bonus slice; this slice preserves the current `0.1.201` version and completes the matching shippable data.
- Scope: expand the existing `qinglan_contribution_hall` with implemented equivalents from `文本材料/data/sect_contribution_shop.json#huangfeng_valley`.
- Runtime data: Qinglan now sells `foundation_pill -> seeking_immortals:foundation_building_pill_low`, `recipe_jiangchen -> seeking_immortals:alchemy_formula_foundation_building_pill_paper`, `recipe_huanglong -> seeking_immortals:alchemy_formula_cultivation_pill_paper`, `recipe_ningshen -> seeking_immortals:alchemy_formula_calming_pill_jade`, and `flying_sword_low -> seeking_immortals:flying_sword`.
- Reuse decision: this uses the existing `ShopService`, virtual `sect_contribution` currency, registered pill/formula/artifact item carriers, and current JUnit/Gradle validation path rather than adding a new runtime loader, packet, registry entry, model, texture, recipe, loot table, GUI, or prerequisite-specific bridge.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed.
- Backup path: `.bak/20260706_0.1.199_qinglan_huangfeng_stock/`.
- Verification: focused `ShopServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 22s. Final `.\gradlew.bat --no-daemon --max-workers=1 build` is pending.
