# 0.1.457 Quest authority deepen (Wave1 priority-1 follow-up)

## Class
Code (quest services + tracker packet/UI + client parse) + lang + tests + docs

## Priority board (1→5)
1. **任务权威闭环加深** ← this wave
2. 召唤/傀儡/灵兽实体化
3. 法宝/炼器深度
4. 秘境实例化内容
5. 美术与 live smoke 抛光

## Changes
- `TextQuestChainService`
  - catalog `rewards_finale` → authority grant via `resolveItem` (fallback soft table)
  - branch finale bonus ledgered as `id#branch`
  - chain finish auto-completes `main_chapter_ref` via `MainStorySoftService.completeQuiet`
  - richer tracker lines: `cost=/own=/LOCK=/REW=`
- `QuestTrackerActionPacket`: NPC proximity gate on advance/branch; status line prefix
- `SyncQuestTrackerPacket`: no focus steal over dialogue; refresh open tracker
- `QuestTrackerScreen` / `ClientQuestTrackerData`: Advance + branch buttons using existing UTF encodings
- `MainStorySoftService`: chapter↔chain refs, `startChapter`, quiet complete
- `QuestHookSoftService`: honest empty mapping (no silent huangfeng); expanded hook map
- `FtbRewardBridgeService`: ledger mirror only, no independent pay table

## Protocol
13 unchanged (still one UTF action field)

## Build
`./gradlew.bat --no-daemon --max-workers=1 test --tests ...TextQuest* --tests ...QuestHook* --tests ...MainStory* build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.457`
Network package warning notes QuestTrackerActionPacket/SyncQuestTrackerPacket logic only (still one UTF field / line list; protocol remains 13).

## Backup
`.bak/20260713_014133_wave457_quest_authority/`
