# 2026-07-04 0.1.89 execution wave closure

Change class: code + resources + docs/tests. The requested 0.1.85 wave was completed on a workspace that had already advanced to `mod_version=0.1.88`, so the final shippable version was bumped forward to `0.1.89` to satisfy the Gradle version gate without using `-PaiSkipVersionBumpCheck=true`.

## Implemented

- `SyncLearnedTechniquesPacket` now computes cooldown remaining ticks against `player.getServer().overworld().getGameTime()`, matching release enforcement.
- Added no-protocol command entry points for `market`, `worldpack`, and six-sect candidate/application listing.
- Added six sect definitions as server-side model/service data using existing `QuestProgress` sect fields.
- Kept Qinglan GUI and packet flow unchanged.
- Changed the execution-wave market and worldpack ticket data to use registered `seeking_immortals:metal_spirit_stone` instead of legacy unregistered `spirit_stone`.
- Added focused tests for cooldown remaining math, shop/worldpack data, sect definitions, and shipped JSON resource contracts.
- Updated JEI notes to reflect the existing shipped-recipe alchemy category and its limitation.

## Protocol

`ModNetwork.PROTOCOL_VERSION` remains `8`. This pass did not change packet fields, field order, encode/decode shape, registration order, or channel compatibility.

## Validation

- Focused tests passed after escalated Gradle wrapper access: `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.network.SyncLearnedTechniquesPacketTest --tests com.xunxian.seekingimmortals.shop.ShopServiceTest --tests com.xunxian.seekingimmortals.worldpack.WorldpackDataServiceTest --tests com.xunxian.seekingimmortals.sect.SectDefinitionServiceTest --tests com.xunxian.seekingimmortals.resources.JsonSanityTest` -> `BUILD SUCCESSFUL in 25s`.
- Required final build passed: `./gradlew --no-daemon --max-workers=1 build` -> `BUILD SUCCESSFUL in 20s`; `aiPreflight` recorded `mod_version=0.1.89`.
- Forced full verification also passed: `./gradlew --no-daemon --max-workers=1 build --rerun-tasks` -> `BUILD SUCCESSFUL in 38s`; 11 actionable tasks executed.
- The network-package warning was reviewed and does not require a protocol bump for this pass.

## Remaining risk

- Manual client regression remains pending for market command use, worldpack command display, six-sect application edge cases, JEI/Patchouli visual checks, and full in-world Phase 10 storyline behavior.
- Non-Qinglan sect applications are data/model-only in this pass; Qinglan-specific GUI/shop flows are intentionally unchanged and do not yet provide full multi-sect content.
