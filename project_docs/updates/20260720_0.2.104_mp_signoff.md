# 0.2.104→0.2.105 专服 + 双客户端多人复签基建

日期：2026-07-20  
`mod_version`：`0.2.105`（0.2.104 基建 + 专服 Screen 侧加载修复）  
`ModNetwork.PROTOCOL_VERSION`：`26`（无包字段变更；仅 handler 实现迁移）

## 目标

落实 `master_plan.md` §6.9「专服 + 双客户端」可执行复签：代码权威回归 + 命令探测 + 人工清单。  
**不**伪称真人双客户端已签字；签字仍需人工在专服上完成。

## 变更

### 代码

- `ModNetwork.protocolVersion()`：供 live-smoke 读取协议。
- `LiveSmokeChecklistService`：
  - `multiplayerAutoProbe`：protocol / server_mode / auction ledger / outbox / station ops / mp sequence。
  - `MANUAL_MP_STEPS`：mp_pvp、mp_station_concurrent_form、mp_auction_outbid、mp_outbox_reconnect、mp_technique_cooldown_reconnect、mp_protocol_match。
  - `signMultiplayer` + 报告 `live_smoke_mp_report_*.md`。
- `SeekingImmortalsCommand`：`/seeking_immortals live_smoke mp [run|sign]`。
- `PlayerPersistentDataClonePolicy`：多人签字 4 键跨死/克隆持久。

### 测试

- 新增 `MultiplayerAuthorityRegressionTest`（拍卖/工站/登录 claim/PvP/协议/live_smoke 面）。
- `PlayerPersistentDataClonePolicyTest` durable key 40→44。

### 文档 / 资源

- `project_docs/manual_multiplayer_signoff_checklist.md`
- 本 update 笔记
- 双语 lang：`live_smoke.mp_header` / `mp_manual_header` / `mp_signed`

## 验证

- 单元/契约：`MultiplayerAuthorityRegressionTest` + 既有网络方向/拍卖兼容/冷却 NBT。
- 全量 `./gradlew build`（见提交说明）。
- 专服：若环境允许，`./gradlew runServer` 启动日志无 mod 致命错误（双客户端仍待人工）。

## 备份

`.bak/20260720_230500_0.2.104_mp_signoff/`

## 剩余

- 真人专服 + 双客户端按清单签字。
- 完整实机单机烟测 §6.1–8。
- 工作室级手绘 GeckoLib 精模。
