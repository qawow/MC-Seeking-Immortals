# 0.2.70 多方块 form 建站事务

## 本批完成

### form → consume → commit
- `MultiblockOperationalService.form`：结构已成型后预留结构材料与碎片税，再 `forceIntact` 启封。
- 新工站 `ensureState` 默认 `DISABLED/0HP`，未建站前效率为 0。
- 已损伤运行态拒绝 form，提示改用 overhaul。
- 失败退还材料与碎片。

### 未解析材料逐项税
- `MultiblockMaterialCatalog.unresolvedShardTax`：仅对无法解析为物品的组件按价带 minPrice 计税（封顶 128）。
- `form` / `overhaul` 均使用该税，不再“全部解析失败才加税”。

### 命令与文案
- `/seeking_immortals station form <id>`
- 中英文建站消息键：`form_need_shell` / `form_need_materials` / `form_need_shards` / `formed_ok` 等。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体。
- 已有世界若已有工站 SavedData 条目行为不变；新位置需 form 启封。

## 验证
- 定向契约测试覆盖 form 预留顺序、unresolved 税、命令注册。
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 730 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.70`。

## 备份
- `.bak/20260720_103500_0.2.70_form_commission/`

## 后续
1. 工站交互入口自动引导 form（方块 use / 软制作提示）。
2. 更深材料 id 解析以降低碎片税。
3. 实机客户端/专服/多人烟测。
