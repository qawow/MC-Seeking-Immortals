# 2026-07-07 - 0.1.371 Talisman Soul Charm version reconcile

## Summary
- Preserved the `refine_talisman_soul_charm` recipe after the shared tree required additional version-gate reconciliation.
- Full build attempts at `0.1.369` and `0.1.370` were blocked by `aiPreflight` because shippable tracked-file fingerprints had changed since the previous successful build.
- Advanced the shared version to `0.1.371` without reverting concurrent work.

## Verification
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL.
- `aiPreflight` recorded `mod_version=0.1.371`.

## Compatibility
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed by this recipe/docs slice.
- The inherited network-package warning remains a release-audit item if pre-existing packet diffs changed compatibility.
