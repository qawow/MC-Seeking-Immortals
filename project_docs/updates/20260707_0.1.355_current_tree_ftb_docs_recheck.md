# 0.1.355 Current-Tree FTB Docs Recheck

## Summary

- Change class: docs-only coordination and verification note
- Current version: `mod_version=0.1.355`
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, order, encoding, registrations, or channel behavior changed
- Backup paths: `.bak/20260707_0.1.353_docs_format_repair/` and `.bak/20260707_0.1.355_current_tree_docs_recheck/`

## Work Completed

Repaired the current `0.1.353` Fallen Demon/Yin FTB documentation after a formatting pass had split `./gradlew` commands and corrupted the `seeking_immortals:void_crystal` item id. The pass also restored current `QuestProgress` code styling in the feature note.

This coordination pass preserves the parallel `0.1.354` Dao spells and `0.1.355` Yellow Umbrella recipe work without changing Java source, FTB quest SNBT, tests, recipes, assets, registries, network packets, capabilities, player NBT schema, shops, worldpack data, or gameplay behavior.

## Verification

- `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.quest.FtbQuestSnbtTest`
- `./gradlew --no-daemon --max-workers=1 build`

Both passed. The final full build recorded `mod_version=0.1.355` in `aiPreflight`.

Remaining risk: underlying FTB item tasks are still non-consuming inventory checks only; live FTB client smoke tests and server-authoritative quest/reward/state bridges remain future work.
