# 2026-07-07 - 0.1.360 Artifact failure loot

- Change class: code/resource/docs gameplay behavior.
- Text-material read: scanned all 315 files under `文本材料` as UTF-8 and parsed all 207 JSON files successfully.
- Direct basis: `data/seeking_immortals/artifacts/refinement_failure_loot.json`, `data/seeking_immortals/reference/text_material_id_map.json`, `ArtifactDataService`, and `ArtifactRefinementService`.
- Implementation: `ArtifactDataService` now parses default and tier-specific failure-loot entries. Failed exact command refinement now rolls one salvage entry, grants the mapped item when valid, skips missing/invalid mappings safely, and reports the salvage.
- Mapping: added temporary `scrap_spirit_iron -> seeking_immortals:spirit_iron` as an implemented-partial failure-scrap fallback.
- Version/protocol: current `mod_version=0.1.360`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed.
- Verification: focused artifact data/refinement tests passed. Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL and `aiPreflight` recorded `mod_version=0.1.360`.
- Backups: `.bak/20260707_062415_0.1.357_artifact_failure_loot/`, `.bak/20260707_062550_0.1.358_artifact_failure_loot_reconcile/`, `.bak/20260707_0.1.359_artifact_failure_loot_docs/`, and `.bak/20260707_0.1.360_artifact_failure_loot_version_reconcile/`.
- Remaining risk: one command-path salvage roll only; real refinement forge UI, custom serializer, failure explosion/mismatch handling, JEI display, dedicated scrap itemization, source-accurate acquisition, and live multiplayer smoke tests remain future work.
