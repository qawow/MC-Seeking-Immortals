# `0.2.246` Q-B-1 安全硬化与术法回补

## 范围

- 收紧结构化任务证明的管理员权限、路由一致性和当前步骤边界。
- 回补已达到功法层数、但此前因境界不足而延迟的术法解锁。
- 不接入 Q-B-2 或后续事件域，不改变网络消息格式。

## 实现

- `DetailedQuestProofService.adminProve` 在服务边界校验 permission 2；`DetailedQuestProofEvent.asAdmin` 保留原始 producer。
- `DetailedQuestRuntimeService.advanceVerifiedRoute` 要求传入路由与目录记录完全一致，且任务已开始、未完成、当前步骤匹配；内部推进再次校验 verified route 步骤。
- `MethodLayerTechniqueService.backfillEligibleTechniques` 遍历玩家已学功法并按当前层数和境界重新授予合法术法；登录、无雷劫突破完成及雷劫最终成功后调用。
- 契约测试覆盖权限、错路由、错步骤、producer 保留和延迟术法回补。

## 版本与验证

- 变更类别：Java 行为、测试、受控生成资源与文档。
- `mod_version=0.2.246`。
- 未改网络字段、顺序、类型、注册或频道行为，`ModNetwork.PROTOCOL_VERSION=31` 保持不变。
- 两个受控生成目录按 `spell -> visual` 顺序刷新，2,292/5,727 profiles 检查通过。
- `./gradlew build --no-daemon --max-workers=1 --console=plain`：`BUILD SUCCESSFUL in 1m 26s`。
- 测试：1,219 项，failure/error/skipped 均为 0。
- JAR SHA-256：`b3400910a75b8b480f9ccdc8d4f91688e8cca7166b9032fb09b87ba3fa44c223`。

## 回滚与剩余工作

- 完整回滚：`.bak/20260730_232529_q_b_hardening_finalize/`。
- 早期局部备份：`.bak/20260730_011500_q_b_hardening/`、`.bak/20260730_010000_q_b_1_security/`。
- Q-B-2 至 Q-B-5、D-A、阴阳窟专属闭环、维度分类、本命飞剑/NPC 迁移和最终实机签字仍未完成。
