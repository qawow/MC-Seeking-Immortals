# 0.2.110 傀儡核心炉/化形池专属校验与渡口费 OR 门

日期：2026-07-20  
`mod_version`：`0.2.110`  
`ModNetwork.PROTOCOL_VERSION`：`26`（无包变更）

## 变更

1. **`puppet_core_forge`**：专属 `PuppetCoreForgeStructure`（核心+半径 1 灵矿环），不再纯 ring。
2. **`spirit_beast_evolution_pool`**：专属 `SpiritBeastEvolutionPoolStructure`（聚灵环+角柱+甲板净空）。
3. **冥河渡口 requires**：`yin_stone|nether_ferry_coin` 与 `ferry_pass|nether_ferry_token`（OR 组，现有 `_or_`/`|` 解析）。
4. **生活功法层数**：`artifact_refining_basic` / `treasure_appraisal_art` 的 `layers_max` 对齐矩阵 2（长春功仍 13 层原著带映射）。

## 测试
- `PuppetBeastStationStructureTest`
- 扩展 `ItemFerryAndPillRoutingTest`
- 全量 `./gradlew build`

## 备份
`.bak/20260721_003000_0.2.110_puppet_beast_stations/`

## 剩余
- 真人专服双客户端签字、完整实机烟测、工作室 GeckoLib
- 可选：死 bulk 秘籍显式授予、更多 currency 门控、hub→阵法核心
