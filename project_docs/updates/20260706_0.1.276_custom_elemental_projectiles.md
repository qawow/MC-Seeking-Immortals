# 2026-07-06 0.1.277 Custom elemental projectiles build verified

Change class: code/resources/data/docs.

Source basis:
- `文本材料/data/techniques/elemental.json`: `fireball`, `water_arrow`, `metal_needle`
- Current shipped `data/seeking_immortals/cultivation/qi_refining_techniques.json`
- User constraint: prefer custom mod spell projectiles/visuals over vanilla fireball/fire-charge style implementations.

Implemented:
- Reused `seeking_immortals:cultivation_fireball` as a mod-owned elemental projectile with synced FIRE/WATER/METAL variants.
- Replaced the rendered item-model fireball look with a custom glowing projectile-core renderer.
- Added colored spiritual-dust trails and impact bursts, with fire burn, water slow, and metal weakness effects.
- Added reusable `ElementalProjectileSpell`.
- Wired `fireball_art`, `fire_bullet_art`, `water_arrow`, and `metal_needle` through the custom projectile path.
- Added `water_arrow` and `metal_needle` to Qi Refining technique data through the current Changchun appendix manual approximation.

Compatibility:
- No SimpleChannel packet fields/order/encoding/registrations changed.
- `ModNetwork.PROTOCOL_VERSION` remains `9`.
- No new item/block/model/texture/recipe/loot/shop/GUI/runtime-loader surface was added.

Verification:
- Changed technique/lang JSON parsed successfully.
- `.\gradlew.bat --no-daemon --max-workers=1 compileJava --rerun-tasks` passed.
- Final current-tree `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 21s and `aiPreflight` recorded `mod_version=0.1.277`.
- The inherited network-package warning was reviewed; this spell slice did not touch packet files.

Deferred:
- Dedicated particle types/textures and spell icons.
- Live-world client smoke test of projectile framing.
- Replacing remaining raycast/lightning/vanilla-particle approximations with custom entities or area visuals.
- Source-accurate prerequisite-method enforcement for `changchun_gong`.
