# 0.1.497 Wave497 — catalog manual tooltip localization

Date: 2026-07-15

## Problem

User reported 练气入门篇 / 补天丹方 still showing code/English after Wave496.

Root cause for manuals:

1. `CatalogManualItem` tooltip used raw `manualId` (`recipe_bu_tian`, `refinement_manual_low`)
2. `ManualCatalogService.study` messages printed raw `type` / `realmMin` / `recipeId`
3. Specialty method displays still had English prefixes (`sword专精入门`)

Note: closest match for “入门篇” in shipped items is **炼器入门篇** (`refinement_manual_low`). 补天丹方 is `recipe_bu_tian`.

## Changes

- `CatalogManualItem`: tooltip shows Chinese title/type/realm/recipe
- `ManualCatalogService`: localized type/recipe/realm in study feedback
- Added `manual.type.*` and catalog manual tooltip keys
- Fixed specialty method/skill-tree Chinese displays

## Version / protocol

- `mod_version`: `0.1.496` → `0.1.497`
- protocol unchanged: `18`

## Build

`./gradlew build` → **BUILD SUCCESSFUL**

## Backup

`.bak/20260715_034500/`
