# 2026-07-06 0.1.212 Mulan-Tianlan War Phase Hooks

- Change class: worldpack data/test/docs slice.
- Source: `文本材料/data/mulan_tianlan_war.json`.
- Implementation: added daily-event hooks for `border_skirmish`, `fashi_array_clash`, `holy_beast_intervention`, and `ceasefire_or_escalation` using existing `WorldpackDataService`, current `mulan`/`tianlan` regions, and existing effect tokens only.
- Compatibility: preserved broader existing Mulan/Tianlan events such as `mulan_tianlan_campaign`, `tiannan_mulan_border_war`, `tianlan_holy_beast_ritual`, and `tianlan_mulan_scout_clash`.
- Tests/build: changed `daily_events.json` parsed successfully with 132 events and no duplicate ids; focused `WorldpackDataServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 20s; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 23s and `aiPreflight` recorded `mod_version=0.1.212`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields/order/encoding/registrations or channel behavior changed.
- Backup: `.bak/20260706_0.1.212_mulan_tianlan_war_phases/`.
- Build-verification docs backup: `.bak/20260706_0.1.212_mulan_tianlan_war_phases_build_verify/`.
- Remaining work: real war-cycle scheduling, player side choice, battlefield encounters, fashi array objectives, holy-beast trial routing, side reputation, rewards, NPCs, and richer FTB Quests/Patchouli presentation remain deferred.
