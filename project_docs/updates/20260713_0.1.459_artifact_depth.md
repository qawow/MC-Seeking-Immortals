# 0.1.459 Artifact / refine depth (Wave3 priority-3)

## Class
Code (refinement + forge blocks + activation + natal) + lang + docs

## Priority board (1→5)
1. 任务权威闭环 — 0.1.454/457
2. 召唤/傀儡/灵兽 — 0.1.455/458
3. **法宝/炼器深度** ← this wave
4. 秘境实例化内容
5. 美术与 live smoke 抛光

## Changes
- `ArtifactRefinementService.selectRecipeId(player, forgeGrade)` grade filter
- failure salvage grade bonus rolls + public `grantDefaultFailureLoot`
- refine success grows natal if bound artifact matches
- forge blocks: no false `no_recipe` on fail; serializer path salvages
- integrity last-light + natal-scaled integrity/SP/CD
- repair kit only consumes when repaired>0
- natal milestones + stack growth tooltip

## Protocol
13 unchanged

## Build
`./gradlew --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.459`

## Backup
`.bak/20260713_015612_wave459_artifact_depth/`
