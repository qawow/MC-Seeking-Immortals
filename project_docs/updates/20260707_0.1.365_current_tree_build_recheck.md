# 2026-07-07 0.1.365 Current-Tree Build Recheck

## Summary

Rechecked the shared tree after the Tiannan seven-sects FTB item-task bridge and parallel Demon Rift/Qingning Mirror version movement.

## Verification

- Focused `FtbQuestSnbtTest` passed after the Tiannan bridge changes.
- Final current-tree `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL.
- `aiPreflight` recorded `mod_version=0.1.365`.

## Protocol

`ModNetwork.PROTOCOL_VERSION` remains `9`; this recheck changed docs only and did not change packet fields/order/encoding/registrations or SimpleChannel behavior.

## Backups

- `.bak/20260707_0.1.365_current_tree_build_recheck/`

## Remaining Risk

The inherited network-package warning still needs a focused protocol audit before release if those pre-existing packet diffs changed compatibility. FTB item tasks remain inventory-check bridges only, and the parallel portal/refinement work still needs live smoke tests.
