# 2026-07-06 0.1.225 dimension portal rules

- Scope: verification and docs sync for the current dimension and multiblock portal slice.
- Current runtime behavior: the shipped `seeking_immortals:tianyuan` and `seeking_immortals:spirit_fengyuan` dimension JSON files are present; `WorldpackGameplayService` seeds default anchors for loaded custom dimensions; Spirit Realm worldpack routes require a complete portal array instead of ordinary UI travel.
- Portal structure: sneak-use the center block of a complete 3x3 same-level Spirit Gathering Array floor. Mortal regions route to `tianyuan`; `tianyuan` routes to `spirit_fengyuan`; Spirit Realm regions route back to `tianyuan`.
- Tests: added `WorldpackPortalArrayRulesTest` to cover Spirit Realm tag gating and portal destination selection.
- Protocol: no packet field/order/encoding/registration or channel behavior changed; `ModNetwork.PROTOCOL_VERSION` remains 9.
- Verification: final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 38s under the current `0.1.226` aggregate; `test` executed `WorldpackPortalArrayRulesTest` with 2 tests and 0 failures/errors, and `aiPreflight` recorded `mod_version=0.1.226`.
- Follow-up: custom DimensionType/aura nature, dedicated portal visuals, travel costs, richer structure validation, and real generated destinations remain deferred.