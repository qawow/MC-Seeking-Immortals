# 2026-07-06 0.1.174 Diyuan pressure daily-event hook

- Change class: shipped worldpack daily-event data/test/docs plus version metadata.
- Text-material source: `文本材料/data/daily_random_events.json#diyuan_pressure_wave`, with supporting context from `secret_realms.json#diyuan` and `region_cards/spirit_fengyuan.json`.
- Implemented: added `diyuan_pressure_wave` under the existing `spirit_fengyuan` region because `diyuan` is currently shipped as a secret realm, not a worldpack region.
- Scope: the event reuses existing daily-event schema fields and current effect-token approximations only: `trade_risk_up` and `secret_realm_ticket_hint`.
- Backup path: `.bak/20260706_0.1.174_diyuan_pressure_event/`.
- Version/protocol: `mod_version=0.1.174`; `ModNetwork.PROTOCOL_VERSION=9` unchanged because no packet fields/order/encoding/registrations changed.
- Verification: changed worldpack JSON parsed successfully; focused `WorldpackDataServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 23s after wrapper network/cache approval; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 25s and aiPreflight recorded `mod_version=0.1.174`.
- Remaining work: real Diyuan layered dungeon gameplay, pressure/no-fly hazards, permit/token access, pressure-resistance pills and charms, ancient beast encounters, core-crystal rewards, quest-chain routing, and source-accurate realm gates.
