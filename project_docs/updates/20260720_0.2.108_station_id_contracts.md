# 0.2.108 工站 id 契约与飞舟泊桩专属校验

日期：2026-07-20  
`mod_version`：`0.2.108`  
`ModNetwork.PROTOCOL_VERSION`：`26`（无包变更）

## 变更

1. **炼器台委托 id**：G1/G3/G4–G6 启封改为目录 id `refinement_forge_gN`，去掉不存在的 `refinement_forge` 回退。
2. **软炼器/祭炼效率**：附近工站列表扩到 g1–g6，不再引用死 id。
3. **对话阵法状态**：`resolveNearbyArrayState` 用 `teleport_array_pedestal` / `immortal_teleport_grand_array` 扫附近成型，不再把 validator 名当 station id。
4. **飞舟泊桩**：`flying_boat_dock` pattern validator 改为专属 `flying_boat_dock`，`MultiblockStationService` 走 `FlyingBoatDockStructure`（与 `FlightVehicleService` 几何一致）。

## 测试
- 新增 `StationIdContractTest`
- 扩展 `M07FormationsMultiblockTest` dock validator
- 全量 `./gradlew build`

## 备份
`.bak/20260720_235900_0.2.108_station_ids/`

## 剩余
- 真人专服双客户端签字、完整实机烟测、工作室 GeckoLib
- 可选：meta 修复台/蓝图台可放置交互、傀儡/化形池专属几何、hub→阵法核心接线
