# 0.1.494 zh item localize

- Change class: resources + code (tooltips) + project docs.
- Backup: `.bak/20260715_160000_zh_item_localize/`.
- Version: `mod_version=0.1.494`.
- Protocol: remains `18`.

## Delivered
1. Translated 37 English-only `item.seeking_immortals.*` names in `zh_cn.json` to Chinese.
2. Artifact tooltips in `ArtifactCatalogItem` / `FlyingArtifactItem` now use bilingual `tooltip.seeking_immortals.artifact.*` keys.
3. Bulk catalog carrier descriptions (869) localized to `目录载体：…`.
4. High-impact catalog index display fields mapped to Chinese where item lang exists (+ extra king/priority/treasure names).

## Remaining
- ~220 catalog index `display` fields still English (mostly chronicle/event/internal ids, not item hover names).
- Material/pill enum display names are already Chinese code literals.
