# 2026-07-03 0.1.81 Qinglan Sect contribution exchange

## Summary

- Added the first sect contribution exchange MVP through Qinglan Sect.
- `mod_version` is `0.1.81`.
- `ModNetwork.PROTOCOL_VERSION` remains `7`; no packet fields, order, registration, encoding, or channel compatibility changed.

## Changes

- Added `SectContributionService` for Qinglan Sect status, join, shop, purchase, donation, debug contribution, steward spawning, and steward interaction.
- Added `/seeking_immortals sect status|join|shop|buy <entry>|donate spirit_grass|spawn_steward`.
- Added OP debug helper `/seeking_immortals debug add_contribution <amount>`.
- Added `QuestProgress.spendContribution(int)` so contribution is deducted only when the balance is sufficient.
- Qinglan Sect join requires the Seven Mysteries quest to be complete and the Yue anchor to have been reached.
- Contribution exchange rewards existing sect-secret formula items for Foundation Building Pill, Longevity Pill, and Return Yang True Water.

## Verification

- JSON parse check passed for `zh_cn.json` and `en_us.json` after localization changes.
- `./gradlew --no-daemon --max-workers=1 test` passed: `BUILD SUCCESSFUL in 17s`.
- `./gradlew --no-daemon --max-workers=1 build` passed: `BUILD SUCCESSFUL in 31s`.
- `aiPreflight` recorded `mod_version=0.1.81`; the network-package warning was reviewed and no protocol bump is required for this MVP because no packet wire shape changed.

## Backup

- Existing edited files were backed up to `.bak/20260703_191702/`.
