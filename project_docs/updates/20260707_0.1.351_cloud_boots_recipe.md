# 2026-07-07 0.1.351 Cloud Boots refinement recipe

- Observed current shared `mod_version=0.1.350`, verified the inherited tree with a full build, and avoided recent parallel hot files (`IllusionSpell`, Star Palace / Inverse FTB quest data, and `FtbQuestSnbtTest`).
- Added `data/seeking_immortals/recipes/refine_cloud_boots.json` as a vanilla shaped crafting approximation for the text-material `refine_cloud_boots` recipe: 3x Spirit Silk + 2x Spirit Iron -> Cloud-Treading Boots.
- Bumped `mod_version` to `0.1.351`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed.
- Build verification: `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL and `aiPreflight` recorded `mod_version=0.1.351`.
- Remaining risk: vanilla crafting does not enforce realm gates, forge grade, success chance, manual unlocks, failure loot, JEI category, or source-accurate material acquisition.
