# 2026-07-03 0.1.84 Parallel Worldpack Wave

## Summary

- `mod_version` is `0.1.84`.
- `ModNetwork.PROTOCOL_VERSION` remains `8`; this pass adds no packet fields, order changes, encoding changes, registrations, or incompatible channel behavior.
- The requested 30-agent plan was constrained by the active agent-thread limit, so the coordinator reused the available workers and landed a scoped, buildable MVP in the main workspace.

## Implemented

- Added a read-only worldpack data skeleton:
  - `WorldpackDataService`
  - shipped `worldpack/regions.json`
  - shipped `worldpack/secret_realms.json`
  - shipped `worldpack/daily_events.json`
- Extended `ShopService`:
  - item-currency purchase helper
  - per-entry `stock`
  - per-entry `refresh_ticks`
  - no partial deduction on failed currency or stock checks
  - existing Qinglan contribution shop behavior preserved
- Added a shipped low-tier item-currency shop template:
  - `market_herbal_stall.json`
- Expanded automated validation:
  - text-material JSON parse and index coherence
  - shipped worldpack schema checks
  - shop metadata checks
  - worldpack loader unit tests

## Verification

- `./gradlew --no-daemon --max-workers=1 test` passed after Gradle wrapper network access was allowed.
- Forced `./gradlew --no-daemon --max-workers=1 test --rerun-tasks` passed with Java recompilation.
- The first normal build after documentation sync hit the same-version fingerprint guard because `0.1.84` had already been recorded earlier in the wave.
- A single documented `-PaiSkipVersionBumpCheck=true` build was used to record the intended `0.1.84` fingerprint without bumping to `0.1.85`.
- Final normal `./gradlew --no-daemon --max-workers=1 build` then passed with `BUILD SUCCESSFUL in 29s`.

## Remaining Risk

- Worldpack data is loaded but not yet consumed by travel, secret-realm instancing, or daily event gameplay.
- Item-currency shop support is server-side core plus data template only; future merchant/NPC UI wiring remains pending.
- Large text-material registry imports are intentionally deferred to a later resource/art pass.
