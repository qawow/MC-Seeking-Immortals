# 2026-07-07 - 0.1.369 FTB Fallen Demon / Yin current-tree recheck

## Summary
- Rechecked the current shared tree after parallel version movement advanced `gradle.properties` to `0.1.369`.
- Preserved the `0.1.368` Fallen Demon / Yin FTB bridge without reverting concurrent work.
- The chapter still upgrades `1710`, `1715`, and `1716` to non-consuming checks for Yin Stone x8, Soul-Gathering Stone x1, and Soul Fragment x2.
- `FtbQuestSnbtTest` still validates 56 implemented item tasks across 215 packaged quest nodes.

## Verification
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL.
- `aiPreflight` recorded `mod_version=0.1.369`.

## Compatibility
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed by this recheck.
- The inherited network-package warning remains a release-audit item if pre-existing packet diffs changed compatibility.
