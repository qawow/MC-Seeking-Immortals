# 0.1.357 FTB Mulan/Tianlan/Demonic Item Tasks

## Summary

- Change class: resource/test/docs/version; no packet schema or runtime state changes
- Current verification version: `mod_version=0.1.360`
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, order, encoding, registrations, or channel behavior changed
- Backup paths: `.bak/20260707_0.1.356_ftb_mulan_demonic_item_tasks/`, `.bak/20260707_0.1.357_ftb_mulan_demonic_item_tasks_reconcile/`, `.bak/20260707_0.1.359_ftb_mulan_demonic_buddhist_compile_reconcile/`, `.bak/20260707_0.1.359_ftb_mulan_verified_docs/`, and `.bak/20260707_0.1.360_ftb_mulan_current_docs_recheck/`
- Text-material read: 315 files read as UTF-8; 207 JSON files parsed; 0 JSON parse errors

## FTB Item Tasks

Converted five Mulan/Tianlan/Demonic Six chapter checkmarks into non-consuming item checks:

- `1000000000001812`: `seeking_immortals:beast_core` x1 for Tianhu beast taming
- `1000000000001814`: `seeking_immortals:beast_core` x1 for Tianlan beast-soul training
- `1000000000001820`: `seeking_immortals:soul_fragment` x1 for Guiling Gate soul/corpse contact
- `1000000000001822`: `seeking_immortals:demonic_blood_coral` x1 for Tianmo blood rite
- `1000000000001825`: `seeking_immortals:beast_core` x1 for North Waste demonic-core treasure routing

All tasks use `consume_items: false` and `match_nbt: false`. `FtbQuestSnbtTest` now validates 48 implemented item tasks while preserving 9 packaged chapters and 215 quest nodes.

## Verification

Passed under the current shared tree:

- `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.quest.FtbQuestSnbtTest`
- `./gradlew --no-daemon --max-workers=1 build`

`aiPreflight` recorded `mod_version=0.1.360` on the latest recheck after the parallel Artifact Failure Loot slice. The inherited network-package warning is unrelated to this FTB item-task bridge because no packet fields, ordering, encode/decode format, registrations, or channel behavior changed.

Remaining risk: this is still an FTB inventory-check bridge only. It does not grant rewards, consume costs, lock branches, validate Mulan/Tianlan faction state, validate beast contracts, validate demonic karma, spawn bosses, sync `QuestProgress`, or prove live FTB client behavior.
