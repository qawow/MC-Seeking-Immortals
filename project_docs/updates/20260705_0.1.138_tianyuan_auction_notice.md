# 2026-07-05 0.1.138 Tianyuan auction-notice daily-event backfill

- Change class: worldpack data/test/docs/version metadata.
- Text-material source: `daily_random_events.json#auction_notice`.
- Implemented entry: `tianyuan_auction_notice` in shipped worldpack daily events.
- Runtime equivalent: current worldpack daily-event UI/command flow using the existing `tianyuan` region and existing effect tokens only.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains 9 because no packet fields, order, encoding, registrations, or channel compatibility changed.
- Backup path: `.bak/20260705_0.1.138_tianyuan_auction_notice/`.
- Verification: focused worldpack/resource tests passed with BUILD SUCCESSFUL in 21s, and final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 23s. `aiPreflight` recorded `mod_version=0.1.138`.
