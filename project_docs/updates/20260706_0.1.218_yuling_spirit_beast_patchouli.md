# 2026-07-06 0.1.218 Yuling spirit-beast Patchouli guide

- Change class: resource/docs/version metadata.
- Coordination: preserved the `0.1.217` Tianyuan City Patchouli slice already present in the shared worktree and the build-verified `0.1.216` Star Palace patrol supply Patchouli slice; scoped this pass to a separate guide entry to avoid Java, shop, packet, worldpack, registry, model, and texture hot files while another Codex is active.
- Text-material source: `sect_contribution_shop.json#spirit_beast_mountain`, with existing shipped equivalents in `yuling_pavilion_contribution_hall`.
- Implementation: added the zh_cn Patchouli entry `yuling_spirit_beast_contribution`, documenting the existing `spirit_beast_feed -> seeking_immortals:spirit_grass` and `beast_soul_essence -> seeking_immortals:beast_core` contribution-shop hooks.
- Prerequisite-library strategy: reused the mandatory Patchouli guide and existing `ShopService` contribution-shop data; no new custom beast shop, runtime loader, entity, UI, packet schema, registry entry, model, texture, recipe, loot table, or worldpack data was introduced.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains 9 because no packet fields, order, encoding, registrations, or channel behavior changed.
- Backup paths: `.bak/20260706_0.1.218_yuling_spirit_beast_patchouli/` and `.bak/20260706_0.1.218_yuling_build_recheck/`.
- Verification: changed Patchouli JSON parsed successfully; all 25 zh_cn Patchouli JSON files parsed successfully; resource JSON parse covered 410 files; all 276 text-material files were read and all 154 text-material JSON files parsed successfully. Focused `ShopServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 16s. A first final build hit the known Gradle `test-results` cleanup lock after `aiPreflight` passed; immediate rerun `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 22s and `aiPreflight` recorded `mod_version=0.1.218`.
- Follow-up: dedicated spirit-beast nurture pill, feed item, feeding rules, affinity and intimacy gains, contract behavior, beast entities, GeckoLib animation, source drops, and FTB Quests routing remain deferred.
