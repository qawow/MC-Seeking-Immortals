# 2026-07-07 - 0.1.364 Qingning Mirror refinement recipe

- Change class: resource/data/test/docs. Added one vanilla crafting approximation for an existing artifact refinement recipe; no Java runtime, packet, registry, GUI/menu, shop, worldpack, FTB, spell, or artifact-service code changed.
- Coordination: current `gradle.properties` moved through `0.1.362` and parallel `0.1.363` recheck docs while this slice was in progress, so Qingning Mirror was reconciled under current `mod_version=0.1.364` without reverting shared work.
- Text-material basis: `data/seeking_immortals/artifacts/refinement_recipes.json#refine_qingning_mirror`; current mappings `hundred_year_ice -> seeking_immortals:cold_jade` and `kunwu_copper -> seeking_immortals:kunwu_copper`; existing output carrier `seeking_immortals:qingning_mirror`.
- Implementation: added `data/seeking_immortals/recipes/refine_qingning_mirror.json`, using 2x Cold Jade and 1x Kunwu Copper to craft Qingning Mirror through the existing artifact-refinement recipe group.
- Test coverage: `ResourceJsonParseTest` asserts the recipe type, shape, inputs, and output.
- Version/protocol: current `mod_version` is `0.1.364`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed.
- Backup: `.bak/20260707_0.1.362_qingning_mirror_recipe/`.
- Verification: focused `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.resources.ResourceJsonParseTest` passed; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL and `aiPreflight` recorded `mod_version=0.1.364`.
