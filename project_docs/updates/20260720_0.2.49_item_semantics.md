# 0.2.49 专用物品与目录说明第二阶段

## 本阶段完成

- `detox_minor_pill`：炼气门槛下清除原版中毒/反胃及普通 poison family；不清凋零、魂伤或高阶魔毒，无可清状态时不消耗。
- `talisman_ink_bottle`：24 条制符配方统一追加一瓶符墨；生活技能门槛与完整材料预检先于任何扣料，同类材料先合并计数再统一提交。
- `spirit_sand_pouch`：单独右键只说明用途；玩家携带时，自由阵场成功激活后才扣一袋，并将持续时间由 90 秒延长至 150 秒。
- `yin_coffin_nail`：只选取十六格内最近的自有 Ghost 侍灵，疗伤、延寿、附加强化并切换为 GUARD；无合法目标时失败且不扣。
- `wind_feather_raft_blueprint`：保留化神使用门槛，要求成型飞舟船坞及风羽×4、灵蚕丝×4、昆吾铜×2、灵石碎片×8，成功后生成一张现有 `wind_feather_raft_ticket`。
- 飞行载具燃料从“生成前扣除”调整为“预检后生成、生成成功再提交”；提交异常会丢弃新船，避免失败吞燃料。
- 新增 `CatalogItemDescriptionService`：1190 条 bulk item 均生成双语用途定位和交互策略；869 条“目录载体”占位不再直接展示，专用消耗品继续叠加精确效果与境界 tooltip。

## 自动化验证

- 定向命令：`./gradlew test --tests 'com.xunxian.seekingimmortals.registry.ModBulkItemsTest' --tests 'com.xunxian.seekingimmortals.item.CatalogConsumableServiceTest' --tests 'com.xunxian.seekingimmortals.item.CatalogItemDescriptionServiceTest' --tests 'com.xunxian.seekingimmortals.craft.TalismanCraftServiceTest' --tests 'com.xunxian.seekingimmortals.structure.FormationItemServiceTest' --tests 'com.xunxian.seekingimmortals.catalog.FlightVehicleServiceTest' --no-daemon`
- 结果：11 项通过。
- 最终普通全量：`./gradlew build --no-daemon`
- 结果：最终普通构建 BUILD SUCCESSFUL（53s）；全量 690 项测试，0 failures、0 errors、0 skipped。

## 版本与协议

- `mod_version`: `0.2.48 -> 0.2.49`
- `ModNetwork.PROTOCOL_VERSION`: 保持 `25`
- 原因：没有修改任何网络包字段、编码顺序、类型、注册、方向或不兼容通道行为。

## 回滚

- `.bak/20260720_0.2.49_item_semantics/`

## 下一阶段

- 继续逐服务核对 access/currency/structure/formation/puppet 载体，避免“有说明但无真实消费端”。
- 把源语料中可恢复的逐物品 lore、配方、来源、用途转成独立双语描述，而不是仅依赖类别推导。
- 完成功法灵根/体质/阵营/声望/来源/环境/转修门槛，再进入 747 条术法 `damage_base/effect_key/tags/target/range` 结构化执行。
- 保留法宝 `powerScale`、储物持续授权、持久交付 outbox、多方块事务与客户端/专服/多人实机烟测。
