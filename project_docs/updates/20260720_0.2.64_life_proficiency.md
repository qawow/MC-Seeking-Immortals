# 0.2.64 生活技能熟练度纳入成功率

## 本批完成

### 熟练度成功率
- `LifeSkillService.successBonus` 现叠加等级加成与熟练度加成。
- 新增 `proficiency()` / `proficiencyBonus(int)`：熟练度 0–10000 线性映射到最多 `+10%` 成功率。
- 总加成上限 `BONUS_MAX + PROFICIENCY_BONUS_MAX`（20% + 10%），最终仍经 `adjustedSuccessRate` 钳制到 3%–95%。
- 制符/傀儡/炼丹/炼器等所有走 `successBonus` / `adjustedSuccessRate` 的路径自动受益。

### 测试
- `LifeSkillServiceTest` 覆盖熟练度线性、上限与源码契约。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体。

## 验证
- 定向 `LifeSkillServiceTest` 通过。
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 717 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.64`。

## 备份
- `.bak/20260720_053345_0.2.64_life_proficiency/`

## 后续
1. 多方块 form→consume→rollback（build materials / operational states 运行时事务）。
2. 实机客户端/专服/多人烟测。
