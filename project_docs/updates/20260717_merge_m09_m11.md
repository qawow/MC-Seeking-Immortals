# 2026-07-17 M09 / M11 主线集成

## 摘要

按顺序将 `task/m09-secret-realms` 与 `task/m11-quests-story` 合入 `main`，保留秘境副本主循环和任务主线全量运行时，并统一主线版本与网络兼容门禁。

## 合并复核

- M09：38 个文件，新增 6,296 行、删除 163 行；`git diff --check` 通过。
- M11：42 个文件，新增 13,785 行、删除 202 行；`git diff --check` 通过。
- 重叠文件为 `ai_handoff.md`、`step_progress.md`、`ModEvents.java`、`en_us.json`、`zh_cn.json`。
- `ModEvents` 的秘境 session/death 与任务 hook 位于不同代码段，自动合并；状态文档和双语 JSON 手工保留两边内容。

## 版本与协议

- `mod_version`：`0.1.507` → `0.1.508`，用于主线代码/资源集成。
- `ModNetwork.PROTOCOL_VERSION`：`21` → `22`。
- 原因：`SyncQuestTrackerPacket.MAX_LINES` 从 32 扩至 72。字段、顺序和类型虽未变化，但新服务端可发送旧 v21 客户端无法完整消费的 33–72 行载荷，属于不兼容 channel behavior。

## 验证

- 最终 `./gradlew build`：BUILD SUCCESSFUL in 1m3s；11 个任务中 9 个执行、2 个 up-to-date。
- `aiPreflight`：通过并记录 `mod_version=0.1.508`。
- 双语 lang JSON：解析通过。
- 合并后的 `git diff --check` 与暂存区 `git diff --cached --check`：通过。
- Java 编译仅保留 3 条既有 JEI deprecated API 警告。

## 已知风险

- M11 当前从 `ModEvents` 的 trial-mob 路径推进秘境任务，并未直接订阅 M09 的 `SecretRealmClearedEvent`；非 trial boss 的通关事件可能不会推进相应任务钩子。
- M09 的秘境死亡处理在 `LivingDeathEvent` 内直接执行返回传送，仍需真实服务端复活流程冒烟验证。
- M09 的过期 session 未按 `expiresAtTick` 清理，异常退出可能长期占用人数；当前 party limit 也是全服同秘境计数。
- M09 入口路径可能在资格校验前先执行区域旅行/扣费，拒绝进入时存在已迁移或重复扣费风险。
- M11 hook 推进未严格核对当前 stage，重复事件可能跨阶段推进；每日事件 hook 当前为空操作。
- M11 对话事件与动作执行器都可能处理 `turnin_quests`，单次交付存在重复推进风险。
