# 2026-07-04 0.1.105 Custom Fireball Spell Entity

- Change class: code/gameplay/client-rendering/docs.
- Backup path: `.bak/20260704_0.1.105_custom_fireball_spell/`.
- Version: `mod_version=0.1.105`.
- Protocol: `ModNetwork.PROTOCOL_VERSION=9`; no packet fields, order, encoding, registrations, or channel compatibility changed.
- Implemented `CultivationFireballEntity` as a mod-owned projectile for `FireballSpell`, replacing vanilla `SmallFireball`.
- Projectile behavior: custom movement, hit detection, indirect magic damage, short burn, small splash damage, flame/smoke particles, impact sound, NBT save data, and Forge entity spawn sync.
- Added `CultivationFireballRenderer` and registered it in `ClientEvents`; no new texture was added because it reuses the existing fire-element spirit-stone item model as a visible core.
- `compileJava` passed after escalating the known Gradle wrapper network restriction.
- Final build passed: `./gradlew --no-daemon --max-workers=1 build` -> BUILD SUCCESSFUL in 31s; aiPreflight recorded `mod_version=0.1.105`.
- Remaining follow-ups from the user's spell rule: convert `FireTalismanItem` away from vanilla `SmallFireball`; replace `ThunderStrikeSpell` real vanilla `LightningBolt` with custom thunder settlement/visuals; consider custom tribulation visuals later for the visual-only lightning in `TribulationService`.
