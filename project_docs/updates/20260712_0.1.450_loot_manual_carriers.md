# 0.1.450 Loot / manual residual carriers

## Class
Resources (+ bulk JSON, lang, models/textures, id-map)

## Audit before
Post-0.1.449 strict item-field scan across all text-material JSON (`item` / `output` / `result` / `product` / `reward_item`) found three real missing physical carriers:

- `ancient_artifact_fragment` (loot/auction material)
- `dayan_fragment` (boss/secret-realm material)
- `manual_ancient_puppet_method` (boss drop + puppet craft unlock manual)

Non-item false positives intentionally ignored: recipe ids (`craft_*` / `recipe_*`), shop ids, tribulation effect ids, and system result tokens (`fail` / `explosion`).

## Changes
- Appended 3 bulk carriers; total 722
- Generated models/textures/lang
- id-map self-mapped as implemented

## Post-audit
Text-material item-field hard-missing = 0  
Shipped shops/alchemy/recipes/loot_tables `seeking_immortals:` refs missing = 0

## Protocol
13 unchanged

## Build
`./gradlew.bat --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**  
`aiPreflight` recorded `mod_version=0.1.450`

## Backup
`.bak/20260712_230049_wave450_loot_manual_carriers/`
