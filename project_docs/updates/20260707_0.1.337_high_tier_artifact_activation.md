# 2026-07-07 0.1.337 High-tier artifact active behavior

- Scope: low-conflict artifact behavior follow-up that avoids the parallel Demonic/Ghost spell files, FTB resources, registries, network packets, shops, worldpack files, recipes, loot tables, and GUI/menu code.
- Source read: re-read all 315 files under `文本材料` as UTF-8 and parsed all 207 JSON files successfully; direct basis was `artifacts_catalog.json` high-tier entries for `xuanhuang_mirror`, `void_refining_bell`, `talisman_treasure_demon_seal`, and the deferred `natal_sword_embryo`.
- Changed behavior: `soul_destroy` artifacts now activate as a soul-shock area burst, `space_control` artifacts now create a void slow/weakness zone, and `talisman_treasure_demon_seal` now performs a monster-only seal burst through the existing talisman-use path.
- Version/protocol: current `mod_version` is `0.1.337`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed.
- Backups: `.bak/20260707_0.1.336_high_tier_artifact_activation/` and `.bak/20260707_0.1.337_high_tier_artifact_activation_reconcile/`.
- Verification: focused `ArtifactActivationServiceTest` passed; full current-tree `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 28s and `aiPreflight` recorded `mod_version=0.1.337`. The inherited network-package warning remains present and unrelated.
- Remaining work: richer active behavior for more artifact types, natal binding, source-accurate acquisition/loot/shop placement, refinement forge UI/custom serializer, JEI display, dedicated art, and live multiplayer smoke checks.
