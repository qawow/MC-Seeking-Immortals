# 2026-07-04 0.1.96 Phase 10 Six Sects (Superseded)

## Summary

- Completed the scoped Phase 10 Yue six-sect line on top of the actual current workspace version.
- The original plan label expected `0.1.91 -> 0.1.92`, but the repository had already advanced to `0.1.95`; the final shipped version is therefore `0.1.96`.
- Superseded protocol note: `ModNetwork.PROTOCOL_VERSION=9` is not current truth for the review-fix source tree.

## Implemented

- Generalized sect runtime beyond Qinglan while preserving legacy Qinglan command/save compatibility.
- Added GUI-driven six-sect selection, server-authoritative application, knocking exam, dialogue choices, mission accept/turn-in, shop buys, and stage advancement.
- `SectStewardEntity` persists sect id and NPC type for command-spawned and structure-spawned recruiters.
- Added six-sect dialogue, mission, contribution-shop, worldgen structure, template pool, structure set, biome tag, and structure NBT resources.
- Superseded implementation note: market/worldpack packet, client snapshot, and lightweight screen classes were later backed up and removed from current source.

## Verification

- Final build command: `./gradlew --no-daemon --max-workers=1 build`
- Result: `BUILD SUCCESSFUL in 34s`
- Build notes: compile/test/check/build completed; JEI deprecation warnings are inherited and non-fatal.

## Backup

- Rollback backup path: `.bak/20260704_0.1.92_phase10_six_sects/`

## Remaining Risk

- Manual in-game checks remain for natural generation and interactions: `/locate structure seeking_immortals:<sect>_outpost`, recruiter NBT correctness, six-sect one-time application, mission reward contribution, per-sect shop separation, and generated outpost placement in new chunks.
- Not included by design: sect wars, true-disciple inheritance, South Sea/Tiannan large-map arcs, custom NPC models, and richer structure art.

## Final Docs-Only Verification Note

- After the successful normal 0.1.96 build, project docs were synced.
- A second normal build was blocked by `scripts/preflight.sh` because tracked docs changed after the recorded 0.1.96 fingerprint.
- This is a docs-only post-build sync, so `mod_version` was not bumped again; the final verification uses `-PaiSkipVersionBumpCheck=true` and this note records the manual override reason.