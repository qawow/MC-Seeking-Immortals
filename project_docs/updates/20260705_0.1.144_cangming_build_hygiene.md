# 2026-07-05 0.1.144 Cangming build-hygiene reconciliation

## Scope

- Preserves the 0.1.142 Yanyue implemented pill-stock data.
- Preserves the 0.1.143 Cangming `recipe_yanghun` contribution-shop backfill.
- Adds a small Gradle test-compile classpath fallback so the standard build can compile the active test tree against main outputs.

## Changes

- `gradle.properties` advanced to `mod_version=0.1.144`.
- `build.gradle` now adds `sourceSets.main.output` directly to `compileTestJava.classpath`.
- No packet fields, packet order, packet encoding, registrations, or channel behavior changed.

## Verification

- Changed JSON parsed successfully with PowerShell `ConvertFrom-Json`.
- Final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 26s.
- `aiPreflight` recorded `mod_version=0.1.144`.
- `ModNetwork.PROTOCOL_VERSION` remains `9`.

## Backup

- `.bak/20260705_0.1.143_cangming_yanghun_formula/`
