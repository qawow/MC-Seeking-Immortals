# 0.1.492 authority polish wave

- Change class: code, localization, tests, project docs.
- Backup: `.bak/20260715_120000_wave492/`.
- Version: `mod_version=0.1.492`.
- Protocol: remains `18` (no new packet fields; dual-cast reuses ReleaseTechnique server path).

## Six targets

1. **Auction require-appraised gate**
   - `AuctionSoftService.AppraisalGateMode` HIGH_TIER/ALL/OFF
   - HIGH_TIER default: min/max equiv ≥ 80 requires ARTIFACT_REFINING≥1 or appraisal tool
   - Settled rewards marked appraised
2. **Multi-cast honest dual-cast**
   - Renamed display to 分神双放
   - After primary cast, fires up to 1–2 other slotted arts (half cost, scaled CD)
3. **Divine sense real passive**
   - `DivineSenseExpansionPassive` duration pulse + range multiplier
   - DetectionSpell uses range multiplier; tick recovers divine sense
4. **SectHall/MarketHall tab parity**
   - Sect: dialogue/mission/shop/progress tabs + candidates
   - Market: multi-shop strip + entry pagination
5. **Dialogue trees cover all catalog chains**
   - `TextQuestDialogueTreeService` auto-builds trees for all ExtendedCatalog chains (≥60)
6. **soft_only closeout**
   - Formation unknown → CATALOG_GENERIC deploy
   - Refine fuzzy recipe match
   - QuestHook/Conflict/Chronicle/Trade residual ids map to playable chains

## Remaining risk
- Human live-smoke re-sign, studio art, true multi-biome leyline structures, dedicated beast entities.
