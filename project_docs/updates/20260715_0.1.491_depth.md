# 0.1.491 depth wave

- Change class: code, resources, localization, tests, project docs.
- Backup: `.bak/20260714_231500_wave491_depth/`.
- Version: `mod_version=0.1.491`.
- Protocol: **17 → 18** (`SyncAuctionLadderPacket`, `SkillTreeActionPacket`).

## Six targets

1. **Auction live ladder + pagination**
   - `SyncAuctionLadderPacket` + `ClientAuctionLadderData`
   - `AuctionSoftService.syncLadder/openHall(page)`
   - `AuctionHallScreen` page controls + live now/next/leader
2. **True escort + kill filters**
   - `EscortMissionService` (SummonedServitor FOLLOW)
   - Typed kill targets (`zombie/skeleton/spider/monster`)
3. **spawn_tables runtime + cluster weight**
   - `BeastSpawnTableService` consumes `text_material/spawn_tables.json`
   - Daily beast events prefer table-driven servitor proxies
4. **Leyline surface marker**
   - Block/item + sparse overworld random_patch feature (hash aura remains authority)
5. **Interactive life skill tree UI**
   - `LifeSkillTreeScreen` + `SkillTreeActionPacket`
   - CultivationStats 4th button
6. **Dialogue node tables (10 chains)**
   - `TextQuestDialogueTreeService` demo trees
   - `TextQuestDialogueService.linesFor` prefers node tables

## Remaining risk
- Studio art for marker block, full dialogue data JSON load, human live-smoke, denser multiplayer auction hall polish.
