# 0.2.187 v649 生成资源刷新

## 更新内容

- 以当前 Git 已跟踪的 v649 原著术法输入重新生成作者法术效果资源。
- 同步更新统一作者视觉目录，新增 v648/v649 技术条目和逐条视觉程序层。
- 更新目录契约测试中的非推断作者术法数量基线。
- 生成器继续只读取 Git 已跟踪的 `novel_curated_techniques_v*.json`，避免并行 scratch 文件改变构建输入。

## 数据与验证

- `authored_spell_effects.json`：2,253 个术法，747 条基础语料，1,506 条原著增量。
- `authored_visual_catalog.json`：5,688 个 profile、13,633 个 semantic layer、9,851 段视觉引文。
- 两个生成器 `--check`、`AuthoredVisualCatalogTest` 通过。
- 普通 `./gradlew build --no-daemon --max-workers=1` 成功，1,161 项测试全部通过，failure/error/skipped 均为 0。

## 版本与协议

- `mod_version=0.2.187`。
- 未改变网络包字段、顺序、类型、注册或频道行为，`ModNetwork.PROTOCOL_VERSION=30` 保持不变。
- 备份目录：`.bak/20260726_0.2.187_v649_refresh/`。
