# 2026-07-07 0.1.335 Demonic/Ghost spell behavior reconcile

- Scope: code-only follow-up to the existing `DemonicGhostSpell` Core Formation demonic/ghost spell slice, preserving the build-verified `0.1.334` current-tree version sync and all parallel item/FTB/artifact work.
- Changed behavior: `blood_shadow_escape` now uses its own failure feedback key when no safe landing point exists. `sky_supporting_demonic_skill` now grants a short strength boost, deflects nearby hostile projectiles, and reports the deflected count expected by zh_cn/en_us localization.
- Version/protocol: current `mod_version` is `0.1.335`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed.
- Backups: `.bak/20260707_0.1.335_demonic_ghost_spell_behavior_reconcile/` contains current docs/version backups and the pre-reconcile spell source copied from the earlier `0.1.333` spell backup.
- Verification: first full build attempt passed `aiPreflight` but failed on a transient Windows lock under `build/test-results/test/binary`; the rerun `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 24s and `aiPreflight` recorded `mod_version=0.1.335`.
- Remaining work: strict prerequisite/method/source gates, dedicated icons and particle textures, source-accurate manual/sect acquisition, PvP balance, and live client visual smoke tests.
