# 20260608 版本 0.1.14：心魔机制裁剪与项目流程规则更新

## 版本

- `mod_version` 从 `0.1.13` 提升到 `0.1.14`。
- 构建产物目标为 `seeking_immortals-0.1.14.jar`。

## 机制调整

- 确认当前代码中没有实现“心魔击杀玩家后削弱自我数值”的逻辑。
- 更新心魔触发提示文本：心魔后续仍可做内心世界表现，但明确不加入“心魔击杀玩家后削弱自我数值”的机制。
- 更新早期 `0.1.7` 负面状态更新记录，将“黑化玩家实体、120% 数值复制、击杀心魔解除”等旧规划表述改为更保守的心魔幻象/感知错乱方向，并明确不再规划削弱自我数值机制。

## 文档流程更新

- 明确后续每次完成更新都需要同步检查并维护：
  - `project_docs/items.md`
  - `project_docs/pending_requests.md`
- 明确后续更新日志命名规则改为“年月日+版本号”，例如：`20260608_0.1.14.md`。
- 本次已同步更新 `items.md` 与 `pending_requests.md`。

## 验证

- JSON 语法校验通过。
- 已执行 `./gradlew build`。
