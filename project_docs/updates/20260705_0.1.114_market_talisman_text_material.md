# 2026-07-05 0.1.114 market talisman text-material backfill

- Change class: shipped data resources, test, version, and project documentation.
- Scope: add a small implemented low-tier talisman slice from `文本材料/data/talisman_catalog.json` and `merchant_shops.json`.
- Canonical map: `fire_burst_talisman` maps to `seeking_immortals:fire_talisman`; `body_guard_talisman` maps to `seeking_immortals:armor_talisman`. Both are `implemented_partial` because existing runtime items cover the low-tier roles but not every text-material rule such as area burst or exact shield value.
- Market data: `market_herbal_stall` now sells the mapped Fire and Armor Talismans using existing `seeking_immortals:metal_spirit_stone` item currency.
- Items/resources: no new item registrations, models, textures, recipes, loot tables, tags, GUI assets, or runtime loaders were added.
- Version/protocol: `mod_version=0.1.114`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged because no packet fields, order, encoding, registrations, or channel behavior changed.
- Backup: `.bak/20260705_0.1.114_market_talisman/`.
- Verification: focused `ShopServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 27s.
- Follow-up: mid/high talismans, talisman paper, inks, talisman crafting, exact area/defense formulas, and non-market talisman shops remain deferred until matching systems exist.
