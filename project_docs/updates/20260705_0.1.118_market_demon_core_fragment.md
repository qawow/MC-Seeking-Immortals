# 2026-07-05 0.1.118 market beast-core fragment text-material backfill

- Change class: shipped data resources, test, version, and project documentation.
- Scope: add a small implemented material slice from `文本材料/data/merchant_shops.json` into the existing `market_herbal_stall`.
- Canonical map: `demon_core_fragment` now maps to `seeking_immortals:beast_core` as `implemented_partial`.
- Market data: `market_herbal_stall` now sells the mapped beast-core equivalent using existing `seeking_immortals:metal_spirit_stone` item currency.
- Items/resources: no new item registrations, models, textures, recipes, loot tables, tags, GUI assets, or runtime loaders were added.
- Version/protocol: `mod_version=0.1.118`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged because no packet fields, order, encoding, registrations, or channel behavior changed.
- Backup: `.bak/20260705_0.1.118_market_demon_core_fragment/`.
- Verification: focused `ShopServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 26s; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 35s.
- Follow-up: exact demon-core fragment/core tiering, beast source drops, and alchemy/array-specific consumption remain deferred.
