# 2026-07-04 0.1.108 Spirit-stone canonical resource refs

- Change class: data resources plus documentation.
- Backup path: `.bak/20260704_0.1.108_spirit_stone_canonical_refs/`.
- Version/protocol: `mod_version=0.1.108`; `ModNetwork.PROTOCOL_VERSION=9`.
- Text-material review input: subagents B/C/D/E/F all recommended canonical id mapping before large item/shop/system imports; this pass addresses the lowest-risk legacy resource references first.
- Updated `spirit_ore` block loot and the core qi/cultivation/breakthrough/spirit-charm recipes from legacy `seeking_immortals:spirit_stone` to registered `seeking_immortals:metal_spirit_stone`.
- No new items, blocks, models, textures, tags, network packets, packet fields, packet order, packet encoding, or packet registrations were added.
- Focused validation passed: `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.resources.JsonSanityTest` ended `BUILD SUCCESSFUL in 20s`.
- Final build passed: `.\gradlew.bat --no-daemon --max-workers=1 build` ended `BUILD SUCCESSFUL in 24s`; `aiPreflight` recorded `mod_version=0.1.108`.
- The inherited network-package preflight warning was reviewed. This pass changed resource JSON and docs only, not packet fields/order/encoding/registrations/channel compatibility, so `ModNetwork.PROTOCOL_VERSION` remains `9`.
