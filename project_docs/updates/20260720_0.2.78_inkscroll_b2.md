# 0.2.78 云笈墨卷 B2 分层与组件（前端重构批次 2/7）

## 本批完成

### client/ui 分层
- `NumberFmt`：统一万/亿/兆数字语系（`cjk/cjkPair/percent/two/clamp01`）。修复实缺陷：CultivationStatsScreen 旧用 B/M 西式单位、HUD 用 万/亿/兆，两处并存不一致 → 全部收敛到 `NumberFmt.cjk`。
- `InkLayout`：响应式布局引擎（`panel(w,h,Spec)->Panel{outer,header,body,listPane,detailPane,footer,stacked,wide}`）+ `Spec.JOURNAL/LORE/CONTAINER` 预设 + `clampScroll` + `tabCell`。B3 起屏幕的 `calculateLayout` 逐步改为薄包装。
- `client/ui/widget/InkRows`：共享 `keyValue` 行、`section` 题签、`syncWait` 等待占位。
- `client/ui/widget/InkPaging`：统一分页数学；`MarketHallScreen` 的 `clampPage/maxPage/pageStart/pageEnd` 改为一行委托（公共形状保留，MarketAuctionPagingTest 原样通过）。

### 测试
- 新增 `InkUiCoreTest`：cjk 单位边界（无 B/M 泄漏）、面板适配/分带不重叠、堆叠断点、滚动与分页与旧语义一致。

## 权威与兼容
- 纯客户端，无网络字段变化，protocol 25。ScreenLayoutTest/分页测试契约全数保留。

## 验证
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 747 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.78`。

## 备份
- `.bak/20260720_133000_0.2.78_inkscroll_b2/`

## 后续
B3a 修炼族屏迁移（CultivationStats/LifeSkillTree/TechniqueEdit/MethodTree/Meditation）。
