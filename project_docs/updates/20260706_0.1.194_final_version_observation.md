# 2026-07-06 0.1.194 Final Version Observation After Parallel Work

- Change class: docs-only current-state observation; no additional code/resource/data content is claimed here.
- Coordination: the shared workspace advanced to `mod_version=0.1.194` after the 0.1.193 verification notes; this version was preserved rather than reverted.
- Current observed data: `daily_events.json` contains both `tianlan_mulan_scout_clash` under `tianlan` and exact `auction_notice` under `dajin`, and parses with 112 daily events.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; this docs-only observation changed no packet fields, order, encoding, registrations, or channel behavior.
- Backup path: `.bak/20260706_0.1.194_final_docs_sync/`.
- Verification: final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 25s and `aiPreflight` recorded `mod_version=0.1.194`.
