# 20260609 版本 0.1.24 更新记录

## 版本

- 本次作为 0.1.24 依赖接入迭代记录。
- `gradle.properties` 中 `mod_version` 已更新为 `0.1.24`。

## 新增/调整

- 阅读并确认 旧第三方 UI 文档：旧第三方 UI 是 旧第三方 UI 的现代版本重写，提供 UI、Shader、模型渲染、数据同步/持久化、游戏内编辑器等基础设施。
- 通过 Modrinth Maven 接入 Forge 1.20.1 可用的 旧第三方 UI 发行包：`已移除的旧第三方 UI 依赖`。
- 在 `mods.toml` 中声明 `旧第三方 UI库` 为 `mandatory=true`、`side=BOTH` 的必需依赖，版本范围 `[1.0.50,)`。
- `README.md` 已补充当前必需依赖：Curios、旧第三方 UI；Patchouli、JEI 仍为可选依赖。

## 文档

- 已同步更新 `project_docs/items.md`：说明 0.1.24 未新增玩家物品，仅接入基础库。
- 已同步更新 `project_docs/pending_requests.md`：记录后续将技能栏、修仙数值页、打坐/闭关 HUD 等迁移到 旧第三方 UI UI 的待办。

## 验证

- `./gradlew build` 已通过；ForgeGradle 成功解析 `已移除的旧第三方 UI 依赖` 并完成 `compileJava`、`processResources`、`reobfJar`、`build`。
- Gradle 输出存在 Gradle 9.0 兼容性弃用警告，属于既有构建脚本/插件层面的提示，本次未影响 1.20.1 Forge 47.x 构建。
