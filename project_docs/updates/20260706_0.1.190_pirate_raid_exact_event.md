# 2026-07-06 0.1.190 Pirate Raid Exact Daily-Event Id

- Change class: worldpack data/test/docs/version metadata.
- Source: `文本材料/data/daily_random_events.json#pirate_raid`.
- Implementation: added exact shipped daily-event id `pirate_raid` under existing region `chaotic_sea`, reusing the current daily-event schema, source weight 11, 18000 ticks, and `trade_risk_up`. The older `chaotic_sea_pirate_raid` event is preserved as a compatibility alias.
- Coordination: preserved the concurrent `0.1.189` Fengyuan `treasure_fair_rumor` exact-id data/test changes already present in the workspace.
- Prerequisite reuse: this slice uses the existing worldpack data service and Gradle/JUnit validation path; no new runtime library integration is needed for a data hook.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, order, encoding, registrations, or channel behavior changed.
- Backup path: `.bak/20260706_0.1.190_pirate_raid_exact_event/`.
- Verification: changed worldpack JSON parsed successfully with 109 daily events and 0 missing region refs; all 154 text-material JSON files parsed successfully. Focused `WorldpackDataServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 14s after wrapper network/cache approval. The first full build hit a transient ForgeGradle `reobfJar` missing `mappings.tsrg` failure after `aiPreflight` passed; immediate rerun `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 20s and `aiPreflight` recorded `mod_version=0.1.190`. A later docs-verification rerun first hit a transient `build/test-results/test/binary` cleanup race while result files were still being written, then immediate rerun passed with BUILD SUCCESSFUL in 21s and again recorded `mod_version=0.1.190`.
