# 0.1.482 Wave482 FTB consume authority

## Change class
Data + tests + docs. Protocol unchanged (16).

## Scope
- Packaged FTB item tasks: `consume_items: false` → `true` (66 tasks across 9 chapters).
- `data.snbt` `default_consume_items: true`.
- Mainline item quests gain first `item` rewards (spirit_stone_shard).
- `FtbQuestSnbtTest` asserts consume authority + mainline rewards.
- Descriptions updated from “不消耗” to “并消耗”.

## Files
- EDIT all `src/main/resources/seeking_immortals/ftbquests/quests/chapters/*.snbt`
- EDIT `data.snbt`
- EDIT `FtbQuestSnbtTest.java`
- EDIT `FtbRewardBridgeService.java` (docs note)
- EDIT gradle.properties 0.1.482
- Docs: ai_handoff, step_progress, pending_requests, open_decisions, checklist

## Backup
`.bak/20260714_022759_ftb_consume_authority/`

## Note
Existing worlds already seeded under `config/ftbquests/quests/` keep prior files (non-overwrite seeder). Fresh installs / deleted config packs get consuming defaults.

## Build
`./gradlew --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.482`.
Protocol remains 16 (no packet field/order change).
