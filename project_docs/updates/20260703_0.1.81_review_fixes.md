# 2026-07-03 0.1.81 Review Fixes

## Change Class

- Code + resources + docs.
- `mod_version` is bumped from `0.1.80` to `0.1.81`.
- `ModNetwork.PROTOCOL_VERSION` remains `7`.

## Implemented

- Alchemy furnace installation now treats the same lid, dan fire, or formula as an already-installed no-op.
- Replacing a different installed alchemy lid, fire, or formula returns the previous installed item to the player inventory, or drops it if inventory insertion fails.
- `QuestProgress` now persists nullable `SecretRoomMarker` and `YuePortalMarker` coordinate compounds with `X/Y/Z` fields.
- Seven Mysteries marker placement helpers store and sync the player's marker coordinates.
- Secret-room and Yue-portal block interactions now require both the stored coordinate and the expected marker structure.
- `SetMeditatingPacket(true)` now validates meditation starts server-side: valid cushion seat, hunger above the configured minimum unless creative, and no nearby hostile mob.
- Spirit-stone passive matching now lives on `SpiritStoneItem` and is used by both server tick logic and cultivation sync preview logic.
- Technique-grade matching now uses proper UTF-8 Chinese source tokens and keeps existing English id fallbacks.
- Added `block.seeking_immortals.earth_wall` to zh_cn and en_us localization.
- Normalized `assets/seeking_immortals/models/item/mystic_vial.json` to UTF-8 without BOM.

## Verification

- Resource sanity check passed: strict JSON parse for shipped mod resources, `earth_wall` zh_cn/en_us key check, `mystic_vial.json` BOM check, and `mystic_vial` model texture-reference check all passed.
- Normal build passed: `./gradlew --no-daemon --max-workers=1 build` ended with `BUILD SUCCESSFUL in 28s`.
- Forced validation passed: `./gradlew --no-daemon --max-workers=1 build --rerun-tasks` ended with `BUILD SUCCESSFUL in 42s`; Java compile, test, reobf, and build tasks executed.
- Preflight warning: expected network-package warning was emitted for the dirty/current network package. Protocol remains `7` because this pass did not change packet fields, field order, field types, encode/decode order, registration order, or channel compatibility.

## Notes

- Packet wire shape is unchanged: no packet fields, order, types, registration order, encode/decode format, or channel compatibility changed.
- Existing quest markers from older saves lack stored coordinates and may need to be re-placed with the OP marker command.

## Backup

- `.bak/20260703_0.1.81_review_fixes/`
