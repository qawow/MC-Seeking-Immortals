# 2026-07-03 0.1.84 Worker D design catalogs

- Added a lightweight design-catalog summary layer for the setting-pack `data` directory covering techniques/refinement/talisman source files.
- `SettingCatalogSummaryService` parses `techniques/index.json`, attempts the declared technique files plus refinement/talisman catalog files, and records present/valid/invalid status with short parse errors.
- The setting-pack reader is intentionally tolerant and non-runtime: malformed JSON is reported for planning and is not loaded into gameplay, registries, packets, JEI, Patchouli, or data packs.
- `mod_version` is bumped to `0.1.84`; `ModNetwork.PROTOCOL_VERSION` remains `8` because no packet format, registration, or channel behavior changed.
- Backup path: `.bak/20260703_0.1.84_worker_d_design_catalogs/`.
- Focused verification passed: `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.design.SettingCatalogSummaryServiceTest` ended `BUILD SUCCESSFUL in 21s`.
- Final build verification passed: `./gradlew --no-daemon --max-workers=1 build` ended `BUILD SUCCESSFUL in 31s`; the existing network-package preflight warning was reviewed and no protocol bump is needed for this non-network slice.
