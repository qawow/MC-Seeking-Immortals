# 2026-07-07 0.1.340 Artifact family activation

- Coordination: observed parallel FTB backup/version movement through `0.1.338`/`0.1.339` and avoided FTB quest files, quest tests, packets, GUI, registries, and artifact/refinement JSON data.
- Source read: full text-material scan read all 315 files under `文本材料` as UTF-8 and parsed all 207 JSON files successfully; direct basis was artifact catalog entries for `ruler`, `mirror`, `sound`, `swarm`, and puppet/beast families.
- Implementation: `ArtifactActivationService` now supports first active behavior for ruler, mirror, sound, and swarm artifact types, maps puppet/beast variants to the existing beast-control suppression behavior, and infers realm gates from tier/game tier when supported high-tier artifact entries omit `realm_min`.
- Version/protocol: this slice was verified at `mod_version=0.1.340`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed. Parallel work later advanced shared `gradle.properties` to `0.1.341`; preserve that newer version.
- Backups: `.bak/20260707_0.1.340_artifact_family_activation/`.
- Verification: focused `ArtifactActivationServiceTest` passed; full current-tree `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 19s and `aiPreflight` recorded `mod_version=0.1.340`. The inherited network-package warning remains present and unrelated.
- Remaining risk: puppet/beast artifacts still do not summon real entities or persist contracts; ruler/mirror/sound/swarm behavior is a first generic bridge rather than source-accurate per-artifact logic; dedicated art, acquisition placement, JEI/refinement UI, and live multiplayer smoke checks remain deferred.
