# 2026-07-04 0.1.92 Full Texture Redraw Prep Blocked

- Requested scope: redraw and overwrite all 323 PNG files under `src/main/resources/assets/seeking_immortals/textures/` using `gpt-image-2`, while preserving paths and dimensions.
- Completed safe prep: original textures were compressed to `backups/20260704_0.1.92_full_texture_redraw/original_textures_before_redraw.zip`.
- Completed prompt prep: generated `backups/20260704_0.1.92_full_texture_redraw/texture_redraw_manifest.json` and `tmp/imagegen/20260704_0.1.92_full_texture_redraw/texture_redraw_prompts.jsonl`.
- Environment prep: installed the OpenAI Python SDK into `tmp/imagegen/python_pkgs` and verified the image CLI can import it through `PYTHONPATH`.
- Blocker: `gpt-image-2` CLI generation exits with `Error: OPENAI_API_KEY is not set. Export it before running.`
- No source PNGs, model JSONs, Java code, packet fields, or `gradle.properties` were changed in this blocked prep pass.
- Version/protocol decision: no resource overwrite happened, so `mod_version` remains the existing `0.1.91`; `ModNetwork.PROTOCOL_VERSION` remains `8`.
- Resume command shape after setting `OPENAI_API_KEY`: run `generate-batch` with `PYTHONPATH=tmp/imagegen/python_pkgs`, input `tmp/imagegen/20260704_0.1.92_full_texture_redraw/texture_redraw_prompts.jsonl`, and output directory `output/imagegen/20260704_0.1.92_full_texture_redraw/raw`, then post-process/crop into the original texture paths.
