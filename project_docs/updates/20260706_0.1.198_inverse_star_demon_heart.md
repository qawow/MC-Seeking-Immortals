# 2026-07-06 0.1.198 Inverse Star Demon-Heart Market Backfill

## Summary

- Backfilled text-material `merchant_shops.json#inverse_star_black_market` entries `demon_heart_pill` and `recipe_demon_heart`.
- Reused existing `ShopService` item-currency market flow, registered `seeking_immortals:calming_pill_low`, and registered `seeking_immortals:alchemy_formula_calming_pill_jade`.
- Avoided new runtime loaders, item/block registries, models, textures, recipes, loot tables, GUI classes, production gameplay logic, and packet changes.

## Data

- `demon_heart_pill -> seeking_immortals:calming_pill_low`, cost 180 low-grade metal spirit stones, stock 3.
- `recipe_demon_heart -> seeking_immortals:alchemy_formula_calming_pill_jade`, cost 350 low-grade metal spirit stones, default black-market stock 4.
- `text_material_id_map.json` now records both partial mappings and the expanded Inverse Star black-market coverage.

## Verification

- Full text-material scan read all 276 files and parsed all 154 JSON files successfully.
- Changed shop/reference JSON parsed successfully.
- Focused tests passed: `ShopServiceTest`, `JsonSanityTest`, `ResourceJsonParseTest` with BUILD SUCCESSFUL in 18s.
- Initial full build under `mod_version=0.1.197` was blocked by aiPreflight because that version was already occupied by previous shippable fingerprints.
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 24s and aiPreflight recorded `mod_version=0.1.198`.

## Compatibility

- `ModNetwork.PROTOCOL_VERSION` remains `9`.
- The inherited network-package warning was reviewed; this slice did not change packet fields, field order, encoding/decoding, registrations, or channel behavior.

## Backup

- Existing edited files were backed up under `.bak/20260706_0.1.197_inverse_star_demon_heart/`.

## Remaining Work

- Dedicated Demon Heart Pill behavior and exact formula identity.
- Real demon-qi corruption resistance and UI/hazard feedback.
- Inverse Star reputation/access gates, contraband/cipher/smuggler goods, and source-accurate black-market risk/reward semantics.
