# 2026-07-06 0.1.176 worldpack event/UI aggregate

- Change class: worldpack daily-event data, client UI localization, focused tests, docs, and version reconciliation.
- Preserved: 0.1.174 `diyuan_pressure_wave` under `spirit_fengyuan`.
- Parallel additions covered: `great_jin_auction_week` under `great_jin_central`, plus localized worldpack effect descriptions in `WorldpackScreen`, zh_cn/en_us lang keys, and `ScreenLayoutTest` coverage.
- Version reconciliation: an attempted final build under `0.1.175` was blocked by aiPreflight because that version fingerprint was already occupied, so the aggregate is finalized as `mod_version=0.1.176`.
- Backup paths: `.bak/20260706_0.1.174_diyuan_pressure_event/`, `.bak/20260706_0.1.175_great_jin_auction_week/`, `.bak/20260706_0.1.175_worldpack_effect_descriptions/`, and `.bak/20260706_0.1.176_version_reconcile/`.
- Protocol: `ModNetwork.PROTOCOL_VERSION=9` unchanged because no packet fields/order/encoding/registrations changed.
- Verification: focused `WorldpackDataServiceTest`, `JsonSanityTest`, `ResourceJsonParseTest`, and `ScreenLayoutTest` passed with BUILD SUCCESSFUL in 26s after wrapper network/cache approval; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 30s and aiPreflight recorded `mod_version=0.1.176`.
- Continuation recheck: the same focused test set passed with BUILD SUCCESSFUL in 17s after Gradle wrapper network/cache approval; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 26s and aiPreflight again recorded `mod_version=0.1.176`.
- Remaining work: real Wanbao auction schedules/lots/bidding UI, Great Jin clan reputation and permits, Diyuan pressure/no-fly hazards, event icons, reward tables, and richer daily-effect behavior beyond localized text.
