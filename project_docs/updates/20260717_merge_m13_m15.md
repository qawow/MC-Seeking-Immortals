# 2026-07-17 M13 / M15 主线集成

## 摘要

按顺序将 `task/m13-dimensions-ascension` 与 `task/m15-artifacts` 合入 `main`，保留维度/飞升主循环与法宝/器灵主循环，并统一主线版本门禁。

## 合并复核

- M13：39 个文件，新增 5,957 行、删除 136 行；`git diff --check` 通过。
- M15：20 个文件，新增 2,325 行、删除 25 行；`git diff --check` 通过。
- 重叠文件为 `ai_handoff.md`、`step_progress.md`、`ModEvents.java`、`en_us.json`、`zh_cn.json`。
- `ModEvents` 的换维飞行策略与法宝协同位于不同代码段，自动合并；状态文档和双语 JSON 手工保留两边内容。

## 版本与协议

- `mod_version`：`0.1.508` → `0.1.509`，用于主线代码/资源/构建脚本集成。
- `ModNetwork.PROTOCOL_VERSION`：保持 `22`；两分支均未修改 packet 字段、顺序、类型或 channel behavior。

## 验证

- 最终 `./gradlew build`：BUILD SUCCESSFUL in 1m1s；11 个任务中 9 个执行、2 个 up-to-date。
- `aiPreflight`：通过并记录 `mod_version=0.1.509`。
- 双语 lang JSON：解析通过。
- 合并后的 `git diff --check` 与暂存区 `git diff --cached --check`：通过。
- Java 编译仅保留 3 条既有 JEI deprecated API 警告。

## 已知风险

- M13 飞升备份包含全部物品，但唯一/白名单物仍保留在当前库存；恢复备份会复制这些物品。
- M13 飞升门在新校验失败且没有 pending 时回落旧入口流程，可能绕过境界/渡劫门槛。
- M13 路线旅行未核验当前维度与 route.from，也未执行已加载的费用/失败率/allowed 约束。
- M13 飞升状态、备份和旅行冷却存于普通 player persistent data，死亡 clone 未复制这些键。
- M15 本人重复认主仍返回成功，使仅在认主失败后调用的器灵觉醒入口不可达；祭炼层数也缺少可达的持续成长路径。
- M15 协同扫描整个背包且不校验装备/主人；携带或偷来的法宝也可能提供战斗加成。
- M15 法宝飞行重新授权未消费 M13 `FlyingAuthorityPolicy`，禁飞维度可能被下一 tick 绕过。
- M13 新增的 `gradlew.unix` 与现有 `gradlew` 内容完全相同且模式为 100644，属于冗余构建脚本副本。
