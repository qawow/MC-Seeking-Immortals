# 0.2.207 强轮廓几何加厚与瓶器补词

日期：2026-07-28
`mod_version`：0.2.206 → **0.2.207**；`ModNetwork.PROTOCOL_VERSION` 保持 **30**。

## 内容

- `gourd_vessel` 补入掌天瓶、小瓶、瓶影，明确瓶器描述优先使用专属轮廓。
- 巨爪增加指节与指尖星芒；灵蛇改为分段蜿蜒躯干并补头部轮廓；元神法相增加躯干、肩臂、头环、腰环与游光；火鸟增加双翼羽线、尾焰和喙端星芒。
- 重新生成 `authored_spell_effects.json` 与 `authored_visual_catalog.json`，分别保持 2,292 与 5,727 个 profile。

## 验证

- 上一次并行构建被系统以 exit 137 终止；降低并发后执行 `./gradlew build --no-daemon --max-workers=1`，`BUILD SUCCESSFUL in 1m 41s`。
- 1,162 项测试全部通过，failure/error/skipped 均为 0；作者视觉目录及其他生成资源检查全部通过。

## 版本与协议

- 本批属于代码、生成资源与运行时视觉行为变更，`mod_version` 升至 `0.2.207`。
- 未改变任何网络包字段、顺序、类型、注册或频道行为，协议版本保持 `30`。

## 备份

- 源码与版本文件：`.bak/20260727_030800/`
- 本更新的进度文档：`.bak/20260728_0.2.207_vfx_geometry/`
