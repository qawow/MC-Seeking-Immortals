# 2026-07-05 0.1.134 Fallen Demon Valley worldpack secret realm

- Change class: data/resource + test + docs + version metadata.
- Coordination: detected and preserved the parallel 0.1.133 Danxia furnace contribution-shop slice; this worldpack slice advances the workspace to 0.1.134.
- Text-material source: full scan covered all 172 files under `文本材料`; this slice uses `data/secret_realms.json#fallen_demon_valley`.
- Implementation: added `fallen_demon_valley` to shipped `worldpack/secret_realms.json` under the existing `fallen_demon_valley` region.
- Resource reuse: uses the existing `seeking_immortals:immortal_jade` ticket item, current worldpack schema, current command/GUI listing flow, and existing region anchors.
- Test coverage: `WorldpackDataServiceTest` now asserts the built-in snapshot includes `fallen_demon_valley` with the expected region, min realm, and tags.
- Version/protocol: `mod_version=0.1.134`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged because no packet fields/order/encoding/registrations changed.
- Backup paths: `.bak/20260705_0.1.133_fallen_demon_valley_realm/` and `.bak/20260705_0.1.134_fallen_demon_valley_realm_reconcile/`.
- Verification: aggregate final build passed with `.\gradlew.bat --no-daemon --max-workers=1 build`; result was BUILD SUCCESSFUL in 36s and aiPreflight recorded `mod_version=0.1.134`.
- Follow-up: layered dungeon structure, ancient demon projection encounters, demonized-gear loot, space-rift hazards, and demon-qi purge mechanics remain deferred.
