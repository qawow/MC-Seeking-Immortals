# 2026-07-06 0.1.197 Daily-Random Exact IDs

- Scope: cooperative worldpack daily-event data backfill for the remaining exact source ids from text-material `daily_random_events.json`: `spirit_rain`, `wandering_merchant`, `bandit_cultivator`, `sect_recruit`, `demon_qi_surge`, and `beast_migration`.
- Coordination: preserved the build-verified 0.1.195 Chaotic Sea anti-demon market slice and the build-verified 0.1.196 Barbarian King Territories secret-realm slice, then advanced this merged workspace to `mod_version=0.1.197`.
- Implementation: reused existing `WorldpackDataService`, daily-event JSON schema, existing regions, current effect tokens, and existing command/GUI display flow. Existing regional compatibility hooks remain unchanged.
- Data check: full text-material scan read all 276 files and parsed all 154 JSON files successfully. Shipped `daily_events.json` parses with 118 events, 0 duplicate ids, and 0 missing exact ids from text-material `daily_random_events.json`.
- Backup paths: `.bak/20260706_0.1.195_daily_random_exact_ids/` and `.bak/20260706_0.1.197_daily_random_exact_ids_version_reconcile/`.
- Verification: focused `WorldpackDataServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 20s; forced `test --rerun-tasks --stacktrace` passed in 26s after a transient test-result cleanup race; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 20s after one transient ForgeGradle `reobfJar/mappings.tsrg` miss.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encoding, registrations, or channel compatibility changed.
- Deferred: exact spirit-rain 10%/multi-day buff math, real merchant/bandit/sect-recruit encounters, demon-qi hazards, beast migration spawn multipliers, region-specific rewards, and richer effect UI.
