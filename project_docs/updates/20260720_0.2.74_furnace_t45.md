# 0.2.74 T4/T5 丹炉运行态条目（master_plan P0-1）

## 本批完成
- `multiblock_station_patterns.json` 新增 `alchemy_furnace_g4`（maxHp 400）与 `alchemy_furnace_g5`（maxHp 500），validator `alchemy_furnace_shell` 携带 tier 4/5（复用现有 T4+ 岩浆基座壳校验）。
- `AlchemyFurnaceBlockEntity` 工站 id 钳制从 `min(3,...)` 放宽到 `min(5,...)`：四/五品丹炉现在拥有独立运行态（耐久/效率/建站），不再共享三品条目。
- 测试：结构目录 87→89 计数更新；新增 g1–g5 与 refinement_forge_g2 目录条目存在性契约与钳制上限契约。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体；旧世界 T4/T5 炉首次使用时按 0.2.71 语义自动/提示建站。

## 验证
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 736 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.74`。

## 备份
- `.bak/20260720_113500_0.2.74_furnace_t45/`

## 后续（master_plan 路线）
1. P0-2 工站材料表逐站抽查降税。
2. P0-3 炼丹失败/爆炸联动工站耐久。
