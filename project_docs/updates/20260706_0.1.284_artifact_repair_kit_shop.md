# 2026-07-06 0.1.284/0.1.285 Artifact Repair Kit Shop Acquisition

- Change class: shipped shop/resource data, focused tests, one unit-test/bootstrap-safe display helper fix, docs, and version metadata.
- Text-material basis: `文本材料/data/sect_contribution_shop.json` lists `flying_sword_low` at 180 contribution and `artifact_repair_kit` at 25 contribution with monthly stock 5.
- Runtime data: Qinglan and Danxia Valley contribution halls now sell `flying_sword_low -> seeking_immortals:flying_sword_low` and `artifact_repair_kit -> seeking_immortals:artifact_repair_kit`; the repair kit uses stock 5 / 96000-tick refresh through existing `ShopService` fields.
- Coordination: detected and preserved the parallel/current `0.1.283` artifact integrity/repair runtime, so this acquisition slice advanced to `0.1.284`; final verification ran after the shared tree advanced to `mod_version=0.1.285`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields/order/encoding/registrations or channel behavior changed.
- Build fix: the first full build exposed a plain-JUnit bootstrap failure in `WorldpackPortalArrayRulesTest`; `WorldpackGameplayService.itemDescriptionId` now derives display translation keys directly from item resource ids without touching `ForgeRegistries`, while server-side item consumption still uses registry lookups.
- Backups: `.bak/20260706_0.1.283_artifact_repair_kit_shop/`, `.bak/20260706_0.1.284_artifact_shop_reconcile/`, `.bak/20260706_0.1.284_worldpack_route_display_bootstrap_fix/`, and `.bak/20260706_0.1.285_post_build_docs/`.
- Verification: changed JSON parsed locally; focused `ShopServiceTest`, `ArtifactActivationServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed; focused `WorldpackPortalArrayRulesTest` passed after the display bootstrap fix; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 31s and `aiPreflight` recorded `mod_version=0.1.285`.
- Deferred: rank-gate enforcement, repair/refinement recipes or workstation outputs, storage bracelet inventory, broader NPC/quest/loot placement, Wanbao services, dedicated item art, and live economy balance checks.
