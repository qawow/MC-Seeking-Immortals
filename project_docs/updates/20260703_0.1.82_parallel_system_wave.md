# 2026-07-03 0.1.82 parallel system wave

## Summary

- Change class: code, resources/data, localization, tests, docs, and build configuration.
- `mod_version` moved from `0.1.81` to `0.1.82`.
- `ModNetwork.PROTOCOL_VERSION` moved from `7` to `8` because 0.1.82 adds sect GUI sync/action packets.
- Backup path used by the final coordinator pass: `.bak/20260703_205601_0.1.82_docs_final/`.

## Implemented

- Added a reusable server-side `ShopService` and shipped `data/seeking_immortals/shops/qinglan_contribution_hall.json`.
- Qinglan Sect exchange now uses the generic shop core while preserving existing `/seeking_immortals sect` command compatibility.
- Extended sect progression state with compatible `QuestProgress` `sectQuestStage` and `sectFlags` persistence.
- Added sect GUI sync/action flow through `SyncSectDataPacket`, `SectActionPacket`, `ClientSectData`, and `SectScreen`; the client only sends intent and the server validates membership, progression, purchases, and donations.
- Added dedicated `sect_steward` entity and kept named vanilla villager steward compatibility.
- Added OP Qinglan outpost placement using vanilla blocks; no worldgen structure is added in this pass.
- Added JEI alchemy category for shipped built-in alchemy recipes and Patchouli alchemy system documentation.
- Improved technique feedback: HUD cooldown fraction/seconds, low-mana release gating, JSON/configured cost and cooldown lookup, technique-id effect lookup, visible sword projectile renderer, 8-second Big Dipper sword-array loop, and flight feedback hooks.
- Added/updated unit and resource tests for QuestProgress sect state, shop parsing/purchase behavior, sect packet encode/decode, client technique parsing, and shipped JSON validation.

## Verification

- Forced test: `.\gradlew.bat --no-daemon --max-workers=1 test --rerun-tasks` passed, `BUILD SUCCESSFUL in 25s`.
- Final build: `.\gradlew.bat --no-daemon --max-workers=1 build` passed, `BUILD SUCCESSFUL in 27s`; 11 actionable tasks, 5 executed and 6 up-to-date.
- `aiPreflight` recorded `mod_version=0.1.82`. The network warning is expected and covered by `ModNetwork.PROTOCOL_VERSION=8`.

## Remaining Manual Regression

- Seven Mysteries completion gate before Qinglan joining.
- Yue arrival gate and opening the sect GUI.
- Qinglan steward entity and legacy named-villager steward interaction.
- Contribution purchase failure without deduction, purchase success reward, Spirit Grass donation, and inventory-full fallback drops.
- JEI/Patchouli alchemy viewing in a real client, including no-Patchouli startup safety.
- Skill bar GUI Scale 1/2/3/Auto visual checks; single sword, Three-Talent/Big-Dipper visibility; Big Dipper cleanup after 8 seconds; flying mana drain and feedback.

## Notes

- This version still ships only the original Qinglan Sect.
- Sect GUI/NPC/structure work remains an MVP: no full sect GUI container, natural worldgen, dialogue tree JSON, or complete multi-sect Yue storyline is included.
- The generic shop core supports the current sect-contribution currency path and reserves item-currency fields for later expansion.
