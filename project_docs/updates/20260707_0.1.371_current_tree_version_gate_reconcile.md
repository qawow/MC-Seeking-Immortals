# 2026-07-07 - 0.1.371 Current-tree version-gate reconcile

## Summary
- Rechecked the current shared tree after continued parallel movement beyond the `0.1.369` Fallen Demon / Yin FTB recheck.
- A build attempt at `0.1.370` failed only the `aiPreflight` version gate because shippable tracked files had changed since the previous successful fingerprint.
- The shared tree advanced to `mod_version=0.1.371` without using `-PaiSkipVersionBumpCheck=true` and without reverting concurrent edits.
- The Fallen Demon / Yin FTB bridge remains present: `1710`, `1715`, and `1716` check Yin Stone x8, Soul-Gathering Stone x1, and Soul Fragment x2.

## Verification
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL.
- `aiPreflight` recorded `mod_version=0.1.371`.

## Compatibility
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed by this version/docs reconcile.
- The inherited network-package warning remains a release-audit item if pre-existing packet diffs changed compatibility.
