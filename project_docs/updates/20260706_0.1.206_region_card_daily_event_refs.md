# 2026-07-06 0.1.206 Region-Card Daily-Event Refs

- Change class: data/resource/test/docs/version metadata; `mod_version` advanced to `0.1.206`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed.
- Backup: `.bak/20260706_0.1.206_region_card_daily_event_refs/`.
- Text-material scan: read all 276 files under `文本材料`; all 154 JSON files parsed successfully.
- Implementation: added exact worldpack daily-event hooks for `sect_recruitment_spring`, `bandit_cultivator_road`, `spirit_herb_gatherers`, `tianlan_patrol`, `ghost_fog_truce`, `spatial_rift_flutter`, `king_territory_intrusion`, `fengyuan_clan_feud`, `spirit_storm`, and `merit_patrol`.
- Reuse: all hooks use existing shipped regions, `WorldpackDataService`, current command/GUI display flow, saved event-roll compatibility, and existing effect tokens.
- Verification: focused `WorldpackDataServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 14s; full `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 23s and `aiPreflight` recorded `mod_version=0.1.206`.
- Follow-up: deeper runtime behavior remains open for recruitment, encounters, patrols, ghost fog, spatial rifts, demon-king intrusion, clan feuds, spirit storms, and Tianyuan merit rewards.
