# 2026-07-06 0.1.201 Spirit Rain Bonus Gameplay

- Change class: code/resource/data/test/docs. This slice preserves the parallel 0.1.198 Inverse Star demon-heart shop data and 0.1.200 Qinglan Huangfeng stock data, then records the merged workspace under `mod_version=0.1.201` after the docs reconciliation required a fresh preflight fingerprint.
- Source material: `文本材料/data/daily_random_events.json#spirit_rain` with `cultivation_speed_1.1_1day`.
- Implementation: added `spirit_rain_bonus`, moved exact `worldpack/daily_events.json#spirit_rain` to the new token, kept `starter_spirit_rain` on `aura_plus_5`, and made `WorldpackGameplayService` apply +10% through the existing active daily-event aura-gain path.
- UI/tests: `WorldpackScreen` now maps the new token to zh/en localization; `WorldpackDataServiceTest` and `ScreenLayoutTest` cover the data and display mapping.
- Backup paths: `.bak/20260706_0.1.199_spirit_rain_bonus/`, `.bak/20260706_0.1.200_spirit_rain_docs_reconcile/`, and `.bak/20260706_0.1.201_spirit_rain_version_reconcile/`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed.
- Verification: focused `WorldpackDataServiceTest`, `ScreenLayoutTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 18s. A full build at `0.1.200` passed, the docs-only reconciliation then correctly tripped the version gate, and the final `0.1.201` build passed with `aiPreflight` recording `mod_version=0.1.201`.
