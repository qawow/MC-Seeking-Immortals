# 0.2.216 既有几何漏映射修复

## 变更类别

代码、生成资源、测试与文档。复用现有 Lodestone 运行时几何，不新增前置库，不改变网络协议。

## 原文边界

- `chain_net` 接入明确形成或存在的巨网、实体色丝网、光网、雷网和火网。
- `spear_spike` 接入晶/冰/火/水/雷枪、银短枪、长箭/光箭/巨箭、黑弩箭、短戈/长戈/金戈和骨矛。
- `wheel_disc` 接入漆黑/黑色/银色物理圆环、地蛟筋所化淡黄色手镯和巨大云环。
- `seal_cage` 接入铁笼、黑笼和金色囚笼；`bell_chime` 接入四翅所化黄钟及其钟音。
- 排除“丝网般的无数白痕”“自投罗网”“蛛网般裂隙”“弩箭般”和“杀手锏”等比喻或惯用语。

## 语义保真

- 数量只从物件邻近结构读取：无数/千余为 20，一根根/一杆杆/一支支为 12，九具傀儡各持长戈为 9，六杆雷枪为 6，三柄短戈/金戈为 3。
- 黑色雷网为 `yin→thunder`，金色雷网为 `metal→thunder`，银色火网为 `qi→fire`，黑线火网为 `yin→fire`；冰/水枪为 water、青色长箭为 wood、黑环为 yin、淡黄兽筋手镯为 earth。
- 伴随主体不丢失：黑环放出的上千巨型甲虫以 20 份分层采样；黑笼中的蛟影、金囚笼外的赤金火焰龙首、银环爆裂弧、黄钟声波、元磁吞水枪和五具骨架佩环均共存。
- 本批不修改 `VisualPrimitive`、`LodestoneTechniqueVfx`、52-shape switch、每层 8 粒、事件 7/16/30 或 72 copies 上限。

## 差异审计

- 生成目录保持 5,727 profiles、2,292 techniques、1,806 authored、9,947/9,947 引文。
- 术法程序从 10,728 增至 10,763 层，primitive 种类保持 88。
- 59 个变化引文组涉及 48 个预期 technique ID；逐组结构化过滤后非目标变化为 0。
- 目标计数：`chain_net=558`、`spear_spike=157`、`wheel_disc=45`、`seal_cage=23`、`bell_chime=5`。

## 验证

- `python3 -B scripts/generate_authored_spell_effects.py --check`：2,292 profiles，当前。
- `python3 -B scripts/generate_authored_visual_catalog.py --check`：5,727 profiles，当前。
- `AuthoredVisualCatalogTest` 与 `LodestoneVfxContractTest`：通过。
- `./gradlew build --no-daemon --max-workers=1`：`BUILD SUCCESSFUL in 1m 22s`；251 个测试套件、1,168 项测试，failure/error/skipped 均为 0。
- `build/libs/seeking_immortals-0.2.216.jar` SHA-256：`2416e566cd560ba3b15e1413f6d7fd64a23aa2246a8c159ae05bb12376eb562d`。

## 版本与回滚

- `mod_version=0.2.216`。
- `ModNetwork.PROTOCOL_VERSION=30` 保持不变；无包字段、顺序、类型、注册或频道兼容变化。
- 既有文件备份：`.bak/20260728_044650_0.2.216_existing_geometry/`。

## 剩余风险

自动测试和目录差异审计不能替代真实客户端中对透明排序、低粒子档、多名玩家同时施法、远距离 LOD 和快速连续事件的视觉签字；总体原文视觉目标仍需继续扫描剩余具名物件与实机验收。
