# 0.2.103 美术/GeckoLib 可落地深度

## 摘要

在无法交付工作室手绘精模的前提下，完成代码与资源侧美术深度：
1. 召唤仆从：`attack`/`float_idle` 动画，ghost 悬浮、archetype 缩放，四系实体贴图刷新。
2. 灵舟：`SpiritBoatEntity` 升级 GeoEntity，新增 geo/animation/贴图与可见 `SpiritBoatRenderer`（云轿变体贴图）。
3. 技能 HUD：技能图标库扩展到 1200+，`ImmortalUiSkin` 改为资源发现而非 15 个硬编码。

## 说明

本批是“可玩可见”的程序化/工程美术深度，**不是**最终工作室 GeckoLib 精模替换。最终手绘骨骼、绑定、阶段动画仍属外部美术管线。

## 验证

- `./gradlew build --no-daemon` 成功；preflight `mod_version=0.2.103`
- 协议 26 不变

## 备份

`.bak/20260720_224500_0.2.103_art_geckolib/`

## 后续

- 工作室级手绘 GeckoLib 精模（外部）
- 完整实机烟测
- 专服/双客户端复签
