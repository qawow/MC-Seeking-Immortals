# 0.1.454 Quest authority closeout (Wave1 priority-1)

## Class
Code (quest services + tracker packet action encoding) + lang + docs

## Priority board (1→5)
1. **任务权威闭环** ← this wave
2. 召唤/傀儡/灵兽实体化
3. 法宝/炼器深度
4. 秘境实例化内容
5. 美术与 live smoke 抛光

## Changes
- `TextQuestChainService`
  - `resolveItem` now uses Forge item registry (covers bulk carriers)
  - branch lock after non-neutral choice
  - unified authority reward ledger (`seeking_immortals_quest_authority_rewards`)
  - `syncTracker` after start/advance/branch
- `FtbRewardBridgeService` shares the same ledger (no double grant)
- `QuestTrackerActionPacket` supports `start:`, `advance:`, `branch:` inside existing single UTF action (no field/order change → protocol 13)
- tests: deterministic stage costs + ledger null-safety
- lang: authority_reward / branch_already_locked

## Protocol
13 unchanged (action string only; still one UTF field)

## Build
`./gradlew --no-daemon --max-workers=1 test --tests ...TextQuest* build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.454`
Network package warning notes QuestTrackerActionPacket encoding length only (still one UTF field; protocol remains 13).

## Backup
`.bak/20260713_005451_wave454_quest_authority/`
