# 2026-06-30 0.1.62

## Change

- Fixed `TechniqueEditScreen` learned technique overflow behavior.
- The learned list now supports mouse-wheel scrolling inside the list area.
- Removed the visible `+N hidden` overflow summary path and added a small scrollbar indicator.

## Technical Notes

- Existing drag/drop binding and right-click slot clearing are unchanged.
- `SetTechniqueSlotPacket` fields/order/encoding are unchanged.
- `ModNetwork.PROTOCOL_VERSION` remains `5`.

## Verification

- `.\gradlew.bat --no-daemon --max-workers=1 build` passed, `BUILD SUCCESSFUL in 47s`.
