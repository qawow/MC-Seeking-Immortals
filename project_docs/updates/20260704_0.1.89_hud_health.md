# 2026-07-04 0.1.89 HUD and health overlay refactor

- Change class: client UI Java, layout test, and project docs.
- Version: `mod_version=0.1.89`.
- Protocol: `ModNetwork.PROTOCOL_VERSION=8`; no packet fields, field order, encoding/decoding, registrations, or channel compatibility changed.
- Backup path: `.bak/20260704_0.1.88_hud_health/`.
- Note: the user plan targeted 0.1.88, but the shared worktree had already consumed 0.1.88 for texture final verification; this HUD code change built successfully with the current 0.1.89 worktree.
- Kept the 7-slot technique skill bar anchored on the right and exposed reserved width for adjacent HUD layout.
- Expanded the top-right cultivation HUD labels to full Chinese labels and clamped/truncated the panel before the right-side skill bar.
- Added `CultivationHealthOverlay`, a client-only left-top native health panel with 气血 text, red health fill, and gold 护体 absorption fill.
- Replaced only the vanilla player-health hearts by canceling `VanillaGuiOverlay.PLAYER_HEALTH`; armor, hunger, oxygen, experience, and other vanilla HUD layers remain untouched.
- Focused test: `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.client.ScreenLayoutTest` passed with `BUILD SUCCESSFUL in 22s`.
- Build: final `./gradlew --no-daemon --max-workers=1 build` was rerun after documentation/concurrent-state checks and passed with `BUILD SUCCESSFUL in 23s`; 11 actionable tasks, 6 executed and 5 up-to-date.
- Remaining risk: manual GUI Scale 1/2/3/Auto checks are still needed for right skill bar, right-top cultivation HUD, left-top health/absorption display, F1 hiding, and overlap with scoreboards/minimaps.
