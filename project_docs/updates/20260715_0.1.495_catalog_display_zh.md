# 0.1.495 catalog display zh

- Change class: resources (catalog index JSON) + project docs.
- Backup: `.bak/20260715_170000_catalog_display_zh/`.
- Version: `mod_version=0.1.495`.
- Protocol: remains `18`.

## Delivered
- Localized remaining **222** English-only `display` fields across 16 catalog `*_index.json` files to Chinese.
- Prefer existing item/block zh names; explicit map + token humanizer for event/hook/manifest/loot ids.
- Post-check: English-only catalog index displays = **0**.

## Notes
- These are catalog/index displays (commands/soft browsers), not item registry names.
- Item hover names were already cleaned in 0.1.494.
