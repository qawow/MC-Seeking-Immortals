# 2026-07-07 - 0.1.367 Snake Pearl current-tree recheck

## Summary
- Rechecked the current shared tree after parallel version movement advanced `gradle.properties` to `0.1.367`.
- Preserved the Snake Pearl recipe, Confucian spell effects, Tiannan FTB bridge, Demon Rift portal, Qingning Mirror recipe, and earlier shared edits.
- Confirmed the Snake Pearl recipe still uses current mapped carriers for `demon_core_fragment` and `beast_blood_vial`.

## Verification
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL.
- `aiPreflight` recorded `mod_version=0.1.367`.

## Compatibility
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed by this recheck.
- The inherited network-package warning remains a release-audit item if pre-existing packet diffs changed compatibility.
