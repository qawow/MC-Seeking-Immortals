# 2026-06-25 0.1.54 筑基期技能 MVP

## 变更分类

- Code / resources / gameplay data / docs。
- `mod_version`：`0.1.53` -> `0.1.54`。
- 网络协议：未修改 packet 字段、顺序、编码或通道行为，`ModNetwork.PROTOCOL_VERSION` 维持 `5`。

## 已完成

- 新增筑基期 6 个技能：
  - 神识扩展：筑基初自动解锁，神识读取值 ×1.5。
  - 御剑飞行进阶：筑基初技能飞行，速度为练气御剑 1.8 倍，每秒 3 灵力。
  - 罡气护体：消耗 50 灵力，下一次受伤全额抵消。
  - 五行遁术：筑基中，五行灵根校验，向视线方向遁行约 20 格。
  - 北斗剑阵：筑基后，7 把飞剑齐射 MVP。
  - 阵法感知：筑基圆满，粒子标记附近阵法/灵力边界。
- 筑基 technique JSON 新增 6 个条目，名称与 `SkillType` 显示名对齐。
- 自动解锁逻辑泛化为 configured technique skill，继续兼容旧 `unlockEligiblePhase4Skills()`。
- 进阶御剑新增独立飞行授权来源 `foundation_flying`，死亡、重生、换维会清理状态。

## 验证

- `./gradlew --no-daemon --max-workers=1 build`
- 结果：BUILD SUCCESSFUL in 41s。
- `aiPreflight`：passed，记录 `mod_version=0.1.54`。

## 剩余风险

- 尚未进行真实客户端/服务端游戏内回归。
- 北斗剑阵仍是七剑齐射 MVP，未实现 8 秒持续环绕攻击。
- 御剑飞行进阶未实现三把飞剑护体视觉或飞剑模型骑乘。
- 技能图标、飞行动效、阵法边界轮廓仍为后续美术/客户端表现工作。
