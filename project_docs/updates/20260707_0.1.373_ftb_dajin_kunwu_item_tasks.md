# 2026-07-07 - 0.1.373 FTB Dajin / Kunwu item-task bridge

## Summary
- Re-read the full `文本材料` corpus: 315 UTF-8 files read, 207 JSON files parsed successfully.
- Expanded the packaged Dajin / Kunwu FTB chapter with five more non-consuming item tasks.
- `1000000000001602` now checks 1x `seeking_immortals:immortal_jade` for Wanbao auction preview / bidding collateral.
- `1000000000001607` now checks 1x `seeking_immortals:jade_slip_blank` for Kunwu intelligence recording.
- `1000000000001608` now checks 1x `seeking_immortals:cold_jade` for Kunwu cold-tide preparation.
- `1000000000001609` now checks 1x `seeking_immortals:spirit_gathering_array` for the Kunwu outer-array formation route.
- `1000000000001613` now checks 1x `seeking_immortals:demon_suppress_talisman_blank` as the current demon-sealing array-script proxy.
- `FtbQuestSnbtTest` now validates 61 implemented item tasks.

## Verification
- Focused FTB quest test passed: `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.quest.FtbQuestSnbtTest`.
- Final Gradle build passed: `.\gradlew.bat --no-daemon --max-workers=1 build`.
- Final current-tree build recorded `mod_version=0.1.374` after parallel version movement beyond the initial `0.1.373` Dajin / Kunwu bridge.

## Compatibility
- `mod_version` advanced from the observed current `0.1.372` tree to `0.1.373` for this FTB slice; current final tree is `0.1.374` after a parallel version advance.
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed.
- No Java gameplay services, item/block/entity registries, GUI/menu/container classes, shops, capabilities, player NBT schema, spell/technique data, artifact/refinement maps, worldpack schemas, or runtime data loaders changed.

## Remaining Risk
- These are FTB inventory checks only. They do not consume resources, grant authoritative rewards, lock branches, write `QuestProgress`, validate auction access, prove Kunwu permit state, validate formation skill, trigger the puppet-king encounter, add quest icons, or replace live FTB client smoke tests.
