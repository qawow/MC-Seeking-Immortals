# 2026-07-06 0.1.267 Artifact catalog commands

- Change class: code/tests/docs. This slice exposes the shipped artifact catalog through existing server commands and uses the current shared `mod_version=0.1.267`.
- Text-material basis: the earlier artifact import read all 315 files under `文本材料` and parsed all 207 JSON files successfully; this slice reuses the shipped artifact/refinement JSON resources from `data/seeking_immortals/artifacts/`.
- Implementation: added `/seeking_immortals artifact` command coverage for P0 launch artifacts, catalog counts, source-file entry counts, artifact info lookup, and refinement recipe lookup by artifact id or recipe id.
- Service/test hook: added `ArtifactDataService.Snapshot#priorityArtifacts` and coverage in `ArtifactDataServiceTest`.
- Version/protocol: `mod_version=0.1.267`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged because no packet fields, field order, encoding, registrations, or channel behavior changed.
- Backup paths: `.bak/20260706_0.1.266_artifact_catalog_commands/` and `.bak/20260706_0.1.266_artifact_catalog_commands_reconcile/`.
- Verification: focused `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.artifact.ArtifactDataServiceTest` passed with BUILD SUCCESSFUL in 20s. Current-tree `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 28s, then the docs-inclusive recheck passed with BUILD SUCCESSFUL in 20s; `aiPreflight` recorded `mod_version=0.1.267`.
- Follow-up: register real P0/P1 artifact items, implement refinement workstations and recipes, add category-specific equipment/Curios behavior, implement talisman-treasure use/durability, connect Wanbao auction/appraisal services, and add dedicated item art.
