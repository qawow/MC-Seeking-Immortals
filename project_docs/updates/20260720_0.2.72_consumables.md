# 0.2.72 剩余消耗品可执行化

## 本批完成

### 目录消耗品接入真实效果
以下原惰性载体现在右键有服务端权威效果：
- 旅行类：`spirit_boat_ticket`（乱星海）、`ferry_pass`（冥河）、`teleport_talisman_chaotic_sea`、`diyuan_access_token`（地渊）——统一走 `WorldpackGameplayService.travel`。
- 阵法类：`spirit_gathering_array_disk` 部署一次性 SPIRIT_GATHER 自由阵场（复用现有阵场服务与灵砂延时）。
- 凭证类：`auction_invitation` / `star_palace_tax_receipt` / `star_palace_patrol_seal` 兑换商会/星宫声望，巡防令附短时伤减；`sect_identity_token` 展示当前宗门与职务，无宗门 fail-closed。
- 见闻类：`void_palace_map_fragment` / `fallen_demon_scout_report` / `kunwu_map_scroll` 映射到真实 chronicle 事件（A4_void_palace_built / E_ancient_demon_seal_weak / A1_kunwu_peak）并走既有见闻发现权威。
- 医疗类：`mortal_medicine`（合成定义）与 `demon_qi_purge_pill` 复用恢复/驱魔效果。

### 分类器
- `BulkItemClassifier` EXECUTABLE 集扩容；无 catalog 行的 `mortal_medicine` / `diyuan_access_token` 走合成定义。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体。

## 验证
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 733 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.72`。

## 备份
- `.bak/20260720_105500_0.2.72_consumables/`

## 后续
1. 按用户指令转入代码问题审查批次。
2. 实机客户端/专服/多人烟测。
