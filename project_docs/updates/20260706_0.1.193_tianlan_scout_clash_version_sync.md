# 2026-07-06 0.1.193 Tianlan Scout-Clash Version-Sync Verification

- Change class: version/docs verification; no additional gameplay/data content beyond preserving the Tianlan scout-clash hook.
- Coordination: final verification detected that the shared workspace had advanced from the planned `0.1.192` label to `mod_version=0.1.193`; the version advance was preserved rather than reverted.
- Preserved implementation: `tianlan_mulan_scout_clash` remains under existing region `tianlan`, preserving the existing `mulan_scout_clash` event under `mulan_grassland`.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, order, encoding, registrations, or channel behavior changed.
- Backup path: `.bak/20260706_0.1.192_tianlan_scout_clash_event/`.
- Verification: final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 20s and `aiPreflight` recorded `mod_version=0.1.193`.
