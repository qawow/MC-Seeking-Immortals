# 2026-07-05 0.1.158 Test runtime + merged data verification

- Change class: build logic, version metadata, docs, and verification of the current merged text-material data workspace.
- Preserved 0.1.157 Outer Sea pearl worldpack data, Tiannan Body Tempering market data, Chaotic Sea Sea Calm market data, and schema-normalization work.
- Fixed focused test runtime classpath by adding `sourceSets.main.output` to the `test` task classpath in `build.gradle`.
- `mod_version` is now `0.1.158`.
- No packet fields/order/encoding/registrations changed; `ModNetwork.PROTOCOL_VERSION` remains `9`.
- Backup paths: `.bak/20260705_0.1.158_test_runtime_outer_sea_final/` and `.bak/20260705_0.1.158_docs_after_merged_157/`.
- Verification: the initial focused worldpack/resource test rerun failed with `NoClassDefFoundError` for main classes; after the classpath fix, the same focused command passed with BUILD SUCCESSFUL in 21s. Final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 23s and `aiPreflight` recorded `mod_version=0.1.158`.
