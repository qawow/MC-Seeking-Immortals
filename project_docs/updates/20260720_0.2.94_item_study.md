# 0.2.94 空白载体铭刻与丹方/手册研读

## 变更类
代码 + 资源（lang）+ 测试。无网络包字段/顺序/通道变化。

## 内容
1. **空白玉简/纸方** `jade_slip_blank` / `paper_formula_scroll`
   - 进入 executable consumable 白名单，effect=`inscribe_formula`
   - 右键按介质（玉/纸）随机铭刻对应 `AlchemyFormulaItem`，经 `giveOrEnqueue` 交付并消耗空白载体
2. **丹方研读** `AlchemyFormulaItem.use`
   - 写入持久 `seeking_immortals_studied_formulas`
   - 丹炉 `hasInstalledFormula` 接受已研读知识或槽位装入；研读不消耗丹方物
   - Clone 经 `AlchemyFormulaKnowledge.copyProgressionData` 保留
3. **Bulk 手册** `BulkItemKind.MANUAL`
   - classifier 将 manual/technique 与常见残页 id 映射为 `CatalogManualItem`
   - `ManualCatalogService.study` 对无 manuals_catalog 行的 bulk 手册 soft study（记 studied + 尝试 source→method）

## 版本与协议
- `mod_version` 0.2.93 → **0.2.94**
- `ModNetwork.PROTOCOL_VERSION` 保持 **26**

## 验证
- 全量 `./gradlew build --no-daemon` BUILD SUCCESSFUL

## 备份
`.bak/20260720_190500_0.2.94_item_study/`

## 仍未完成
- bulk 符箓 84 条直放/消耗映射
- inspect_only 阵具激活加深
- equipment 灵舟/傀儡右键 board/summon
- 缺表丹药 + spirit_pill_voucher
- 最终美术与实机/多人复签
