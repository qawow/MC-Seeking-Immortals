# 2026-07-04 0.1.101 Review Fixes (0.1.92 Plan)

## Summary

- Actual build version is `mod_version=0.1.101` because the working tree had advanced beyond the original plan label `0.1.92`.
- `ModNetwork.PROTOCOL_VERSION` is `8`.
- Fixed the reviewed health HUD replacement gate, technique cooldown overflow, Qinglan sect shop failure feedback, and Patchouli optional-dependency isolation.
- Backed up and removed transient market/worldpack packet/UI files so this review-fix pass does not add packet registrations or a new client UI surface.
- A later concurrent protocol-9 worldpack/market writeback was backed up and superseded; current source truth is protocol 8.

## Verification

- Focused tests passed: `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.network.SyncLearnedTechniquesPacketTest --tests com.xunxian.seekingimmortals.client.ScreenLayoutTest` ended with `BUILD SUCCESSFUL in 16s`.
- Final build passed: `./gradlew --no-daemon --max-workers=1 build` ended with `BUILD SUCCESSFUL in 18s`; aiPreflight recorded `mod_version=0.1.101`.

## Notes

- Backup path: `.bak/20260704_0.1.92_review_fixes/`.
- Additional cleanup backup: `.bak/20260704_0.1.92_review_fixes/final_cleanup_after_protocol_reappeared/`.
- Concurrent writeback backup: `.bak/20260704_0.1.92_review_fixes/after_concurrent_worldpack_rewrite/`.
- No packet fields, field order, encoding, registrations, or channel compatibility changed.
- No new obtainable items, blocks, models, textures, recipes, loot tables, tags, data packs, or creative-tab entries were added.
- Manual checks remain recommended for `runClientNoPatchouli`, chat/open-screen/F1 health visibility, GUI Scale 1/2/3/Auto, and Qinglan no-stock/currency-error prompts.