# 0.2.50 傀儡图纸与制作事务

## 完成内容

- `basic_wood_puppet_blueprint` 作为 `assemble_basic_wood` 的行囊知识凭证。
- `giant_ape_puppet_blueprint` 作为 `assemble_giant_ape` 的行囊知识凭证。
- 图纸只需持有，不随每次装配消耗，避免任务/商店稀有图纸变成一次性材料。
- 装配顺序统一为：傀儡操控技能 -> 图纸 -> 完整材料 -> 扣料 -> 成功率 -> 实体生成。
- 技能锁定、缺图纸或材料不足均零修改库存。
- 成功率失败保留原设计的材料损耗与少量练习经验；实体生成失败则退还全部材料，不发临时强化或成功熟练度。
- `puppet_repair_kit` 进入 `repairOwnedPuppets` 消费端并优先于旧通用修缮材料。

## 验证

- 定向：`./gradlew test --tests 'com.xunxian.seekingimmortals.craft.PuppetCraftServiceTest' --tests 'com.xunxian.seekingimmortals.catalog.SummonHonestMvpServiceTest' --no-daemon`
- 结果：BUILD SUCCESSFUL。
- 最终普通全量：`./gradlew build --no-daemon`
- 结果：BUILD SUCCESSFUL（58s）；全量 691 项测试，0 failures、0 errors、0 skipped。

## 版本与协议

- `mod_version`: `0.2.49 -> 0.2.50`
- `ModNetwork.PROTOCOL_VERSION`: 保持 `25`
- 无网络包字段、编码、顺序、注册、方向或不兼容通道变化。

## 回滚

- `.bak/20260720_0.2.50_puppet_items/`

## 下一步

- 为 `sect_contribution_token` 确定可防刷的兑换入口和数值。
- 为可获取的其余傀儡图纸/功法补真实来源后再增加配方门槛。
- 扩充 `formation_item_behaviors` 覆盖剩余阵盘，并逐个验证形成、消耗和失败回滚。
