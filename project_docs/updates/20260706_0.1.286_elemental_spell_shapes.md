# 2026-07-06 0.1.286 Elemental spell shapes

- Direct source basis: `文本材料/data/techniques/elemental.json` entries `thunder_palm`, `flame_ring`, `frost_armor`, and `gold_beam`.
- Added executable Qi Refining spell effects through the existing server-authoritative release path:
  - `ThunderPalmSpell`: short-range custom spiritual-thunder palm arc, no vanilla lightning entity.
  - `FlameRingSpell`: caster-centered custom DustParticle fire ring AOE.
  - `FrostArmorSpell`: icy armor shell particles with defensive/fire-resist buffs.
  - `GoldBeamSpell`: custom piercing gold beam along the look vector.
- Added `SkillType` entries, `SkillEffectRegistry` wiring, Qi Refining technique data entries, and zh_cn/en_us release messages.
- `mod_version` is `0.1.286`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed.
- Backup path: `.bak/20260706_0.1.284_elemental_spells/` with reconciliation backups under `post_parallel_current/` and `post_artifact_shop_current/`.
- Validation: changed technique/lang JSON parsed locally; `compileJava --rerun-tasks` passed after rerunning outside the sandbox dependency-resolution block; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 33s.
- Deferred: `lava_burst`, `mist_rain`, `sand_storm`, `blizzard`, `cyclone`, `chain_lightning`, light/dark projectiles, Foundation elemental bursts, dedicated particle assets, balance tuning, and live client visual smoke tests.
