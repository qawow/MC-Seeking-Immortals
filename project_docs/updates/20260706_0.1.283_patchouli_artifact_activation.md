# 2026-07-06 - 0.1.287 Patchouli Artifact Activation Guide

- Change class: resources/docs Patchouli guide slice plus a narrow Java test/build guard.
- Coordination: began on the observed shared `mod_version=0.1.283` tree, then preserved later parallel advances through `0.1.286` and finalized under `mod_version=0.1.287` without reverting parallel edits.
- Source basis: `文本材料/docs/ARTIFACTS_FANREN.md`, `ARTIFACTS_AND_PILLS.md`, `ARTIFACT_ELEVEN_TIERS.md`, `REFINEMENT_ARTIFACTS_MASTER_LIST.md`, `文本材料/data/artifact_tier_rules.json`, `forge_artifact_priority.json`, and `talisman_treasure_templates.json`.
- Implementation: added mirrored zh_cn/en_us Patchouli `artifact_activation` entries that explain current right-click artifact activation, server-side gates, effect families, talisman-treasure uses, artifact repair kit behavior, Curios flying carriers, and deferred storage/refinement/auction/art-placement work.
- Build guard: `WorldpackGameplayService.resolveItem` now safely returns `null` when `ForgeRegistries` is unavailable or unbootstrapped in plain JUnit, preserving display-only route hint fallback while leaving bootstrapped server-side item consumption unchanged.
- Version/protocol: current shared `mod_version=0.1.287`; `ModNetwork.PROTOCOL_VERSION=9` unchanged because this finish did not change packet fields/order/encoding/registrations or channel behavior.
- Backups: `.bak/20260706_0.1.283_patchouli_artifact_activation/`, `.bak/20260706_0.1.284_worldpack_route_hint_bootstrap_guard/`, `.bak/20260706_0.1.287_patchouli_artifact_activation_reconcile/`, and `.bak/20260706_0.1.287_patchouli_artifact_activation_post_build_docs/`.
- Verification: changed Patchouli JSON parsed successfully, all 84 guide JSON files parsed successfully, focused `WorldpackPortalArrayRulesTest` passed, and final current-tree `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 31s; `aiPreflight` recorded `mod_version=0.1.287`.
