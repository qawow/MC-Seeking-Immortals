# 0.1.477 learned methods protocol sync

## Change class
Code + docs + protocol. `mod_version` 0.1.476 → 0.1.477.
`ModNetwork.PROTOCOL_VERSION` 13 → **14** (new packet registration).

## Backup
`.bak/20260714_011109_0.1.477_learned_methods_sync/`

## Implemented
1. **SyncLearnedMethodsPacket** — encode/decode/handle list of learned method ids
2. **ClientMethodData** — client mirror + display helpers
3. **ModNetwork** — register packet; protocol 14
4. **ManualCatalogService**
   - `learnedMethodIds`
   - `markLearnedMethod` auto-sends sync when newly granted
   - `syncLearnedMethods` public helper
5. **ModEvents login** — send learned methods with other sync packets
6. **ClientEvents** — reset ClientMethodData on logout/join
7. **CultivationStatsScreen** — shows synced learned methods (fallback: waiting sync)

## Build
BUILD SUCCESSFUL; jar `seeking_immortals-0.1.477.jar`.

## Compatibility
Clients/servers must both be 0.1.477+ (protocol 14). Mixed 13/14 will refuse channel.

## Remaining deferred
Art, biomes, GeckoLib, human live-smoke re-sign, interactive method-tree UI.
