# 2026-07-07 - 0.1.319 FTB Spirit Realm Service Chapter, Verified Under 0.1.320

Change class: resource/data/test/docs quest-presentation slice.

Implemented an additive bundled FTB Quests chapter, `seeking_immortals_spirit_realm_service.snbt`, with 36 safe checkmark nodes sourced from Spirit Realm, Tianyuan merit, Fengyuan human-clan, eighteen-clan, Diyuan, and barbarian demon-king text-material lines. The packaged defaults now expose 131 safe nodes across 6 chapters.

`FtbQuestDefaults` seeds the new chapter only when missing, preserving existing user/server FTB quest packs. `FtbQuestSnbtTest` now expects the sixth chapter and 131 total nodes. Focused `FtbQuestSnbtTest` passed with BUILD SUCCESSFUL in 13s.

No packet fields, packet order, channel behavior, capabilities, `QuestProgress`, rewards, item/block/entity registries, dimensions, shops, GUI classes, or runtime data schemas changed. `ModNetwork.PROTOCOL_VERSION` remains `9`.

Backup path: `.bak/20260707_0.1.319_ftb_spirit_realm_service/`.

Full current-tree `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 19s and `aiPreflight` recorded `mod_version=0.1.320`. The inherited network-package warning remains present and unrelated to this FTB/data/test/docs slice.
