# 2026-07-03 0.1.75 alchemy data and quality outputs

## Summary

- Moved alchemy recipes to datapack JSON under `data/seeking_immortals/alchemy/recipes/*.json` while preserving the existing `AlchemyRecipe.findById` and `findByHeldIngredient` callers.
- Added `AlchemyRecipeManager` reload/cache loading with built-in recipe fallback.
- Added pill/material naming maps and aligned `文本材料/data/alchemy_recipes.json` to registered mod ids.
- Added low/medium/high/supreme output support for the 18 catalog pill species.

## Verification

- UTF-8 JSON parse check passed for all alchemy recipe JSON, mapping JSON, setting-pack alchemy JSON, and lang files.
- Mod recipe count: 21. Setting-pack catalog recipe count: 18.
- Final required build passed with current worktree `mod_version=0.1.76`: `./gradlew --no-daemon --max-workers=1 build` -> `BUILD SUCCESSFUL in 37s`.
- `ModNetwork.PROTOCOL_VERSION` remained `6`; no packet fields/order/encoding changed.

## Notes

- The 0.1.74 furnace review behavior was not intentionally changed: opening checks, exact earth-fire-room gating, normal dismantle returns, and explosion/destruction paths were left intact.
- The current worktree had advanced to `mod_version=0.1.76` before final verification. A minimal Seven Mysteries quest compile unblock was added so the required build could complete, and this is documented separately in progress docs.
- No new PNG textures were added; new models reuse existing pill/manual textures.