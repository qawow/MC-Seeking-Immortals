# 2026-07-04 0.1.109 Spirit-stone recipe refs pass 2

## Change class

Resource/data/docs change. This pass continues text-material canonicalization without changing Java logic, registries, packet fields, packet order, packet encoding, packet registration order, or channel compatibility.

## Implemented

- Bumped `mod_version` to `0.1.109`.
- Updated five shipped recipe JSON files from legacy `seeking_immortals:spirit_stone` to registered `seeking_immortals:metal_spirit_stone`:
  - `spirit_gathering_array.json`
  - `spirit_detector.json`
  - `ling_gen_test_stone.json`
  - `leyline_compass.json`
  - `speed_talisman.json`
- Scoped the batch to avoid conflicts with another Codex worker: no Java, network, shop, Patchouli, or registry files were touched.

## Protocol

`ModNetwork.PROTOCOL_VERSION` remains `9`; this pass changes resource JSON references only.

## Backup

Rollback path: `.bak/20260704_0.1.109_spirit_stone_recipe_refs/`.

## Verification

- Focused validation passed: `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.resources.JsonSanityTest` ended BUILD SUCCESSFUL in 18s after rerunning with approval for the Gradle wrapper network/cache access.
- Final required build passed: `./gradlew --no-daemon --max-workers=1 build` ended BUILD SUCCESSFUL in 24s; `aiPreflight` recorded `mod_version=0.1.109`.
- The inherited network-package preflight warning was reviewed. This pass changed resource JSON and docs only, not packet fields/order/encoding/registrations/channel compatibility, so `ModNetwork.PROTOCOL_VERSION` remains `9`.

## Follow-up

Remaining legacy `seeking_immortals:spirit_stone` refs after this pass are expected in:

- `recipes/fire_talisman.json`
- `recipes/armor_talisman.json`
- Patchouli basics and spirit-stones icons
