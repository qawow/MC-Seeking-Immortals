# 2026-07-07 - 0.1.370 Talisman Soul Charm refinement recipe

## Summary
- Re-read the full `文本材料` corpus: 315 UTF-8 files read, 207 JSON files parsed successfully.
- Added the player-facing vanilla shaped recipe approximation `data/seeking_immortals/recipes/refine_talisman_soul_charm.json`.
- The recipe maps `talisman_paper` to the existing `seeking_immortals:talisman_paper_mortal`, uses `seeking_immortals:soul_gathering_stone`, and outputs the already registered `seeking_immortals:talisman_treasure_soul_charm`.
- Added `ResourceJsonParseTest` coverage for recipe type, pattern, mapped ingredients, and output.
- Preserved the parallel `0.1.368` Fallen Demon / Yin FTB item-task bridge and current `0.1.367` artifact/spell records without reverting their files.

## Verification
- Changed recipe JSON parsed successfully.
- Focused `./gradlew --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.resources.ResourceJsonParseTest` passed.
- A first full build at `0.1.369` failed only because `aiPreflight` detected that version's successful fingerprint had already moved in the shared tree.
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL at `0.1.370`.
- `aiPreflight` recorded `mod_version=0.1.370`; the inherited network-package warning remains unrelated to this resource/test/docs slice.

## Compatibility
- `mod_version` advanced to `0.1.370` after observing parallel version movement through `0.1.368` and a reused `0.1.369` fingerprint.
- `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, field order, encode/decode format, registrations, or channel behavior changed.
- No Java gameplay services, registries, GUI/menu/container classes, shops, worldpack schemas, capabilities, player NBT schema, FTB files, spell/technique data, or runtime data loaders changed by this recipe slice.

## Remaining Risk
- This is a vanilla crafting approximation, not the final refinement forge/custom serializer.
- Realm gates, forge-grade checks, manual unlocks, success/failure rolls, JEI display, source-accurate material acquisition, and live recipe-book smoke checks remain deferred.
