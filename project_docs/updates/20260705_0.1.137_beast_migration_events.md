# 2026-07-05 0.1.137 Beast migration daily-event backfill

- Change class: worldpack data/test/docs/version metadata.
- Text-material source: `daily_random_events.json#beast_migration`.
- Implemented entries: `mulan_beast_migration` and `fengyuan_beast_migration` in shipped worldpack daily events.
- Runtime equivalent: current worldpack daily-event UI/command flow using existing regions and effect tokens only.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains 9 because no packet fields, order, encoding, registrations, or channel compatibility changed.
- Backup path: `.bak/20260705_0.1.137_beast_migration_events/`.
- Verification: focused worldpack/resource tests passed with BUILD SUCCESSFUL in 26s, and final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 29s. `aiPreflight` recorded `mod_version=0.1.137`.
