# 0.2.193 v655–v662 生成资源刷新

## 更新内容

- 以当前 Git 已跟踪的 v655–v662 八个原著精读输入（合计 24 个术法条目）重新生成作者法术效果资源。
- 同步更新统一作者视觉目录和目录契约测试基线（非推断作者术法 1,782 → 1,806）。
- 补记 0.2.192 批次：该批构建验证由本次 0.2.193 统一构建覆盖完成（第 585 步 Verification 标记为 Superseded）。

## 数据与验证

- `authored_spell_effects.json`：2,292 个术法，747 条基础语料，1,545 条原著增量。
- `authored_visual_catalog.json`：5,727 个 profile、13,749 个 semantic layer、9,947 段视觉引文。
- 两个生成器 `--check` 与 `AuthoredVisualCatalogTest`（含 1,806 基线断言）通过。
- 普通 `./gradlew build --no-daemon --max-workers=1` 在 1 分 30 秒内成功，1,162 项测试全部通过，failure/error/skipped 均为 0。
- 过程记录：构建曾两次失败于 `:authoredSpellEffectsCheck`（并行编辑会话在重生成与检查之间修改了 Java 源，导致 `java_source_audit` 聚合指纹过期）；采用"重生成 → 立即构建"的紧凑循环后通过。

## 版本与协议

- `mod_version=0.2.193`。
- 未改变网络包字段、顺序、类型、注册或频道行为，`ModNetwork.PROTOCOL_VERSION=30` 保持不变。
- 备份目录：`.bak/20260726_0.2.193_v662_refresh/`。
