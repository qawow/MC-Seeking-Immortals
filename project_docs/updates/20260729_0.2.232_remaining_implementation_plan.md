# `0.2.232` 后续完整实现计划

## 本次更新

- 重写 `project_docs/implementation_plan.md`，以当前 `0.2.232`、协议 30 和提交 `0090c725` 为基线，取代原 `0.1.57` 旧计划。
- 把 28 项已确认前端交互问题纳入统一台账，并拆为高危、列表框架、中危、低危、产品入口和旧屏协议清理批次。
- 为详细任务自然证据、对话动作分型、阴阳窟专属玩法、维度分类、本命飞剑实例绑定、旧 NPC 迁移和当前版本实机复签定义实现顺序、测试门及完成条件。
- 明确排除已经在 `0.2.176+` 完成的过期缺口，避免重复实现或回退现有生产路径。

## 变更分类

纯项目文档。未修改 Java、资源、数据包、构建逻辑、运行时配置或网络消息，因此：

- `mod_version` 保持 `0.2.232`。
- `ModNetwork.PROTOCOL_VERSION` 保持 `30`。
- 本批不运行 Gradle 构建；最近代码基线的普通完整构建已通过 251 个套件、1,182 项测试。

## 回滚

既有计划和进度文档备份位于：

`.bak/20260729_implementation_plan_0.2.232/`

## 后续入口

下一代码批应从计划的 F-A“五项高危交互修复”开始；执行时按真实当前版本增加补丁号，并在普通 `./gradlew build` 成功后独立提交。
