# 2026-07-07 0.1.376 Current Tree Recheck After Void Cold Jade Pendant

- Scope: docs-only current-tree build verification after the `0.1.375` Void Cold Jade Pendant recipe slice and parallel version movement.
- Coordination: preserved the parallel Formation Spells / Dajin-Kunwu FTB edits and did not revert unrelated shared-tree changes.
- Recheck: `refine_void_cold_jade_pendant.json` remains present, and `ResourceJsonParseTest` still covers the recipe shape/material/output.
- Version/protocol: current `mod_version` is `0.1.376`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or channel behavior changed by this recheck.
- Verification: final current-tree `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL and `aiPreflight` recorded `mod_version=0.1.376`.
- Backup: existing docs edited by this recheck were backed up to `.bak/20260707_0.1.376_current_tree_after_void_cold_jade_docs/`.
- Remaining risk: inherited network-package warning still needs protocol audit if packet compatibility changed; Void Cold Jade Pendant remains a vanilla-grid approximation pending the real refinement workstation/custom serializer.
