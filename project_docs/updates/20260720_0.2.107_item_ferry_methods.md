# 0.2.107 渡口物品路由与空 method 矩阵补术

日期：2026-07-20  
`mod_version`：`0.2.107`  
`ModNetwork.PROTOCOL_VERSION`：`26`（无包变更）

## 变更

### 物品 P0
1. **`ferry_pass`**：由 plain `Item` 改为 `CatalogConsumableItem`，右键走 `travel_nether_ferry`。
2. **`nether_ferry_coin` / `nether_ferry_token`**：不再被 `isEquipment`/`VEHICLE` 误识别为可登船载具；保持材料/通行费载体。
3. 别名丹药 `appearance_lock_pill` / `marrow_drain_pill` / `qingxu_pill`：契约测试锁定 `PillEffectCatalog` 别名解析与 PILL 分类。

### 功法矩阵
- 14 个空 `method_layer_tables`（生活/通修/专精）补入可执行 `unlock_technique_ids`（炼丹急救、定神/火球、神识扫描等语料 id）。
- 层数不变（生活 1 层、通修 9 层）。

## 测试
- `ItemFerryAndPillRoutingTest`
- `ArtifactBindsAndMethodMatrixTest`（空矩阵现有术法）
- 全量 `./gradlew build`

## 备份
`.bak/20260720_234500_0.2.107_item_ferry_methods/`

## 剩余
- 真人专服双客户端签字、完整实机烟测、工作室 GeckoLib
- 可选：专属 Structure 类接线 ring 站、talisman/summon 保真、per-station BOM
