# 2026-07-06 - 0.1.241 Wind Feather Raft ticket travel

## Summary

- Preserved the build-verified `seeking_immortals:wind_feather_raft_ticket` item carrier and current `0.1.241` workspace.
- Implemented state on disk: `WorldpackGameplayService` now treats Great Jin/Dajin -> Tianyuan as the first Wind Feather Raft route and consumes one ticket for non-creative players.
- The route reuses the existing worldpack travel service, saved anchors, Spirit-Realm portal requirement, Forge item lookup, and inventory stack consumption.
- Tianyuan City/Tianyuan Merit guide entries and `text_material_id_map.json` now describe the server-side ticket-consumption route.

## Verification

- `WorldpackPortalArrayRulesTest` covers allowed current regions `great_jin_central` and `dajin` when targeting `tianyuan`, plus rejected non-Great-Jin or non-Tianyuan combinations.
- Final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 24s and `aiPreflight` recorded `mod_version=0.1.241`.
- No packet fields/order/encoding/registrations or channel behavior changed; `ModNetwork.PROTOCOL_VERSION` remains `9`.

## Deferred

- Tianyuan merit exchange sources, ticket vendors/rewards, route NPC/UI presentation, broader route policies, Diyuan entry checks, pressure/no-fly behavior, FTB Quests routing, and dedicated route art remain future work.

Backup paths: `.bak/20260706_0.1.241_wind_feather_ticket_travel/` and `.bak/20260706_0.1.241_wind_feather_travel_docs_reconcile/`.
