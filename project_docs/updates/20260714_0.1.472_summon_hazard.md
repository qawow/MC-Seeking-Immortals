# 0.1.472 summon contract gate + trial hazard tick

## Change class
Code + lang + docs. `mod_version` 0.1.471 → 0.1.472. Protocol remains 13.

## Backup
`.bak/20260714_002943_0.1.472_summon_hazard/`

## Implemented
1. **SummonHonestMvpService.summonProxy**
   - BEAST archetype requires `BeastContractService.hasContract`
   - Prefers `BeastContractService.summon` for contract-scaled stats
   - Non-creative catalog summons cost 1-3 spirit stone shards
2. **SecretRealmTrialService.tickHazard**
   - Mild periodic reapply while active secret realm
3. **ModEvents.onPlayerTick**
   - Calls hazard tick every 5 seconds for server players

## Build
BUILD SUCCESSFUL; jar `seeking_immortals-0.1.472.jar`.

## Remaining deferred
Art, biomes, GeckoLib, human live-smoke re-sign.
