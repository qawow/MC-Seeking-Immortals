# 2026-07-06 0.1.235 Spirit Realm aura profiles

- Change class: code/tests/docs runtime gameplay slice.
- Source material: `dimensions_catalog.json`, `dimension_registry.json`, `DIMENSIONS_DESIGN.md`, and `SPIRIT_REALM_INTERFACE.md`.
- Implementation: `SpiritualAuraManager` now maps `seeking_immortals:tianyuan` to dimension aura x2.0, `seeking_immortals:spirit_fengyuan` to x1.8, and both to `SPIRIT_REALM` aura nature through the existing aura detector, HUD sync, cultivation tick, and meditation formulas.
- Portal compatibility: preserved the existing 3x3 same-level Spirit Gathering Array multiblock portal rules and `WorldpackGameplayService` destination routing.
- Version/protocol: `mod_version=0.1.235`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields, order, encoding, registration, or channel behavior changed.
- Backups: `.bak/20260706_0.1.230_spirit_realm_aura/`, `.bak/20260706_0.1.234_spirit_realm_aura_reconcile/`, and `.bak/20260706_0.1.235_spirit_realm_aura_reconcile/`.
- Verification: focused `SpiritualAuraManagerTest` and `WorldpackPortalArrayRulesTest` passed with BUILD SUCCESSFUL in 20s; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 19s and `aiPreflight` recorded `mod_version=0.1.235`.
- Verification recheck: this thread reran `.\gradlew.bat --no-daemon --max-workers=1 build`; it passed with BUILD SUCCESSFUL in 18s and `aiPreflight` recorded `mod_version=0.1.235`.
- Deferred: custom DimensionType data, visible portal activation effects, travel costs or permit rules, stricter multiblock validation, generated destination structures, and in-game client/runtime smoke checks.
