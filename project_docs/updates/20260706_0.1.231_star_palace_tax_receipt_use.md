# 2026-07-06 0.1.231 Star Palace tax receipt use

- Change class: code/resources/docs. Added server-side right-click use behavior for `seeking_immortals:star_palace_tax_receipt`.
- Source material: `consumables_catalog.json#star_palace_tax_receipt` effect `island_trade_tax_paid`, plus the current `chaotic_sea_island_general` shop-stock carrier.
- Implementation: `StarPalaceTaxReceiptItem` stores QuestProgress flag `star_palace_island_trade_tax_paid` in existing PlayerCultivation capability NBT, consumes one receipt only on first successful use, preserves creative-mode stacks, and refuses duplicate/no-data uses without consuming.
- Version/protocol: `mod_version=0.1.231`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields, order, encoding, registration, or channel behavior changed.
- Backup: `.bak/20260706_0.1.231_star_palace_tax_receipt_use/`.
- Verification: language JSON parsed successfully. Focused `ShopServiceTest`, `JsonSanityTest`, `ResourceJsonParseTest`, and `QuestProgressTest` passed with BUILD SUCCESSFUL in 13s. Final `.\gradlew.bat --no-daemon --max-workers=1 build --rerun-tasks` passed with BUILD SUCCESSFUL in 34s and `aiPreflight` recorded `mod_version=0.1.231`.
- Remaining: actual ShopService tax/discount use of the paid flag, ferry/tax quest checks, Star Palace reputation consequences, visible UI/status feedback, NPC placement, and source-accurate pearl-economy rewards.
