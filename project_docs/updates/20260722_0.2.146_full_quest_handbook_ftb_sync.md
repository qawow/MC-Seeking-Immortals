# 0.2.146 完整任务手册与原生/FTB 同步加固

## 完成内容

- 原生任务账本继续唯一持有阶段、成本、分支、声望、奖励与同步；FTB 只投影进度或请求一个经过原生校验的精确单阶段事务。
- FTB 9 章 215 节点确定性覆盖 62 条原生链和 241 个阶段；15 个回写节点先提交 native transition，再标记 FTB readiness task 完成。
- 所有 FTB 物品任务均非消耗，章节不再携带奖励；损坏/多重/跨链/跳阶段标签、消费任务、奖励及多人队伍回写全部失败关闭。
- Patchouli version 5 在 zh/en 各提供 120 条目；新 quests 分类含任务总览、7 个主线章节和 9 个原生/FTB 章节，覆盖 35 条作者任务线并使用真实 entry/anchor 链接。
- FTB `guide_page` 仅接受 9 个生成条目，并通过反射调用可选 Patchouli API；未安装 Patchouli 时不产生硬依赖。
- FTB 默认包 revision `20260722_2` 支持四代已知默认、安全升级、规范化哈希、定制/删除保留、pending 候选、备份与回滚；FTB 缺失时不写配置，managed 与 management 子路径符号链接均拒绝跟随。
- 文本材料 manifest 登记新手册语料并重算为 290 文件、12,327 条目；重建器同步维护 `total_entries`。

## 验证

- `python3 -B scripts/generate_ftb_quest_projection.py --check`：通过，9 chapters / 215 quests / 62 chains / 241 stages。
- `python3 -B scripts/generate_quest_handbook.py --check`：通过，17 bilingual entries。
- 任务、FTB、Patchouli、默认包迁移定向测试：通过。
- `./gradlew build`：`BUILD SUCCESSFUL in 1m 19s`，`aiPreflight` 记录 `mod_version=0.2.146`。
- 并发 VFX 在上述构建后继续修改共享文件，因此版本指纹门拒绝同版本重复 build；随后 `./gradlew cleanTest test` 强制执行当前树 216 个套件、987 项测试，failure/error/skipped 均为 0，未修改并发文件。

## 版本与兼容

- 变更分类：代码、资源、生成脚本、测试与文档。
- `mod_version=0.2.146`。
- 没有修改网络包字段、顺序、类型、注册或频道行为；`ModNetwork.PROTOCOL_VERSION=28`。
- 回滚目录：`.bak/20260722_quest_ftb_completion/`。

## 剩余风险

- 尚需真实客户端逐页检查中英文长页排版、FTB `guide_page` 点击与可选 Patchouli 缺失启动。
- 尚需用真实旧默认包、用户定制包和已删除受管文件的服务器配置执行迁移烟测。
- 多成员 FTB 队伍的反向写入按设计禁用，以避免共享团队进度代表错误玩家；原生任务路径不受影响。
