# 2026-07-06 0.1.225 Patchouli guide compatibility

- Change class: resource/test/docs compatibility fix.
- Fixed the starter guide invalid-book bug by moving `book.json` to `data/seeking_immortals/patchouli_books/seeking_immortals_guide/book.json`, removing the misplaced `zh_cn/book.json`, and adding an `en_us` fallback mirror for the current categories and entries.
- Added resource-test coverage for canonical Patchouli book layout, language-folder path matching, and JSON parsing.
- Backup path: `.bak/20260706_144026_patchouli_guide_compat/`.
- Version/protocol: `mod_version=0.1.225`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields, order, encoding, registration, or channel behavior changed.
- Verification: focused `ResourceJsonParseTest` and `JsonSanityTest` passed with BUILD SUCCESSFUL in 17s; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 22s and `aiPreflight` recorded `mod_version=0.1.225`.
