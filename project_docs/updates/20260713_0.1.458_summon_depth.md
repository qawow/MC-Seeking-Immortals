# 0.1.458 Summon / puppet / beast depth (Wave2 priority-2)

## Class
Code (summon services + entity AI + beast contract + capture jar + puppet craft) + lang + docs

## Priority board (1→5)
1. 任务权威闭环 — 0.1.454 / deepen 0.1.457
2. **召唤/傀儡/灵兽实体化** ← this wave
3. 法宝/炼器深度
4. 秘境实例化内容
5. 美术与 live smoke 抛光

## Changes
- `SummonedServitorEntity`: Stance FOLLOW/GUARD/AGGRESSIVE/STAY; owner right-click cycle; crafted flag; repair/extendLife; beast combat credit on hit/kill
- `SummonHonestMvpService`: setStanceAll, dismissOwned, focusTarget, repairOwnedPuppets; craft-aware concurrent cap; crafted spawn overload
- `BeastContractService`: recordCombatCredit HIT/KILL/SURVIVE; jar-seal affinity start; beastIdFromSummonId
- `ArtifactCaptureService` / `CaptureJarItem`: sneak-seal → beast contract; release as BEAST with contract scaling
- `PuppetCraftService`: crafted=true spawn + repair hint

## Protocol
13 unchanged

## Build
`./gradlew --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**
`aiPreflight` recorded `mod_version=0.1.458`

## Backup
`.bak/20260713_014947_wave458_summon_depth/`
