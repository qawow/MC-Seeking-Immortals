# 2026-07-03 0.1.80 Review Fixes

## Change Class

- Code + resources + docs.
- `mod_version` is bumped from `0.1.79` to `0.1.80`.
- `ModNetwork.PROTOCOL_VERSION` remains `7`.

## Implemented

- `PlayerCultivation.failTribulationPenalty()` now records the failed target realm before clearing active tribulation state.
- Core Formation tribulation failure clears gold-core grade and score through `clearGoldCore()`.
- Nascent Soul and higher tribulation failure keeps any existing gold core.
- `SyncLearnedTechniquesPacket` now bounds learned-technique count, slot count, cooldown count, and technique-id string length.
- `SetTechniqueSlotPacket` now bounds technique-id string length on encode/decode.
- `SyncCultivationDataPacket` decode now mirrors the existing encode string limits.
- Seven Mysteries chiseled-bookshelf and crying-obsidian markers now intercept only when the player's quest stage matches the marker.
- Removed unused `ModEvents.showMeditationStatus()` and `describeSpiritLand()`.
- Backed up and removed legacy `src/main/resources/assets/xiuxian/`.

## Verification

- Resource check: `rg "xiuxian:" src/main/resources project_docs docs` returned only explanatory docs hits in `docs/texture-resource-audit.md`; `src/main/resources` has no active `xiuxian:` references and `src/main/resources/assets/xiuxian` no longer exists.
- Tests: `.\gradlew.bat --no-daemon --max-workers=1 test` passed, `BUILD SUCCESSFUL in 12s`.
- Final build: `.\gradlew.bat --no-daemon --max-workers=1 build` passed, `BUILD SUCCESSFUL in 28s`.
- Preflight warning: expected network-package warning was emitted for dirty network files. Protocol remains `7` because this pass did not change packet fields, field order, field types, registration order, or channel compatibility.

## Notes

- Packet wire shape is unchanged: no packet fields, order, types, registration order, or channel compatibility were changed.
- The Gradle preflight network warning is expected because network package files changed; protocol remains `7` for the reason above.
- Seven Mysteries dedicated NPCs, structures, dialogue UI, and worldgen remain future work beyond this MVP marker cleanup.

## Backup

- `.bak/20260703_0.1.80_review_fixes/`
