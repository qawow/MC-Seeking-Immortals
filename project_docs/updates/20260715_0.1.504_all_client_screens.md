# 0.1.504 全部客户端界面统一重构

- 将炼丹、拍卖、市场、宗门、商店、任务、对话、探索、炼制方案和储物界面统一为问道录视觉。
- 所有非容器界面加入响应式面板、裁剪、滚动、动态文字适配和统一按钮状态。
- 保持真实容器尺寸、槽位平面、交易操作、分页、任务分支、对话动作和旅行请求不变。
- 固定容器在极小窗口保留交互坐标，仅裁剪不可见外围，这是兼容菜单协议的预期降级。
- 备份：`.bak/20260715_192144/phase3/`。
- 验证：扩展后的 `ScreenLayoutTest` 已通过；在已提交文本基线叠加本次 UI 变更的隔离副本中，完整 Gradle 构建于 1 分 40 秒通过，383 项测试全部成功。
- 当前共享树构建被 `文本材料/` 的并发改动阻断：技能统计从预期 346 变为 747，索引解析出现 `body.json.json`；这些外部文件未修改、未暂存、未回滚。
