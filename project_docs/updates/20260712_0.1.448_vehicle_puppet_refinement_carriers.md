# 0.1.448 Vehicle / puppet / refinement carriers

## Class
Resources (+ bulk JSON, lang, models/textures, id-map)

## Audit before
After 0.1.446/0.1.447 catalog closeout, a second-pass reaudit still found hard gaps outside the previously scanned herb/talisman/material sets:

- `formation_catalog` short ids (already had placeable `*_formation_core` carriers)
- `flight_vehicles` vehicle bodies / teleport ticket
- `puppet_definitions` puppet body carriers
- `refinement_system` anvil/bellows + high refinement manual

Shop/alchemy/vanilla recipe refs were already fully resolvable.

## Changes
- Appended 18 bulk carriers into `catalog_bulk_items.json` (total 717)
- Generated models/textures/lang for the 18 ids
- id-map:
  - 18 new self-mapped carriers
  - 18 formation short-id aliases → existing formation cores / teleport blocks
  - 7 soft aliases (`spirit_bellows`, `blank_jade_slip`, `demon_suppress_blank`, `war_token`, `yin_protection_charm`, `natal_artifact`, `anvil_core`)

## Post-audit
Scanned catalogs missing = 0  
`formation_catalog` / `flight_vehicles` / `puppet_definitions` / `artifacts` / `materials` / `manuals` / `consumables` / `currency` / `block_items` / `formation_items` / `refine_manual` / `refinement_system` all covered  
shop/alchemy/recipe `seeking_immortals:` refs missing = 0

## Protocol
13 unchanged

## Build
`./gradlew.bat --no-daemon --max-workers=1 build` → **BUILD SUCCESSFUL**  
`aiPreflight` recorded `mod_version=0.1.448`  
First test fail was pre-existing `canonical_type: effect` from 0.1.447 deferred tribulation entries; normalized to allowed `future_loader` for JsonSanity.

## Backup
`.bak/20260712_224513_wave448_remaining_carriers/`
