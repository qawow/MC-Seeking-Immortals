# `0.2.238` F-C3 丹炉显示与绘制层级收口

## 变更

- `AlchemyFurnaceMenu` 不再把空闲 `totalTicks=0` 改写为 1，新增 `isCrafting()` 明确区分空闲与炼制中。
- `AlchemyFurnaceScreen` 空闲时显示双语空闲态、0 进度和中性进度条；炼制中对剩余/总时长以及负值、超范围值安全钳制。
- 丹炉状态文字迁移到 `renderLabels`，让 `AbstractContainerScreen` 的槽位/携带物品 tooltip 在其后绘制。

## 验证

- `./gradlew test --tests com.xunxian.seekingimmortals.client.ScreenLayoutTest --tests com.xunxian.seekingimmortals.client.AlchemyFurnaceInteractionTest --no-daemon --max-workers=1 --console=plain`：通过。
- `python3 -B scripts/generate_authored_spell_effects.py` 后按顺序执行 `python3 -B scripts/generate_authored_visual_catalog.py`；两个 `--check` 通过，分别为 2,292 与 5,727 profiles。
- `./gradlew build --no-daemon --max-workers=1 --console=plain`：`BUILD SUCCESSFUL in 1m 35s`；完整测试 1,205 项，failure/error/skipped 均为 0；`:aiPreflight` 通过。
- JAR SHA-256：`c7f7197035b95c06f2b1023a60207e5c806b832f111de1cff61fa8e64af14029`。

## 版本与回滚

- 变更类别：代码、资源、测试、生成资源、版本和项目文档。
- `mod_version`：`0.2.237 -> 0.2.238`。
- `ModNetwork.PROTOCOL_VERSION`：保持 `30`；未改变网络包字段、顺序、类型、注册或频道行为。
- 备份：`.bak/20260729_0.2.238_f_c3/`。
- 真实客户端的空闲/开始/完成边界、进度条和槽位 tooltip 层级仍留在最终 QA。
