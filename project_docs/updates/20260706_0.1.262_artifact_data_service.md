# 2026-07-06 0.1.262 Artifact data service

- Change class: code/resources/data/tests/docs. This slice preserves concurrent `0.1.259` portal/formula work, `0.1.260` Paper Formula Scroll, and `0.1.261` Pressure Resist Pill formula work, then advances the artifact-focused implementation to `mod_version=0.1.262`.
- Backup path: `.bak/20260706_0.1.260_artifact_data_service/`.
- Text-material basis: all 315 files under `文本材料` were read and all 207 JSON files parsed successfully. Direct artifact sources include `ARTIFACTS_FANREN.md`, `REFINEMENT_ARTIFACTS_MASTER_LIST.md`, `artifacts_catalog.json`, `artifact_tier_rules.json`, `artifact_eleven_tier_map.json`, `refinement_recipes.json`, `flight_vehicles.json`, `talisman_treasure_templates.json`, `forge_artifact_priority.json`, and related auction/drop/faction indexes.
- Implementation: shipped 16 artifact/refinement JSON files under `data/seeking_immortals/artifacts/`, covering 217 artifact entries, 73 refinement recipes, 8 flight vehicles, 10 talisman-treasure templates, tier rules, ancient treasure indexes, realm drops, faction specialties, Wanbao auction lots, and Forge priority drafts.
- Runtime hook: added `ArtifactDataService` to load the shipped artifact data via the existing Gson/JsonParser stack. Existing `FlyingArtifactItem` remains backward compatible and now uses source ids `flying_sword_low` and `wind_escape_sail` to show catalog tier, realm, type, game tier, and refinement summary in tooltips.
- Tests: artifact resource JSON parsed successfully; focused `ArtifactDataServiceTest` and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 21s.
- Protocol decision: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields/order/encoding/registrations or channel behavior changed.
- Build: final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 25s and `aiPreflight` recorded `mod_version=0.1.262`; the inherited network-package warning was reviewed and this slice did not touch network files.
- Follow-up: register real P0/P1 artifact items, add refinement workstation gameplay, add Curios/equipment slots for artifact categories, implement talisman-treasure uses, connect Wanbao auction/appraisal services, and add dedicated item art.
