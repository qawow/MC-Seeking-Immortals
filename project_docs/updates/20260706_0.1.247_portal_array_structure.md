# 2026-07-06 0.1.247 Portal Array Structure

- Coordination: detected current `gradle.properties` at `0.1.246` after parallel Diyuan/pressure work and advanced this independent portal slice to `0.1.247` without reverting those changes.
- Planning: reused `SpiritGatheringArrayBlock`, `WorldpackGameplayService.usePortalArray`, existing lang files, and vanilla particles/sounds; no new packet, registry, route-data, block/item, GUI, or runtime loader was added.
- Gameplay: sneak-use portal validation now requires a 3x3 same-level Spirit Gathering Array base plus two clear air blocks above the center aperture. Incomplete portals report missing base and blocked aperture counts.
- Feedback: successful portal use keeps the existing server-authoritative travel route and plays portal/end-rod particles plus teleport/resonance sounds at both departure and arrival positions.
- Backup: source-slice backup is `.bak/20260706_0.1.237_portal_array_structure_reconcile/`; current reconciliation backup is `.bak/20260706_0.1.247_portal_array_structure_reconcile/`.
- Version/protocol: `mod_version=0.1.247`; `ModNetwork.PROTOCOL_VERSION=9` because packet fields/order/encoding/registrations and channel behavior were not changed.
- Verification: focused `PortalArrayStructureTest` passed with BUILD SUCCESSFUL in 14s. Full `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 20s and `aiPreflight` recorded `mod_version=0.1.247`.
- Follow-up: custom DimensionType tuning, permit/cost rules, generated gateway/destination structures, stronger frame variants, dedicated portal art, route UI/quest presentation, and in-game smoke checks remain deferred.
