# 0.2.150 作者术法视觉档案接入

## 本步完成

- 将 `文本材料/data/item_descriptions_v118-v122.json` 的术法视觉层编译为 344 条唯一运行时档案。
- 接入 v121 粒子组与拖尾引用：柔气、火星、水雾、木花粉、金属锐屑、土尘、雷弧、阴烟、魂絮，以及薄剑尾、重兵、符灰、身法风环等。
- 保留 v122 形状语言和 40 条“大招预兆”帧；有明确技能几何语义的护盾/领域等仍保留原语义，作者档案补充材质与拖尾。
- 将作者档案加载到 `TechniqueVfxOrchestrator`，通过 Lodestone 客户端四阶段时间轴渲染，并加入粒子子预算与低粒子档路径保护。
- 两个重复术法 ID 已固定决议：`inverse_star_veil` 采用 illusion 记录，`golden_armor_talisman_cast` 采用 talisman/metal_spark 记录；另合并无 `catalog_id` 的青元剑芒视觉专稿。
- `TechniqueVfxPacket` 增加粒子、拖尾和预兆字段，协议升至 `29`。

## 验证

- `python3 -B scripts/generate_authored_vfx_profiles.py --check`：通过，344 条。
- 定向 VFX、协议和多人契约测试：通过。
- `./gradlew build --no-daemon --max-workers=1`：通过。

## 范围说明

当前 344 条是作者逐帧术法档案；其余运行时术法继续使用现有语义回退映射。法宝、丹药、灵兽和环境视觉资料将在后续批次按同一档案管线接入。
