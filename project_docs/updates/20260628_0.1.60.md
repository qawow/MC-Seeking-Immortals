# 2026-06-28 0.1.60

## 变更类型

- Code / gameplay / docs。
- `mod_version`：`0.1.58` -> `0.1.60`。
- 网络协议：未修改 packet 字段、顺序、编码或通道行为，`ModNetwork.PROTOCOL_VERSION` 维持 `5`。

## 完成内容

- 将境界派生数值集中到 `RealmStageConfig` 的百分比成长公式。
- `Realm#getBaseMaxSpiritualPower` 与 `Realm#getStageExpSpan` 改为读取统一曲线。
- `PlayerCultivation` 的最大灵力、神识、生命、恢复、修为获取、飞行速度、攻击、防御、暴击、闪避、法抗等派生值改为按境界倍率和阶段倍率计算。
- `CombatStats` 改为使用同一套境界百分比曲线。
- 打坐状态提示的下一阶段修为值改为读取当前阶段上限，避免高境界/多阶段时显示偏差。
- 更新 Phase 1 测试中的基础数值预期。

## 备份

- `.bak/20260628_0.1.59_realm_percentage_stats/`

## 验证结果

- `./gradlew --no-daemon --max-workers=1 build` 成功：`BUILD SUCCESSFUL in 38s`。
- 后续建议在真实游戏内观察高境界飞行速度、PvP 伤害和修为进度手感，再微调倍率常量。
