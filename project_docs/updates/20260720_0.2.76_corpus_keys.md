# 0.2.76 术法 corpus 字段清点（master_plan P0-4）

## 清点结论
- 747 条 M02 术法中 26 条携带显式 `effect_key`（18 个去重值），其余 721 条按 `type/damage/range/target/tags` 生成运行时（0.2.53/0.2.54 既有语义，无缺口）。
- 18 个 effect_key 逐一对账 `TalismanConsumeSpell` 关键词路由，发现两处路由缺陷并修复。

## 修复
- `mask_qi`：movement 模式此前只识别 invis/hide/ghost，掩气符移动后**不隐身**；关键词补 `mask`。
- `local_spirit_boost`：blob 不含既有 buff 关键词，聚灵类落入默认伤害 AOE；BUFF 分支补 `spirit`/`boost`。
- `resolveMode` 抽出静态 `classifyMode` 供审计测试复用。

## 测试
- 新增 `TalismanConsumeSpellCorpusTest`：
  - 12 个语义关键 key 的显式模式断言；
  - 遍历 `text_material/techniques/*.json` 全部 effect_key，断言除通用标记/显式 AOE 外不落默认 AOE。

## 权威与兼容
- 无网络字段变化，protocol 25。未新增注册对象。

## 验证
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 739 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.76`。

## 备份
- `.bak/20260720_120500_0.2.76_corpus_keys/`

## 后续（master_plan 路线）
- P0 全部完成；下一批 P1-5 秘境内容充实。
