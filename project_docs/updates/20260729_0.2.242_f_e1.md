# 2026-07-29 `0.2.242` F-E1 秘境入口反馈收口

## 范围

- 完成 `implementation_plan.md` 的 UI-20：秘境行不再显示无反馈的禁用按钮。
- 有服务端验证锚点时显示“定位界门”；无可用入口时显示“查看条件”；玩家已有秘境会显示状态且不发送进入请求。
- 复用 `WorldpackActionPacket` 的既有 `action/targetId` 字段，未改变网络格式。

## 实现

- `WorldpackScreen` 依据同步快照选择 `LOCATE_GATE`、`VIEW_CONDITIONS` 或 `IN_PROGRESS` 三态。
- `WorldpackGameplayService` 新增信息型 `locate`/`conditions` 动作：服务端重新解析内建秘境、所属地域和锚点；定位只返回真实维度/坐标，条件反馈按区域、境界、开放周期/剧情旗标、冷却、锚点和凭证顺序返回首项阻断原因。
- `anchorReady` 同时验证目标维度存在，失效锚点不会被客户端标为可定位。
- 补充中英文界面和反馈文案，保留 `enter` 动作的物理界门/服务端剧情门禁。

## 验证

- 定向测试：`ClientWorldpackDataTest`、`SecretRealmEntryAuthorityTest`、`MarketWorldpackPacketTest`。
- 生成器：`authored_spell_effects` `2,292` profiles、`authored_visual_catalog` `5,727` profiles，按 `spell -> visual` 顺序刷新并通过 `--check`。
- 普通构建：`./gradlew build --no-daemon --max-workers=1 --console=plain`，`BUILD SUCCESSFUL`；完整测试 `1,211` 项，failure/error/skipped 均为 `0`。
- JAR SHA-256：`00b6647d35380694eea2cf834d58a946a626396cba71161ef2adbb406c2f825b`。

## 版本与回滚

- `mod_version` 最终为 `0.2.242`。首次 `0.2.241` 构建后收紧锚点校验触发版本指纹门禁，因此按规则再次递增；未使用版本门禁跳过参数。
- `ModNetwork.PROTOCOL_VERSION=30` 保持不变：仅复用既有字符串字段，未改字段、顺序、类型、注册或频道方向。
- 回滚目录：`.bak/20260729_0.2.241_f_e1/`。
- 下一代码批：独立协议批次 F-E2/UI-22，删除不可达旧屏并提升协议；所有前端批次仍需最终真实客户端 QA。
- 本批提交：`a9b10642 fix: 收口秘境入口引导反馈`。
