# 2026-07-06 0.1.169 Spirit event hooks

- Change class: shipped worldpack daily-event data/test/docs/version metadata.
- Text-material source: `daily_random_events.json#ghost_expose_rumor`, `#spirit_storm_major`, and `#ancient_ruin_whisper`.
- Implemented: `ghost_expose_rumor` under `tiannan`, `spirit_storm_major` under `spirit_realm_border`, and `ancient_ruin_whisper` under `spirit_fengyuan`, using existing safe effect-token approximations only.
- No new item/block registries, textures, models, recipes, loot tables, shop data, alchemy data, production Java gameplay, GUI assets, packet fields/order/encoding/registrations, regions, secret realms, or runtime loaders were added.
- Version/protocol: `mod_version=0.1.169`; `ModNetwork.PROTOCOL_VERSION` remains `9`.
- Backup path: `.bak/20260706_0.1.169_spirit_event_hooks/`.
- Validation: changed worldpack JSON parsed successfully; focused `WorldpackDataServiceTest`, `JsonSanityTest`, and `ResourceJsonParseTest` passed with BUILD SUCCESSFUL in 30s after wrapper network/cache approval.
- Final build: `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 25s; `aiPreflight` recorded `mod_version=0.1.169`.
- Remaining work: real ghost-hunt risk, Spirit Realm storm hazards, ancient-ruin quest routing, realm-specific gates, encounter spawns, UI hints, and source-accurate reward tables.
