# 0.2.142 任务手册与原生/FTB 同步

## 完成内容

- 对齐 62 条原生任务链、35 条作者任务线和 7 个主线章节的阶段数、区域、起始区域与跨引用；修正运行时目录和 `text_material/quest_chains.json` 中的 14 个区域值。
- 将 FTB 九章 215 个节点中的旧 `checkmark` 占位替换为真实物品、击杀、进度、维度、自定义或原生阶段镜像任务；原生镜像共 123 个，显式原生单步回写共 15 个。
- 让原生任务账本保持唯一权威：FTB 回写只能经原生 `start/advance`，未知链/阶段、损坏标签、多目标回写和越级推进失败关闭；奖励继续使用原生一次性账本。
- Patchouli 手册升级为双语 104 条目、书籍版本 4；任务系统页解释原生权威、FTB 可选兼容、分支锁、奖励归属和七章主线。English 条目 CJK 残留为 0。

## 验证与版本

- 定向任务、FTB、Patchouli parity 测试及全量 JSON/SNBT 静态审计均通过；最终普通 `./gradlew build` 在 `1m11s` 内 `BUILD SUCCESSFUL`，`aiPreflight` 通过。
- `mod_version=0.2.142`。本批未改变网络包字段、顺序、类型或频道行为，沿用共享树 `ModNetwork.PROTOCOL_VERSION=28`。
- 回滚目录：`.bak/20260722_quest_manual_sync/`、`.bak/20260722_094500_patchouli_audit/`、`.bak/20260722_handbook_en_cjk_cleanup/`、`.bak/20260722_140302_ftb_native_quest_sync/`。
