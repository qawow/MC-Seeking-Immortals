# 0.2.100 法宝 binds 消费与空 matrix 层数

## 摘要

1. 6 条 catalog `binds` 在激活时消费：vehicle_key 登乘对应灵舟/云轿/御风车；quest_key 进入虚天殿/血色禁地或记录大晋拍卖声望。
2. 14 条 `total_layers=0` 的 method matrix 改为显式层：生活/宗门基础 1 层，通修 generic 9 层；`cultivation_methods.setting.layers_max` 同步。

## 变更

- `ArtifactActivationService.applyVehicle` / `applyUtility` + `resolveVehicleBind` / `applyQuestBind`
- `method_layer_technique_matrix_v130.json`、`cultivation_methods.json`
- 双语 binds 消息
- `ArtifactBindsAndMethodMatrixTest`

## 验证

- `./gradlew build --no-daemon` 成功；preflight `mod_version=0.2.100`
- 协议 26 不变

## 备份

`.bak/20260720_210000_0.2.100_binds_matrix/`

## 后续

非代码：美术/GeckoLib、实机烟测、多人复签。可选：丹药分条保真、generic 术法表现深度。
