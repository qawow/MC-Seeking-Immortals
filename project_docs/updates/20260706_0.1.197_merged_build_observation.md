# 2026-07-06 0.1.197 Merged Build Observation

- Change class: docs-only build/version observation after parallel work.
- Current state: `gradle.properties` is `mod_version=0.1.197`; `ModNetwork.PROTOCOL_VERSION` remains `9`.
- Preserved content: the 0.1.196 Barbarian King territory secret-realm hooks remain present, and the parallel remaining daily-random exact-id aggregate remains present.
- Build: final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 21s and `aiPreflight` recorded `mod_version=0.1.197`.
- Backup path: `.bak/20260706_0.1.197_merged_build_observation/`.
- Protocol: unchanged. This observation changed no packet fields/order/encoding/registrations.
