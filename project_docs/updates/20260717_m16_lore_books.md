# 2026-07-17 M16 图鉴与百科落地

## 变更摘要

落地只读展示层：Patchouli 词条扩量（47→87×双语言、8 分类）、术语/数值/视觉语料运行时服务、妖兽图鉴与编年史阅读屏、`SettingCatalogSummaryService` 全类目摘要、lang 对账与 Patchouli 可选守卫复查。

## 实现要点

1. **Patchouli**
   - 分类：basics / techniques / alchemy_pills / artifacts_formations / beasts_puppets / sects_factions / world_realms / reference
   - 新词条覆盖功法/丹药/法宝/妖兽/宗门/秘境/维度/术语/数值/视觉/编年与语料 static 缺口
   - zh_cn / en_us 词条文件名完全对齐；图标均使用已注册 `seeking_immortals:*` 物品 id

2. **语料运行时**
   - 发布：`name_alias_glossary_v103` / `numeric_overview_v103` / `visual_style_v118` / `visual_look_cards_v122` / `chaotic_sea_lore_compendium` → `text_material/`
   - `NameAliasGlossaryService`：primary/alias 检索 + 与 `ItemCatalogService` 软一致性校验
   - `NumericOverviewService` / `VisualStyleService`：百科速查
   - `LoreCompendiumService`：全类目 hub + tooltip 查询

3. **阅读界面**
   - `BestiaryScreen`：解锁驱动可见性（锁态/详态）
   - `ChronicleScreen`：编年事件 + 时间线阶段
   - `LoreCompendiumScreen`：总览/术语/数值/视觉
   - 命令 `/seeking_immortals lore ...` 与未绑定快捷键入口

4. **同步（只读）**
   - `SyncLoreUnlockPacket` 同步 bestiary/chronicle/timeline 解锁集合
   - `LoreScreenActionPacket` 请求打开界面
   - 登录 `LoreSyncService.syncOnly`；**不写**玩法状态

5. **摘要 / 守卫 / lang**
   - `SettingCatalogSummaryService` 扩全类目 + `categoryCount` 供 HUD/tooltip
   - `PatchouliGuideBridge.getBookStackSafe` + `ModCompat.PATCHOULI_LOADED`
   - zh/en 4510 keys 对齐；`scripts/m16_lang_audit.py`

6. **测试**
   - `M16LoreCorpusTest` + `PatchouliGuideParityTest` + 既有 summary 测试

## 验证

- 全量：`bash ./gradlew.unix --no-daemon build -PaiSkipVersionBumpCheck=true` **BUILD SUCCESSFUL**
- lang audit：`python3 scripts/m16_lang_audit.py` → zh_only=0 en_only=0

## 版本与协议

- `mod_version`：任务红线不升（保持 0.1.509）
- `ModNetwork.PROTOCOL_VERSION`：保持 22（仅追加新包，无既有字段变更）

## 备份

`.bak/20260717_051756_m16_lore_books/`
