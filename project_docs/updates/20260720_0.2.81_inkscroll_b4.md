# 0.2.81 云笈墨卷 B4 HUD 覆盖层（前端重构批次 5/7）

## 本批完成
- 4 个 HUD 覆盖层核验通过云笈墨卷取色（JADE_SLIP→QUIET_STUDY 映射自动生效，无需改覆盖层代码）。
- 数字/格式化收拢：
  - `CultivationHealthOverlay.formatValue` 大数分支改 `NumberFmt.cjk`（保留 <100 一位小数血量语义），删除私有 `unitNumber`；
  - `CultivationHudOverlay.clamp01`、`BreathingHudOverlay.format` 委托 `NumberFmt`；清无用 import。
- 契约核验：`skill_bar_frame` 全客户端零引用（0.1.31 无框约定保持）；`ImmortalHudLayoutTest` 布局数学未动。

## 验证
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 748 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.81`；protocol 25。

## 备份
- `.bak/20260720_150000_0.2.81_inkscroll_b4/`

## 后续
B5 文案键化（~163 处硬编码中文 → 双语键）。
