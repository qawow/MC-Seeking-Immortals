# 0.2.67 多方块材料价带碎片回退

## 本批完成

### 材料解析回退
- `MultiblockMaterialCatalog.shardFallbackCost(stationId)`：按结构材料价表 `minPrice` 汇总，并钳制到 128。
- `overhaul` 在可解析结构材料为空时，把价带税加到灵石碎片附加费；
  有可解析材料时仍优先扣具体物品。

### 测试
- 覆盖 `shardFallbackCost` 正值/未知结构 0；
- overhaul 源码契约要求使用 `shardFallbackCost`。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体。

## 验证
- 定向 `MultiblockOperationalServiceTest` 通过。
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 724 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.67`。

## 备份
- `.bak/20260720_100120_0.2.67_material_fallback/`

## 后续
1. 更多材料 id 与 bulk/alias 精确对齐（减少价带税依赖）。
2. 实机客户端/专服/多人烟测。
