# 0.2.75 炼丹爆炸联动工站耐久（master_plan P0-2/P0-3）

## 本批完成

### P0-3 损耗循环
- `AlchemyFurnaceBlockEntity.damageStationState`：按当前炉阶定位 `alchemy_furnace_g{1..5}` 运行态并 `applyDamage`。
- 接入点：
  - 炼制判定爆炸（finishCraft roll < explosionChance）→ 80 点重损；
  - 火超炉阶 `explodeFurnace` → 80 点重损；
  - 火超盖阶 `blowLid` → 30 点中损；
  - 炼败出废丹 → 8 点轻损。
- 与 0.2.65 修理/0.2.66 大修事务闭环：损耗→效率下降（0.2.69 成功率随之下降）→修理/大修。

### P0-2 材料税审计（零改动收口）
- 全 245 条结构材料 id 经 alias 链 + bulk 载体 + 注册物品解析，0 条未解析；
- `unresolvedShardTax` 当前对所有 authored 工站为 0，价带税仅在未来新增未解析 id 时生效。审计结论记录于此，不需要代码变更。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体。

## 验证
- 新增源码契约 `alchemyFailuresWearStationInSource`（80/30/8 与 applyDamage 路由）。
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 737 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.75`。

## 备份
- `.bak/20260720_114500_0.2.75_explosion_durability/`

## 后续（master_plan 路线）
1. P0-4 术法 corpus 未消费字段清点。
2. P1 秘境内容充实。
