# 2026-07-06 0.1.192 Tianlan Scout-Clash Daily-Event Backfill

- Change class: worldpack data/test/docs/version metadata.
- Source: `text-material/data/daily_random_events.json#mulan_scout_clash`.
- Implementation: preserved the existing `mulan_scout_clash` event under `mulan_grassland` and added the Tianlan-side shipped hook `tianlan_mulan_scout_clash` under existing region `tianlan`, reusing the current worldpack daily-event schema, source weight 9, 18000 ticks, and existing `trade_risk_up` plus `rare_loot_hint` effect tokens.
- Prerequisite reuse: uses the existing worldpack data service, command/GUI daily-event flow, and JUnit validation path; no new runtime loader or registry surface was introduced.
- Coordination: starts from the build-verified `0.1.191` Kunwu ancient-ruins workspace and does not alter that event or its Dajin compatibility/source-side data.
- Protocol: `ModNetwork.PROTOCOL_VERSION` remains `9`; no packet fields, order, encoding, registrations, or channel behavior changed.
- Backup path: `.bak/20260706_0.1.192_tianlan_scout_clash_event/`.
- Verification: changed worldpack JSON parsed successfully with 111 daily events; final `./gradlew --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 21s and `aiPreflight` recorded `mod_version=0.1.192`.
