# 2026-07-03 0.1.79 Right-top HUD Overflow Fix

## Summary

- Fixed the top-right cultivation HUD showing incomplete information when the scaled screen is narrow, GUI Scale is high, or high-realm values become large.
- The HUD panel now sizes itself from the visible content and clamps inside the screen.
- Cultivation, mana, divine sense, qi-deviation risk, gold-core/five-elements, and active tribulation progress remain part of the compact HUD layout.

## Technical Notes

- `CultivationHudOverlay` keeps the fix client-side only.
- Large numbers now use compact Chinese units (`万`/`亿`/`兆`).
- `mod_version` is now `0.1.79`.
- `ModNetwork.PROTOCOL_VERSION` remains `7`; no packet format changed.

## Verification

- Final build command: `./gradlew --no-daemon --max-workers=1 build`.
- Result: `BUILD SUCCESSFUL in 34s`.
- Forced verification command: `./gradlew --no-daemon --max-workers=1 build --rerun-tasks`.
- Forced verification result: `BUILD SUCCESSFUL in 50s`.
- Manual follow-up: verify GUI Scale 1/2/3/Auto and high-realm debug values in `runClientNoPatchouli`.

## Backup

- Existing edited files were backed up to `.bak/20260703_0.1.79_hud_overflow/`.
