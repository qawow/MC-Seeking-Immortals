# 2026-07-06 0.1.280 Ice/Thunder Custom Spell Visuals

- Change class: code/resources/gameplay behavior/docs.
- Source basis: current shipped `qi_refining_techniques.json` ids `ice_cone_art`, `ice_freezing_art`, and `thunder_strike_art`, plus the user constraint to avoid vanilla fireball/fire-charge/lightning-entity spell effects where practical.
- Implementation: added `CultivationFireballEntity.SpellElement.ICE`; moved `IceConeSpell` / `ICE_FREEZING` to the custom elemental projectile path; rewrote `ThunderStrikeSpell` to use server-side ray/area magic damage and custom blue-white DustParticle thunder-column visuals instead of creating `LightningBolt`.
- Version/protocol: `mod_version=0.1.280`; `ModNetwork.PROTOCOL_VERSION=9` unchanged because no packet schema or channel behavior changed.
- Backup: `.bak/20260706_0.1.279_ice_thunder_custom_spells/`, with `post_parallel_0.1.279/` and `pre_docs_0.1.280/` sub-backups.
- Verification: zh_cn/en_us lang JSON parsed with explicit UTF-8; `.\gradlew.bat --no-daemon --max-workers=1 compileJava --rerun-tasks` passed; final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 25s and `aiPreflight` recorded `mod_version=0.1.280`.
- Current-tree recheck: parallel shippable changes advanced the shared tree after that build; current `mod_version=0.1.281` also passed `.\gradlew.bat --no-daemon --max-workers=1 build` with BUILD SUCCESSFUL in 25s. Protocol remains 9.
- Remaining risk: no live client smoke test was run, so exact in-world particle framing, sound feel, PvP balance, and long-session combat feel still need manual play checks.
