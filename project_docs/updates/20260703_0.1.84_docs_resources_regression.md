# 2026-07-03 0.1.84 docs/resources/regression pending note

## Status

- 0.1.84 is pending until the main/coordinator build passes and records the final version/protocol truth.
- Last confirmed built truth observed by Worker E: `mod_version=0.1.83`, `ModNetwork.PROTOCOL_VERSION=8`.
- Worker E did docs-only coordination for the 0.1.84 wave.

## Worker E scope

- Updated project documentation to mark 0.1.84 as pending rather than complete.
- Added `project_docs/manual_regression_0.1.84.md`.
- Did not edit `gradle.properties`, `ModNetwork`, Java code, data packs, assets, resources, or build logic.
- Did not run or claim the final build.

## Resource validation draft

- Parse all shipped JSON resources after other workers settle.
- Check item/block models reference existing textures.
- Check zh_cn/en_us localization for new visible entries, commands, screens, tooltips, JEI/Patchouli labels, and entities.
- Confirm no active `xiuxian:` namespace references remain in shipped resources.
- Confirm Patchouli remains optional and no-Patchouli client startup is still viable.
- Confirm JEI alchemy category still displays shipped recipes and does not claim unsynced datapack recipes.
- Confirm new blocks, if any, have loot tables and mining tags when survival-obtainable.

## Manual regression pointer

- Use `project_docs/manual_regression_0.1.84.md`.
- Keep manual results pending until run in a real client/server environment.

## Build/protocol decision

- Protocol remains `8` unless another 0.1.84 worker changes packet wire format, registration, or channel compatibility.
- Final build result is pending and must be filled by the coordinator/main worker.

## Backup

- Existing edited docs were backed up to `.bak/20260703_0.1.84_worker_e_docs/`.