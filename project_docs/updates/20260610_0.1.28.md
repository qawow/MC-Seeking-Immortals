# 2026-06-10 0.1.28 旧第三方 UI UI/图形稳定性修复

## 修复内容

- 按 旧第三方 UI Agent 指南复查当前 UI：优先稳定 Screen/Menu 生命周期、Overlay/HUD 显示条件、布局、样式隔离与同步状态，不进行无必要的大规模 组件 树重构。
- 修复 `CultivationStatsScreen` 中 `PANEL_WIDTH` 固定宽度残留：在极窄屏或高 GUI Scale 下，修仙页面板现在会按可用宽度收缩。
- 分页按钮、返回按钮、灵力状态条与灵根纯度条改为跟随当前面板宽度布局，降低越界、按钮挤出和状态条覆盖风险。
- 保留当前稳定默认/占位皮肤，不替用户决定最终 UI 样式。

## 验证

- 已执行 `./gradlew build` 并通过。
- 产物：`build/libs/seeking_immortals-0.1.28.jar`。

## 仍需人工游戏内验证

- GUI Scale 1/2/3/Auto 下打开原版背包与“修仙”页，确认分页按钮、返回按钮、灵力条不越界。
- 低分辨率/窄窗口、宽屏、JEI/XEI 同屏场景下确认修仙页不遮挡原版槽位或被配方面板严重遮挡。
- 登录、退出、死亡重生、切维度后确认同步占位、技能栏 7 槽、吐纳 HUD 状态稳定。
- 技能 tooltip 与 旧第三方 UI 占位皮肤无透明度/颜色污染。
