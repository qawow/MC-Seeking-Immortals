# 2026-07-06 - 0.1.245 current-tree build recheck

Change class: docs/build-verification reconciliation only.

Context:
- After the 0.1.244 Pressure Resist Pill item slice, the shared workspace advanced to `mod_version=0.1.245`.
- The parallel state makes `seeking_immortals:diyuan_permit` the consumed `ticket_item` for worldpack secret realm `diyuan` through the existing secret-realm ticket path.
- This thread preserved that parallel state and did not revert it.

Verification:
- `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 23s.
- `aiPreflight` recorded `mod_version=0.1.245`.

Protocol: unchanged at `ModNetwork.PROTOCOL_VERSION=9`; this thread did not touch packet fields/order/encoding/registrations or channel behavior.

Backups:
- Recheck docs: `.bak/20260706_0.1.245_current_tree_build_recheck/`
- 0.1.244 item slice: `.bak/20260706_0.1.244_pressure_resist_pill_item/`

Next item-focused candidates:
- `fengyuan_clan_ginseng`
- `spirit_realm_condense_pill`
