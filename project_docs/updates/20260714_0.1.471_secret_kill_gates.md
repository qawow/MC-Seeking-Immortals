# 0.1.471 secret-realm kill gates

## Change class
Code + lang + docs. `mod_version` 0.1.470 → 0.1.471. Protocol remains 13.

## Backup
`.bak/20260714_001821_0.1.471_secret_kill_gates/`

## Implemented
1. **SecretRealmTrialService**
   - No rare proxy on enter.
   - Mid/core chests sealed empty until kill.
   - Patrol/guardian tagged with persistent trial NBT.
   - `onTrialMobKilled`: patrol → mid unlock; guardian → core unlock + rare proxy.
2. **BossEncounterService**
   - No pre-placed boss cache.
   - Boss tagged; `onBossKilled` places cache chest + bonus shards.
3. **ModEvents.onLivingDeath**
   - Routes mob kills by player to trial/boss handlers.

## Build
BUILD SUCCESSFUL; jar `seeking_immortals-0.1.471.jar`.

## Remaining deferred
Art, biomes, GeckoLib, human live-smoke re-sign.
