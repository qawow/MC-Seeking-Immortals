# 0.1.422 Text-material dual-track wave41

Date: 2026-07-12

## Scope
Hard-gate spatial node requires (item/realm) and mid-stage quest rewards.

## Changes
- `SpatialNodeRequiresService`: maps catalog requires to items/realms; soft rep/event tags warn only
- Command spatial travel enforces requires before routing
- Placeable specialized gates enforce first matching node-type requires
- Avoid double-charge: command path calls gate methods with `enforceRequires=false` after node-level enforce
- Quest mid-stage soft reward at half-chain milestone (2 shards, once)

## AI defaults
- Item + realm hard gates: yes
- Reputation/event OR tags without item mapping: soft warn only
- Creative/instabuild bypasses item consume

## Version / protocol
- mod_version 0.1.422
- PROTOCOL_VERSION remains 9

## Backup
- `.bak/20260712_wave41_spatial_requires/`
