# 2026-07-06 0.1.289 Contribution shop rank gates

- Change class: code/resource/data/docs; final `mod_version=0.1.289`.
- Text-material basis: `文本材料/data/sect_contribution_shop.json` `rank_min` / exchange gates for implemented Huangfeng/Qinglan, Danxia, Yanyue, and Luoyun contribution-shop entries.
- Runtime: `ShopService` parses optional `rank_min`, normalizes `outer_disciple`, `inner_disciple`, and `core_disciple`, and rejects gated sect-contribution purchases before stock reservation or contribution spending when the current `QuestProgress` sect stage is too low.
- Data: Qinglan/Danxia `foundation_pill` and `recipe_ningshen` require inner disciple; Qinglan/Danxia `flying_sword_low` requires outer disciple; Yanyue `foundation_pill` requires inner disciple; Luoyun `recipe_luoyun_spirit` requires inner disciple; `artifact_repair_kit` remains ungated.
- Version/protocol: `ModNetwork.PROTOCOL_VERSION=9` unchanged; no packet fields/order/encoding/registrations, `SyncSectDataPacket` shape, SimpleChannel behavior, or player NBT schema changed.
- Backups: `.bak/20260706_0.1.287_shop_rank_gates/`, `.bak/20260706_0.1.288_shop_rank_gates_post_build_docs/`, and `.bak/20260706_0.1.289_shop_rank_gates_version_reconcile/`.
- Verification: focused `ShopServiceTest` + `JsonSanityTest` passed with BUILD SUCCESSFUL in 37s; a post-doc/resource build at `0.1.288` correctly failed `aiPreflight` because `text_material_id_map.json` changed after the previous successful fingerprint; after advancing to `0.1.289`, final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 32s and `aiPreflight` recorded `mod_version=0.1.289`.
- Deferred: realm/reputation/monthly/per-player gates, UI lock presentation, stricter alchemy privilege semantics, NPC/quest placement, and broader source-accurate contribution-shop stock routing.
