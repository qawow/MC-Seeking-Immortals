# 0.2.84 P1 秘境内容闭环

## 本批完成

- 对账 `SecretRealmCatalogService` 的 19 条作者深潜秘境：原有分层试炼壳覆盖完整，但 11 个已列 Boss 没有奖励表，灵缈园与七脉试炼洞没有 Boss。
- 灵缈园新增 `garden_heart_warden`，并把园心层的“无固定大BOSS”运行态占位替换为真实守关者。
- 七脉试炼洞把已有 `meridian_guardian` 生成项提升为目录 Boss。
- 为 `ice_fire_demon`、`sealed_demon_lord`、`diyuan_guardian`、`stone_spirit_puppet`、`peak_trial_spirit`、`puppet_guardian`、`ghost_cultivator`、`ruin_stone_guardian`、`mulan_fashi_captain`、`abyss_jiao`、`yin_corpses_general` 及两个新 Boss 补齐源语料与运行时奖励表。
- 19/19 秘境现均形成“分层 trial -> 命名 Boss -> 非空奖励表”闭环；23 个作者 Boss 全部可被 `BossEncounterService` 识别并走原有 owner/session/encounter、首次清除与 outbox 权威链。

## 权威与兼容

- 没有新增网络字段、包顺序、注册或持久 schema；`ModNetwork.PROTOCOL_VERSION` 保持 `25`。
- 奖励继续由 `BossLootService` 过滤唯一物、区分首次/重复清除并经 `InventoryDeliveryService.giveOrEnqueue` 交付。
- 新 Boss 复用现有服务端试炼战斗壳；未伪造独立实体类型或未完成的专属美术。

## 验证

- JSON 四文件解析成功。
- 定向：`./gradlew test --tests 'com.xunxian.seekingimmortals.worldpack.SecretRealmM09ServiceTest' --no-daemon`，成功。
- 普通：`./gradlew build --no-daemon`，BUILD SUCCESSFUL；全量 752 项，0 failures/errors/skipped。
- `runClient`：首次沙箱内因 Gradle lock 只读失败；获批后在沙箱外启动成功，完成资源重载、注册表冻结、Sound Engine 与纹理图集创建后主动 Ctrl-C。日志仅有当前 Linux 环境缺失 `flite` 导致 narrator 初始化失败，无寻仙问道资源加载错误。

## 版本、备份与后续

- `mod_version`: `0.2.83 -> 0.2.84`。
- 协议：保持 `25`。
- 回滚：`.bak/20260720_0.2.84_secret_realm_content/`。
- 下一批：master_plan P1 宗门专属内容；其后 NPC 对话树、灵兽/傀儡成长。
- 实机遗留：仍需逐秘境击杀/开箱、首次与重复掉落、断线重连、专服和多人归属烟测。
