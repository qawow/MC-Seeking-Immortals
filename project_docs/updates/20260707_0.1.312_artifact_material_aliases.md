# 2026-07-07 - 0.1.312 Artifact material aliases

- Change class: data/test/docs; implemented as the `0.1.312` cooperative artifact-refinement mapping slice.
- Source basis: full `文本材料` corpus scan remains complete at 315 files / 207 parsed JSON files; direct basis was `materials_catalog.json` entries `demon_core_low`, `demon_core_high`, and `talisman_paper`, plus shipped `artifacts/refinement_recipes.json` consumers such as `refine_yellow_umbrella`, `refine_hunyuan_bowl`, and `refine_talisman_soul_charm`.
- Implementation: `text_material_id_map.json` now maps low/high demon-core source ids to the existing `seeking_immortals:beast_core` carrier and generic `talisman_paper` to existing `seeking_immortals:talisman_paper_mortal`; no new item ids or runtime loaders were added.
- Tests: `ArtifactRefinementServiceTest` now verifies exact command-refinement plans for low/high demon-core aliases and talisman-paper alias resolution.
- Version/protocol: advanced `mod_version` to `0.1.312`; `ModNetwork.PROTOCOL_VERSION` remains `9` because no packet fields/order/encoding/registrations or SimpleChannel behavior changed.
- Backups: `.bak/20260707_0.1.312_artifact_material_aliases/`.
- Verification: changed JSON parsed successfully; focused `ArtifactRefinementServiceTest`, `ResourceJsonParseTest`, and `JsonSanityTest` passed with BUILD SUCCESSFUL in 24s. Final `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 23s and `aiPreflight` recorded `mod_version=0.1.312`; inherited network-package warning reviewed as unrelated to this mapping slice.
