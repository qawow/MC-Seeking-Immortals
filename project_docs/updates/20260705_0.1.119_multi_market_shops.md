# 2026-07-05 0.1.119 multi-market text-material shops

- Change class: code, data resources, localization, tests, and docs.
- Coordination: another Codex had already completed `0.1.118` batches for Qinglan contribution backfill and market demon-core fragment backfill, so this multi-market batch was advanced to `mod_version=0.1.119` instead of reusing `0.1.118`.
- Scope: extended the existing item-currency market flow from the single `market_herbal_stall` to a small whitelist of market shops without changing packet fields, field order, encoding, packet registrations, or channel compatibility.
- Added shipped shops:
  - `chaotic_sea_island_general`, importing implemented text-material equivalents for `spirit_recovery_pill` and `soul_gathering_pill`.
  - `inverse_star_black_market`, importing implemented text-material equivalent `demon_core_mid`.
- Server validation: client-provided `shopId` is accepted only when it is one of the known item-currency market shops; unknown shops fall back to the default market sync after an error message.
- Command compatibility: old `/seeking_immortals market open|list|buy <entry>` behavior remains defaulted to `market_herbal_stall`; new forms accept `/market open <shopId>`, `/market list <shopId>`, and `/market buy <shopId> <entry>`.
- Client compatibility: `ShopScreen` refresh now sends the currently synced shop id instead of hardcoding the default herbal stall.
- Canonical map: added explicit `soul_gathering_pill` mapping and recorded the two new partial shop mappings; `demon_core_mid` now includes `merchant_shops.json` as a source.
- Version/protocol: `mod_version=0.1.119`; `ModNetwork.PROTOCOL_VERSION=9` remains unchanged.
- Backup paths: `.bak/20260705_0.1.118_multi_market_shops/` and `.bak/20260705_0.1.119_multi_market_shops_reconcile/`.
- Focused validation: `.\gradlew.bat --no-daemon --max-workers=1 test --tests com.xunxian.seekingimmortals.shop.ShopServiceTest --tests com.xunxian.seekingimmortals.resources.JsonSanityTest --tests com.xunxian.seekingimmortals.resources.ResourceJsonParseTest` passed after version reconciliation with BUILD SUCCESSFUL in 25s after wrapper network/cache approval.
- Final build: `.\gradlew.bat --no-daemon --max-workers=1 build` passed with BUILD SUCCESSFUL in 36s; aiPreflight recorded `mod_version=0.1.119`.
- Continuing verification observed one transient `reobfJar` short-copy failure (`output.jar` expected length 161, actual 0); immediate rerun passed with BUILD SUCCESSFUL in 31s and `aiPreflight` again recorded `mod_version=0.1.119`.
- Protocol decision: the inherited network-package warning was reviewed. This pass changed shop service/commands/client refresh plus shop/reference JSON, localization, tests, docs, and version metadata; it did not change packet fields/order/encoding/registrations/channel compatibility, so `ModNetwork.PROTOCOL_VERSION` remains `9`.
- Remaining risk: manual in-game checks are still needed for opening each market shop through commands, GUI refresh on non-default shops, stock refresh timing, inventory-full fallback drops, and balance of high source prices against low-spirit-stone currency.
