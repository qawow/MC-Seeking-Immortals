# 0.2.61 任务链推进权限收口

## 本批完成

### 未启动不可 advance
- `TextQuestChainService.advance` 在 `stage <= 0` 时 fail-closed，提示 `text_quest.not_started`。
- 不再把任意 advance 当作隐式 start，接取门槛（境界/区域/宗门）必须走 `start`。

### 当前步骤 hook 匹配
- 新增 `expectedHookForStage` / `matchesCurrentStepHook`。
- `QuestHookRuntime.tryAdvanceByHook` 把触发 hook 传入 `tryAdvanceActive`；仅当 hook 等于当前 stage 的 authored step hook 才推进。
- 无 authored hook 的阶段仍允许对话/turnin 推进。

### 分支声望防刷
- `chooseBranch` 对相同分支再选直接拒绝，不重复 `ReputationService.add`。

### 本地化
- 中英文新增 `text_quest.not_started` 与 `text_quest.branch_same`。

### 测试
- `TextQuestChainServiceTest` / `QuestHookRuntimeTest` 增加源码与目录契约。

## 权威与兼容
- 无网络字段变化，protocol 25。
- 未新增注册物品/方块/实体。

## 验证
- 定向任务测试通过。
- 普通 `./gradlew build --no-daemon` BUILD SUCCESSFUL。
- 全量 710 项测试，0 failures / 0 errors / 0 skipped。
- `mod_version=0.2.61`。

## 备份
- `.bak/20260720_051541_0.2.61_quest_advance/`

## 后续
1. CAST_* 注册表映射到 mode-aware `TalismanConsumeSpell`（或等价专用效果）。
2. 鉴定扣费先校验再写 NBT。
3. Curios 飞行 owner/完整性门禁。
4. 功法个性化成本 / 生活熟练度 / 旧 NBT 清理。
5. 实机客户端/专服/多人烟测。
