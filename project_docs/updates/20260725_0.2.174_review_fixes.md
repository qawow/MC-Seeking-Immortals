# 0.2.174 发布审查修复

- 凭据：删除未跟踪 `报错/` 目录及两个含 Minecraft 访问令牌的 ZIP 副本，并加入 `.gitignore`。账号侧令牌仍需持有人撤销或刷新。
- 声音：将五个对话 WAV 转换并替换为 22.05 kHz 单声道 Vorbis OGG，保持 `sounds.json` 自有事件路径不变。
- 秘境：21 个 `secret_realm_*` 入口精确指向对应专属维度，移除新增秘境到旧共享维度的软绑定。
- 商店：服务器停止时清空静态库存缓存，避免集成服务器在存档切换后复用旧库存状态。
- 构建修正：移除早期批次误恢复的 `jiangying_pill` 重复注册载体，保留其到 `jiangchen_pill` 的兼容别名；维度注册表从入口映射补种全部专属维度，并把对账清单同步到 28 个实际 JSON。
- 测试：五组定向测试通过；普通 `./gradlew build --no-daemon --max-workers=1` 在 1 分 23 秒内成功，236 个测试套件共 1,117 项全部通过，`aiPreflight` 记录 `0.2.174`。
- 版本与协议：`mod_version 0.2.173 -> 0.2.174`；网络字段、顺序、类型、注册与频道行为未变化，协议保持 `30`。
- 回滚：`.bak/20260725_201533_review_fixes/`；安全原因未备份凭据诊断包。
